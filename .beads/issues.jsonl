{"id":"acs-5v9","title":"acs v1: single-binary persistent ssh sessions","description":"Implement `acs` as designed in `docs/DESIGN.md`: one Rust binary that is the\nlocal client, the per-connection remote proxy and the per-session remote\nmaster, replacing the `dsh` shell function (ssh + dtach).\n\nInvariants every child must preserve:\n\n- Unfiltered stream: pty bytes reach the local terminal verbatim; keystrokes\n  reach the pty verbatim except the command key (DESIGN §2, §6).\n- Transport is the system `ssh`; no ssh-side configuration (DESIGN §3).\n- Lossless resume after a network drop (DESIGN §5.2).\n\nDone when every child is closed and DESIGN.md's Status reads Built.\n","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-09-18T00:27:52.940842Z","created_by":"michel","updated_at":"2026-09-18T02:06:23.678518Z","closed_at":"2026-09-18T02:06:23.678406Z","close_reason":"All 25 children closed: acs v1 implemented, tested on macOS and Linux, verified end to end over real ssh","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs"]}
{"id":"acs-5v9.1","title":"Scaffold the acs crate: size-tuned profile, role dispatch, verify script","description":"Create the Cargo project the rest of the plan builds on (DESIGN §9).\n\n- `Cargo.toml`: release profile `opt-level = \"z\"`, `lto = true`,\n  `codegen-units = 1`, `panic = \"abort\"`, `strip = true`. Dependencies kept\n  minimal: `rustix` (or `nix`) and `lexopt`; no async runtime, no clap.\n- `src/main.rs`: role dispatch — default client; hidden `_proxy`, `_master`,\n  `_install`, `_version`. Stubs are fine.\n- Module skeleton per DESIGN §9 layout (`proto.rs`, `client/`, `master/`,\n  `proxy.rs`, `install.rs`) and an `xtask/` workspace member placeholder.\n- `scripts/verify.sh`: `cargo fmt --check`, `cargo clippy -- -D warnings`,\n  `cargo test`, `npx markdownlint-cli2` on `**/*.md`.\n- Flip DESIGN.md `Status:` to \"In progress\" and its CLAUDE.md index row, in\n  the same change (repo convention).\n\nAcceptance: `scripts/verify.sh` passes; `cargo zigbuild --release --target\nx86_64-unknown-linux-musl` and `aarch64-unknown-linux-musl` build; the slim\nbinary size for each target is recorded in the PR/commit message as the\nbaseline against the < 1 MB budget.\n","status":"closed","priority":1,"issue_type":"chore","created_at":"2026-09-18T00:27:53.032841Z","created_by":"michel","updated_at":"2026-09-18T00:34:15.862017Z","closed_at":"2026-09-18T00:34:15.861911Z","close_reason":"Crate scaffold, size profile, role dispatch, verify script; slim release: x86_64-musl 320280 B, aarch64-musl 297928 B, aarch64-darwin 286080 B","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","infra"],"dependencies":[{"issue_id":"acs-5v9.1","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.032841Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.2","title":"Frame codec, message types and ACS-READY marker","description":"Implement the wire protocol of DESIGN §5.1 in `proto.rs`.\n\n- Frame: `type u8 | len u32 BE | payload`. Hard maximum frame length\n  (e.g. 1 MiB) rejected as a protocol error.\n- Incremental decoder that accepts arbitrary read boundaries.\n- All message types: HELLO (incl. client identity, `force`, TERM, COLORTERM,\n  size, optional resume{instance, offset}), WELCOME (resumed/gap/fresh,\n  created), BUSY, DATA (u64 offset), INPUT (u64 seq), ACK, RESIZE, PING/PONG,\n  DETACH, KILL, EXIT, TAKEOVER, STATUS/STATUS_REPLY, ERROR.\n- `PROTO_VERSION` constant; the `ACS-READY <proto>\\n` marker and the\n  `ACS-NEED <os> <arch>` line (DESIGN §3, §8): emit and a scanner that\n  discards arbitrary preceding noise.\n\nTests: round-trip every message; decode with the input split at every byte\nboundary; oversize and unknown-type frames error; marker found after\nshell-rc noise and across split reads.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.100672Z","created_by":"michel","updated_at":"2026-09-18T00:36:31.095955Z","closed_at":"2026-09-18T00:36:31.095852Z","close_reason":"proto.rs: frames, all message types, incremental decoder, ACS-READY/ACS-NEED scanner; 9 unit tests","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","proto"],"dependencies":[{"issue_id":"acs-5v9.2","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.100672Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.2","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:54.778527Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.3","title":"Resume buffers: output ring with u64 offsets and input ack tracking","description":"Data structures behind lossless resume (DESIGN §4.2, §5.2).\n\n- Output ring: append bytes, monotonically increasing u64 offsets,\n  configurable capacity (default 1 MiB); `read_from(offset)` returns\n  data / Gap (overwritten) / Future (invalid). Tracks the unsent watermark of\n  the attached client so the master can apply backpressure.\n- Input tracking: client-side queue of sent-but-unacked bytes (push, ack up\n  to seq, iterate for resend); master-side dedupe that writes only the part\n  of a resent INPUT beyond the last written seq.\n\nTests: wrap-around; gap detection exactly at the boundary; resend that\npartially overlaps what the master already wrote is not duplicated.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.169668Z","created_by":"michel","updated_at":"2026-09-18T00:37:51.144158Z","closed_at":"2026-09-18T00:37:51.144059Z","close_reason":"resume.rs: OutputRing (u64 offsets, gap/future, backpressure room), Unacked (client resend/rebase), InputDedupe (master); WELCOME gains input_seq; 9 unit tests","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","proto"],"dependencies":[{"issue_id":"acs-5v9.3","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.169668Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.3","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:54.868377Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.4","title":"Command-key detector: Ctrl-] Ctrl-] then d (detach) / x (exit)","description":"Pure state machine for command mode (DESIGN §6.1-6.3), fed stdin bytes plus\nan injected clock; outputs bytes to forward, actions (Detach, Exit) and the\nnext deadline.\n\n- First Ctrl-] held up to 400 ms; a lone Ctrl-] or Ctrl-] + other key is\n  forwarded as typed. Ctrl-] Ctrl-] within the window enters command mode;\n  `d` / `x` act; any other key, or 2 s of nothing, forwards all held bytes.\n- Recognise all encodings: legacy `0x1D`; kitty `CSI 93;5u` including event\n  types `:1/:2/:3`; modifyOtherKeys `CSI 27;5;93~`. Consume whole escape\n  sequences (never match inside one); buffer sequences split across reads;\n  release events follow their press (forwarded or swallowed together).\n- Bracketed paste (`CSI 200~` ... `CSI 201~`) passes through untouched.\n- Escape key and window configurable (`ACS_ESCAPE_TIMEOUT_MS`).\n\nTests: table-driven `(bytes, timings) -> (forwarded, action)` covering every\nrule above, including a pasted `^]^]d` and a terminal reply (`ESC`-prefixed)\narriving while a Ctrl-] is held.\n","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T00:27:53.238448Z","created_by":"michel","updated_at":"2026-09-18T00:41:34.276971Z","closed_at":"2026-09-18T00:41:34.276874Z","close_reason":"keys.rs Detector: legacy/kitty/modifyOtherKeys, releases, split reads, bracketed paste, passive replies/mouse, timeouts, configurable key; 20 table tests","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.4","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.238448Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.4","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:54.958716Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.5","title":"Terminal mode observer and reset-on-detach sequences","description":"Passive scanner over the output stream that records which terminal modes the\nremote program has turned on, and produces the matching resets for detach,\nexit, or an abandoned reconnect (DESIGN §6.4). It must never modify, delay or\nreorder the stream.\n\nModes: mouse (1000/1002/1003/1006/1015), bracketed paste (2004), alternate\nscreen (47/1047/1049), cursor visibility (25), focus events (1004), kitty\nkeyboard stack (`CSI > flags u` / `CSI < u`), modifyOtherKeys (`CSI > 4;N m`).\n\nTests: each sequence split at every byte boundary; reset output matches the\nrecorded state and is empty when nothing was set.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T00:27:53.306553Z","created_by":"michel","updated_at":"2026-09-18T00:42:52.682352Z","closed_at":"2026-09-18T00:42:52.682227Z","close_reason":"modes.rs ModeObserver: DEC modes (mouse, paste, alt screen, cursor, focus, DECCKM, 2026), kitty stack/set, modifyOtherKeys, keypad, scroll region; ordered reset; 7 tests incl. every split point","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.5","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.306553Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.5","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:55.051390Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.6","title":"Session names and per-uid socket directory","description":"Everything about where sessions live (DESIGN §4.1, §4.4, §4.5).\n\n- Name validation `[A-Za-z0-9._-]`; default `main` or `$ACS_DEFAULT_SESSION`.\n- Directory: `$ACS_SOCKET_DIR` or `/tmp/acs-<getuid()>` (never `$USER`,\n  never `$TMPDIR`). Create `0700`; on existing path `lstat`: must be a real\n  directory, not a symlink, owned by our uid, mode `0700` — else refuse with\n  an error naming the owner and suggesting `ACS_SOCKET_DIR`.\n- `sun_path` length check with a clear error.\n- `flock` helpers for the per-session creation lock and the directory lock\n  used by `--new` to pick the lowest free number.\n\nTests: symlinked dir refused; wrong mode refused; ownership check via an\ninjectable stat; too-long path; `--new` numbering with gaps and under\nconcurrent callers.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.376975Z","created_by":"michel","updated_at":"2026-09-18T00:44:42.301306Z","closed_at":"2026-09-18T00:44:42.301213Z","close_reason":"session.rs: name rules, ACS_DEFAULT_SESSION, /tmp/acs-<uid> or ACS_SOCKET_DIR with symlink/not-dir/foreign-owner refusal and mode tightening, sun_path check, create/dir flocks, lowest free number; sys.rs libc wrappers; 13 tests","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","remote","security"],"dependencies":[{"issue_id":"acs-5v9.6","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.376975Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.6","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:55.157206Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.7","title":"ssh command builder, option passthrough and remote prelude","description":"Build every ssh invocation (DESIGN §3, §7.1, §8).\n\n- Session calls: transport options first (`-T -e none -o ControlMaster=no\n  -o ControlPath=none -o ServerAliveInterval=0`), then the user's `-i`, `-p`,\n  `-J`, `-F`, `-o` in the order given, then `[user@]host`, then the remote\n  command. Side calls (`--list`, install) omit the transport options so they\n  keep the user's multiplexing.\n- ssh binary from `--ssh` / `ACS_SSH`, default `ssh` on PATH.\n- Hidden `--transport-cmd <cmd>` that replaces ssh with a local command, for\n  the integration harness (DESIGN §9.1).\n- Remote prelude (POSIX sh) with the client version baked in: try\n  `~/.local/share/acs/<v>/acs` then `/usr/local/lib/acs/<v>/acs`, `exec\n  _proxy <session> <mode>`, else print `ACS-NEED $(uname -s) $(uname -m)`.\n  Everything interpolated is shell-quoted (`-- command...` args are\n  arbitrary).\n\nTests: argv snapshots for session vs side calls; precedence (a user\n`-o ControlMaster=auto` comes after ours); quoting of hostile command args.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.441531Z","created_by":"michel","updated_at":"2026-09-18T00:45:57.041307Z","closed_at":"2026-09-18T00:45:57.041184Z","close_reason":"ssh.rs: Transport argv (transport opts first for session calls, side calls keep multiplexing, user -i/-p/-J/-F/-o in order, -- before destination, ACS_SSH, --transport-cmd hook), sh_quote, versioned prelude run under sh -c; WELCOME carries session name; 7 tests incl. executing the prelude","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.7","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.441531Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.7","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:55.256081Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.8","title":"Command-line parsing (dsh-compatible)","description":"`acs [ssh options] [user@]<host> [session] [-l|--list] [--new]\n[--no-reconnect] [--force] [-v] [-- command...]` with `lexopt` (DESIGN §4.4,\n§7, §7.1).\n\n- `-r` accepted and ignored (reconnect is the default); `-l` is `--list`;\n  ssh's `-l <login>` rejected with a hint to use `user@host` or\n  `-o User=`.\n- Invalid combinations rejected: `--list` with a session or `--new`;\n  `--new` with a session name.\n- `ACS_DEFAULT_SESSION` resolves the default session name.\n\nTests: table of argv -> parsed config / error message, including every\n`dsh` invocation form.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.509783Z","created_by":"michel","updated_at":"2026-09-18T00:47:37.357212Z","closed_at":"2026-09-18T00:47:37.357087Z","close_reason":"cli.rs: dsh-compatible parsing with lexopt; ssh -i/-p/-J/-F/-o passthrough in order; -r ignored; -l = --list with user@host hint; --new/--force/-v/--no-reconnect/--ssh/--transport-cmd; -- command; ACS_DEFAULT_SESSION; 8 tests","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.8","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.509783Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.8","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:55.357958Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.9","title":"Master: daemon lifecycle, pty, child and peer-uid check","description":"`acs _master` process lifecycle (DESIGN §4.2, §4.5).\n\n- Started by the proxy: double-fork, `setsid`, stdio to `/dev/null`; take the\n  session creation lock; bind the socket **before** spawning the child;\n  report readiness over a pipe (dtach's statusfd) so the proxy never races\n  the bind.\n- Child: `$SHELL -l` in `$HOME`, or the `-- command...` given; env `TERM`,\n  `COLORTERM` from HELLO, `ACS_SESSION=<name>`.\n- Reap the child on SIGCHLD, keep its wait status; unlink the socket on exit.\n- Every minute re-check the socket path and re-bind if it was removed\n  (tmpfiles cleanup).\n- On every accepted connection verify the peer uid (`SO_PEERCRED` on Linux,\n  `getpeereid` on macOS) and drop foreign uids.\n- Instance id: random u64 minted at creation.\n\nTests: start a master in a temp `ACS_SOCKET_DIR` running `/bin/sh -c`; two\nconcurrent starts produce one master; child exit status observed; socket\nremoved on exit and re-bound after deletion.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.580611Z","created_by":"michel","updated_at":"2026-09-18T00:55:32.577451Z","closed_at":"2026-09-18T00:55:32.577350Z","close_reason":"master.rs: spawn() with readiness pipe on fd 3, fork+setsid daemon, creation lock around check+bind, child on pty with setsid/TIOCSCTTY and TERM/COLORTERM/ACS_SESSION, SIGCHLD reaping, socket unlink on exit and re-bind after deletion, peer-uid check; tests: exit status, concurrent starts, rebind, missing session, protocol mismatch","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","remote"],"dependencies":[{"issue_id":"acs-5v9.9","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.580611Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.9","depends_on_id":"acs-5v9.2","type":"blocks","created_at":"2026-09-18T00:27:55.764769Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.9","depends_on_id":"acs-5v9.6","type":"blocks","created_at":"2026-09-18T00:27:55.660447Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.10","title":"Master: session protocol, takeover, resume, resize, kill","description":"The master's side of the protocol (DESIGN §4.2, §5.2, §5.3).\n\n- HELLO handling with attach / create / attach-or-create; WELCOME with\n  instance id and resumed / gap / fresh.\n- One active client: a new attach sends TAKEOVER to the previous one.\n- Output: pty reads go into the ring; DATA frames from the client's offset;\n  backpressure while attached (stop reading the pty when unsent bytes would\n  be overwritten); ring wraps freely while detached.\n- Input: INPUT dedupe by seq, ACK.\n- RESIZE via `TIOCSWINSZ`; on a fresh attach force a redraw by signalling\n  `tcgetpgrp(pty)` with SIGWINCH even when the size is unchanged.\n- PING/PONG; DETACH; KILL = SIGHUP to the child's process group, SIGKILL\n  after 3 s; EXIT{status} to the client on child exit.\n\nTests (framed test client over the unix socket): resume from an offset\nyields exactly the missed bytes; offset beyond the ring yields gap; a new\ninstance yields fresh; takeover; KILL kills a process group that ignores\nSIGHUP; backpressure engages with a stalled client and not when detached.\n","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T00:27:53.654336Z","created_by":"michel","updated_at":"2026-09-18T00:58:36.205856Z","closed_at":"2026-09-18T00:58:36.205695Z","close_reason":"Master protocol verified by 10 integration tests: exact resume replay, gap and fresh attach, same-identity silent takeover vs BUSY + force, KILL SIGHUP then SIGKILL after 3s, backpressure for a stalled client and none while detached, resize, forced redraw on fresh attach, input dedupe","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","remote"],"dependencies":[{"issue_id":"acs-5v9.10","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.654336Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.10","depends_on_id":"acs-5v9.2","type":"blocks","created_at":"2026-09-18T00:27:55.951511Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.10","depends_on_id":"acs-5v9.3","type":"blocks","created_at":"2026-09-18T00:27:56.051540Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.10","depends_on_id":"acs-5v9.9","type":"blocks","created_at":"2026-09-18T00:27:55.858819Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.11","title":"Proxy: connect-or-spawn master and relay frames","description":"`acs _proxy <session> <mode> --proto N` (DESIGN §3, §4.3).\n\n- Resolve the socket directory; connect; on ENOENT/ECONNREFUSED unlink a\n  stale socket and start the master (create modes only), waiting for its\n  readiness report.\n- Write `ACS-READY <proto>` to stdout before the first frame.\n- Check the protocol version in the client's HELLO; on mismatch answer\n  ERROR telling the user to finish or kill the session.\n- Splice stdin/stdout <-> socket until either side closes.\n\nTests: stale socket replaced; two proxies racing to create converge on one\nmaster; mismatched protocol version yields ERROR.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.724896Z","created_by":"michel","updated_at":"2026-09-18T01:04:28.366113Z","closed_at":"2026-09-18T01:04:28.366020Z","close_reason":"proxy.rs: connect-or-start (retries when another master wins), ACS-READY, HELLO proto check, --new naming under dir lock, splice relay, --list; 7 integration tests via stdio incl. stale socket, racing proxies, mismatch, missing session, --new numbering, dropped client detaches","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","remote"],"dependencies":[{"issue_id":"acs-5v9.11","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.724896Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.11","depends_on_id":"acs-5v9.2","type":"blocks","created_at":"2026-09-18T00:27:56.141554Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.11","depends_on_id":"acs-5v9.6","type":"blocks","created_at":"2026-09-18T00:27:56.240509Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.11","depends_on_id":"acs-5v9.9","type":"blocks","created_at":"2026-09-18T00:27:56.336048Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.12","title":"Session listing (--list) via STATUS","description":"`acs <host> --list` (DESIGN §4.3, §4.5).\n\n- Master answers STATUS with name, attached/detached, attached identity (or\n  last attached), creator identity, created-at, idle time, child command,\n  size, acs version.\n- `acs _proxy --list` enumerates `<dir>/*.sock`, queries each, reports and\n  removes stale sockets; no `ps` parsing.\n- Client prints an aligned table; `no sessions` when empty.\n\nTests: listing with attached, detached and stale sessions; output format.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T00:27:53.791059Z","created_by":"michel","updated_at":"2026-09-18T01:17:04.433440Z","closed_at":"2026-09-18T01:17:04.433303Z","close_reason":"list.rs: side call to _proxy --list, STATUS_REPLY frames rendered as NAME/STATE/WHO/IDLE/AGE/COMMAND table, 'no sessions', not-installed case; tests: unit table + integration with attached, detached and stale socket","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client","remote"],"dependencies":[{"issue_id":"acs-5v9.12","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.791059Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.12","depends_on_id":"acs-5v9.10","type":"blocks","created_at":"2026-09-18T00:27:56.432085Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.12","depends_on_id":"acs-5v9.11","type":"blocks","created_at":"2026-09-18T00:27:56.523191Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.13","title":"Client terminal: raw mode, guaranteed restore, signals","description":"Local tty handling (DESIGN §7).\n\n- Raw mode with dtach's termios flags; the original termios restored on\n  every exit path: normal return, SIGHUP/SIGTERM/SIGINT, and panic (drop\n  guard plus signal handler, `panic = \"abort\"`).\n- Self-pipe for signals; SIGWINCH surfaced as a resize event.\n- Single-threaded `poll` loop skeleton with timers (escape timeout, pings).\n\nTests: termios flag computation as a unit test; a pty-based test that the\nterminal is restored after the client is killed by a signal and after a\npanic.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.862722Z","created_by":"michel","updated_at":"2026-09-18T00:50:08.491164Z","closed_at":"2026-09-18T00:50:08.491056Z","close_reason":"tty.rs RawMode (dtach flags, suspend/resume) + emergency restore via panic hook and SIGTERM/HUP/QUIT/INT handlers; sys.rs termios/pty/poll/pipe/signal self-pipe/peer_uid/wait wrappers; pty-based tests prove restore after SIGTERM and after panic","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.13","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.862722Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.13","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:55.454697Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.14","title":"Integration test harness: local transport, pty runner, producers","description":"Shared test infrastructure for everything end-to-end (DESIGN §9.1).\n\n- Temp `ACS_SOCKET_DIR` and temp `HOME` per test.\n- Run the client under a pty with `--transport-cmd` pointing at the built\n  binary's `_proxy` (no ssh).\n- Deterministic producers (numbered lines, binary patterns incl. escape\n  sequences) and a byte-exact capture of the client's stdout.\n- A framed test client for driving the master/proxy directly.\n- Transport killer to simulate a network drop mid-stream.\n\nAcceptance: a smoke test attaches, runs a producer, and asserts byte\nequality of output; harness usable from `cargo test`.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.934679Z","created_by":"michel","updated_at":"2026-09-18T01:05:49.359008Z","closed_at":"2026-09-18T01:05:49.358738Z","close_reason":"tests/common: Remote fixture (temp HOME with versioned install, socket dir, pid-recording transport wrapper to cut links), pty Client runner (keys, resize, wait_for, exit code), binary pattern producer; smoke tests prove prelude->proxy->master->pty chain byte-exact and ACS-NEED","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","test"],"dependencies":[{"issue_id":"acs-5v9.14","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.934679Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.14","depends_on_id":"acs-5v9.10","type":"blocks","created_at":"2026-09-18T00:27:56.708246Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.14","depends_on_id":"acs-5v9.11","type":"blocks","created_at":"2026-09-18T00:27:56.615671Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.14","depends_on_id":"acs-5v9.7","type":"blocks","created_at":"2026-09-18T00:27:56.804669Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.15","title":"Client: ssh transport, handshake and byte-exact session","description":"The client's session path without reconnect (DESIGN §3, §4.4, §5, §6, §7).\n\n- Spawn ssh via the builder; sync on `ACS-READY` (discard noise, show it\n  under `-v`); detect `ACS-NEED` and hand off to install.\n- HELLO with TERM, COLORTERM, size; DATA written verbatim to stdout while\n  tracking the last offset; stdin through the command-key detector into\n  INPUT with seq; RESIZE on SIGWINCH.\n- `d`: DETACH, restore terminal (with observer resets), print\n  `acs: detached from <host>/<name> - reattach with: acs <host> <name>`,\n  exit 0. `x`: KILL, wait for EXIT, exit with the child's status.\n- On creating a session print `acs: new session '<name>' on <host>` on\n  stderr before raw mode.\n- Exit codes per DESIGN §7.\n\nTests (harness): attach-or-create, byte-exact output incl. escape sequences,\ndetach and re-attach, `x` ends the remote session, EXIT status propagated,\nnew-session message.\n","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T00:27:54.004739Z","created_by":"michel","updated_at":"2026-09-18T01:09:38.116376Z","closed_at":"2026-09-18T01:09:38.116230Z","close_reason":"client.rs: dial with marker sync, HELLO/WELCOME, raw mode, verbatim DATA with offset tracking, detector->INPUT with seq, RESIZE, d/x, new-session and reattach messages, mode resets, exit codes; 11 pty tests incl. 300 KB byte-exact output, detach/reattach, x, exit status, single-escape passthrough","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.004739Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9.13","type":"blocks","created_at":"2026-09-18T00:27:56.988175Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9.14","type":"blocks","created_at":"2026-09-18T00:27:56.896223Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9.4","type":"blocks","created_at":"2026-09-18T00:27:57.080490Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9.5","type":"blocks","created_at":"2026-09-18T00:27:57.169988Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9.7","type":"blocks","created_at":"2026-09-18T00:27:57.261053Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9.8","type":"blocks","created_at":"2026-09-18T00:27:57.360296Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.16","title":"Client: liveness, automatic reconnect and lossless resume","description":"Resume after network drops (DESIGN §5.2-5.4).\n\n- PING after 3 s of silence; link dead after 10 s without a frame: kill ssh\n  and redial with backoff 1 s -> 30 s, reset after a connection that lasted\n  30 s. `--no-reconnect` disables.\n- HELLO with resume{instance, offset}; handle resumed / gap (clear + redraw)\n  / fresh (\"session was restarted\").\n- Resend unacked input; drop keys typed while the link is known down, but\n  keep command mode working (`d` detaches while down; `x` reports it needs\n  the link).\n- Status while disconnected: nothing for < 10 s; then one bottom-row line\n  with save/restore cursor and the xterm title stack; one forced redraw\n  after a resume that followed a status line.\n- Cooked mode while ssh authenticates on redial (password / key touch).\n- TAKEOVER ends the client with a message and the reattach hint.\n\nTests (harness): kill the transport mid-stream and assert the client's\nstdout equals the producer's output byte for byte; input sent before the\ndrop is not duplicated; ring overflow while disconnected yields gap\nhandling; `--no-reconnect` exits on drop.\n","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T00:27:54.075054Z","created_by":"michel","updated_at":"2026-09-18T01:15:55.463071Z","closed_at":"2026-09-18T01:15:55.462952Z","close_reason":"reconnect.rs: Liveness (PING after 3s, dead after 10s), Backoff 1s->30s with reset, redial with resume, unacked resend, keys dropped while down but d works and x explains, status line + title stack, redraw via resize pair; 7 pty tests (cut, freeze, input once, --no-reconnect, detach while down, x needs link, gap) + unit tests; daemons close inherited fds","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.16","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.075054Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.16","depends_on_id":"acs-5v9.15","type":"blocks","created_at":"2026-09-18T00:27:57.464472Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.16","depends_on_id":"acs-5v9.3","type":"blocks","created_at":"2026-09-18T00:27:57.559623Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.17","title":"Client: immediate redial on network change","description":"Stretch goal from DESIGN §5.3: skip the backoff wait when the default route\nchanges (Wi-Fi switch, wake from sleep) — route socket / SCNetworkReachability\non macOS, netlink on Linux — where it is cheap to observe.\n\nTests: the reconnect scheduler redials immediately on an injected\n\"network changed\" event.\n","status":"closed","priority":3,"issue_type":"feature","created_at":"2026-09-18T00:27:54.142691Z","created_by":"michel","updated_at":"2026-09-18T01:40:22.774873Z","closed_at":"2026-09-18T01:40:22.774739Z","close_reason":"netwatch.rs: PF_ROUTE (address/interface messages only, ARP churn ignored) on macOS, NETLINK_ROUTE link/addr groups on Linux, FIFO injection for tests; offline wait redials at once on a change (max once per 2s) and resets backoff; unit tests + integration test (20s backoff bypassed)","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.17","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.142691Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.17","depends_on_id":"acs-5v9.16","type":"blocks","created_at":"2026-09-18T00:27:57.654190Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.18","title":"Shared accounts: client identity and confirmed takeover","description":"Accident protection when several people use one remote account (DESIGN §4.5).\n\n- Client identity `<local user>@<local hostname>`, overridable with\n  `ACS_IDENTITY`, sent in HELLO; the master records attached and creator\n  identities.\n- Cross-identity attach gets BUSY{identity, since}; the client asks\n  `take over? [y/N]` on the terminal, retries HELLO with `force` on yes;\n  without a terminal it fails; `--force` skips the question.\n- Same identity takes over silently (own dropped connection, own second\n  terminal).\n\nTests (harness): same identity silent; different identity BUSY; force\ntakes over; non-tty without `--force` fails.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T00:27:54.213017Z","created_by":"michel","updated_at":"2026-09-18T01:18:22.691743Z","closed_at":"2026-09-18T01:18:22.691619Z","close_reason":"Client identity (ACS_IDENTITY or user@host) in HELLO; BUSY answered with a cooked-mode [y/N] prompt naming who and since when, --force skips, non-tty declines; same identity silent takeover; ACS_DEFAULT_SESSION; 5 pty tests","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client","remote"],"dependencies":[{"issue_id":"acs-5v9.18","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.213017Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.18","depends_on_id":"acs-5v9.10","type":"blocks","created_at":"2026-09-18T00:27:57.848530Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.18","depends_on_id":"acs-5v9.15","type":"blocks","created_at":"2026-09-18T00:27:57.747247Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.19","title":"Multi-user integration tests in a Linux container","description":"Prove isolation between Unix accounts (DESIGN §4.5, §9.1) in a Linux\ncontainer with two users (docker/podman; the macOS host cannot switch uids\nwithout root).\n\n- Another user's pre-created `/tmp/acs-<uid>` (squatted, and symlinked) is\n  refused.\n- A connection from another uid to a master's socket is dropped.\n- Two users' `main` sessions are independent.\n- The whole `cargo test` suite also runs on Linux musl in the container.\n\nAcceptance: one script (`scripts/test_linux.sh`) runs it end to end.\n","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-18T00:27:54.284950Z","created_by":"michel","updated_at":"2026-09-18T01:57:26.611781Z","closed_at":"2026-09-18T01:57:26.611566Z","close_reason":"tests/multiuser.rs (squatted dir, symlinked dir, foreign peer uid dropped by master, independent per-user main) + scripts/test_linux.sh running the full suite on Linux musl and the multi-user tests as root with alice/bob; all pass. The Linux run found and fixed a master backpressure stall and a replaced-binary spawn failure.","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","security","test"],"dependencies":[{"issue_id":"acs-5v9.19","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.284950Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.19","depends_on_id":"acs-5v9.18","type":"blocks","created_at":"2026-09-18T00:27:57.945274Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.19","depends_on_id":"acs-5v9.6","type":"blocks","created_at":"2026-09-18T00:27:58.051922Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.20","title":"Payload set: format, ELF trailer and Mach-O embed","description":"Carry slim remote binaries inside a complete binary (DESIGN §8.1).\n\n- Payload set format: index of (target triple, compressed size, SHA-256,\n  gzip bytes).\n- ELF: trailer `[slim][P entries][index][magic + index offset]`, read back\n  through `current_exe()`; writer used by xtask and by `_install --finish`.\n- Mach-O: `embed-payloads` cargo feature using `include_bytes!` of a path\n  supplied at build time.\n- API: `own_target()`, `is_complete()`, `payload_for(target)`.\n\nTests: build a synthetic trailer and read it back; truncated or corrupt\ntrailer means slim, never a crash.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:54.356588Z","created_by":"michel","updated_at":"2026-09-18T01:20:14.979366Z","closed_at":"2026-09-18T01:20:14.979230Z","close_reason":"payload.rs: blob format (gz entries + index + footer), build/parse, ELF trailer via current_exe, embed-payloads feature via include_bytes (build.rs ACS_PAYLOADS_FILE), own target (build.rs ACS_TARGET), uname->target map; dependency-free sha256.rs with FIPS vectors; corrupt/truncated input never panics","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","install"],"dependencies":[{"issue_id":"acs-5v9.20","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.356588Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.20","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:55.553831Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.21","title":"Remote self-install: ACS-NEED, versioned install, _install --finish","description":"Install `acs` on a remote on first contact (DESIGN §8, §8.1, §4.5).\n\n- On `ACS-NEED <os> <arch>` map to a target. Case 1 (own ELF target):\n  stream own file with `cat`. Case 2 (Linux target in P): stream gz(slim_r)\n  to `gzip -dc > ~/.local/share/acs/<v>/acs.new.<token>`, then run\n  `acs.new.<token> _install --finish --sha256 <digest>` with P on stdin,\n  which appends the trailer, verifies the digest, renames to `acs`, and\n  repoints `~/.local/bin/acs` (temp symlink + rename). Case 3 (macOS other\n  arch): clear manual-install message.\n- Random token per install so concurrent installs converge; missing `gzip`\n  gives a clear error; side calls keep the user's ssh multiplexing; redial\n  after install.\n\nTests (harness, local \"remote\" with temp HOME): install from slim fails\ncleanly with the `cargo xtask dist` hint; complete binary installs and the\ninstalled copy is byte-identical to the dist output for that target;\nconcurrent same-version installs converge; two versions coexist and each\nserves its own client.\n","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T00:27:54.429364Z","created_by":"michel","updated_at":"2026-09-18T01:36:46.414425Z","closed_at":"2026-09-18T01:36:46.414306Z","close_reason":"install.rs: plan (self-copy for own target; gzip'd slim + payload blob for other Linux targets; clear errors otherwise), side calls, _install --finish (sha256 check, new file since running exe is ETXTBSY, atomic rename, ~/.local/bin symlink swap), once per run; --version lists installable targets; 6 integration tests incl. payload path via fake uname, concurrent installs, corrupt upload; verified dist binaries in docker","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","install"],"dependencies":[{"issue_id":"acs-5v9.21","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.429364Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.21","depends_on_id":"acs-5v9.11","type":"blocks","created_at":"2026-09-18T00:27:58.336139Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.21","depends_on_id":"acs-5v9.15","type":"blocks","created_at":"2026-09-18T00:27:58.242296Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.21","depends_on_id":"acs-5v9.20","type":"blocks","created_at":"2026-09-18T00:27:58.146198Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.22","title":"Prune unused remote versions","description":"Housekeeping from DESIGN §8: `_proxy` touches its version directory on\nstart; version directories untouched for 30 days with no running master of\nthat version (per STATUS) are removed at the next proxy start.\n\nTests: old unused version pruned; old version with a live master kept;\ncurrent version never pruned.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-18T00:27:54.497216Z","created_by":"michel","updated_at":"2026-09-18T01:38:28.593295Z","closed_at":"2026-09-18T01:38:28.592936Z","close_reason":"prune.rs: proxy touches its version dir; at most daily (.pruned marker) removes other version dirs untouched for 30 days unless a live master reports that version (STATUS); unit test with backdated mtimes + integration test via proxy start","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","install"],"dependencies":[{"issue_id":"acs-5v9.22","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.497216Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.22","depends_on_id":"acs-5v9.12","type":"blocks","created_at":"2026-09-18T00:27:58.533734Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.22","depends_on_id":"acs-5v9.21","type":"blocks","created_at":"2026-09-18T00:27:58.434063Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.23","title":"cargo xtask dist: slim builds, payloads, complete binaries, size budget","description":"Release build pipeline (DESIGN §8.1, §9).\n\n1. Slim builds for `aarch64-apple-darwin`, `x86_64-apple-darwin` (cargo) and\n   `x86_64-unknown-linux-musl`, `aarch64-unknown-linux-musl`\n   (`cargo zigbuild`).\n2. gzip -9 the Linux slim builds into the payload set.\n3. Complete Linux binaries by appending the trailer; complete macOS binaries\n   by rebuilding with `--features embed-payloads`, then ad-hoc `codesign`.\n4. Output `dist/<target>/acs` plus a size report; fail if slim > 1 MB or\n   complete > 2 MB.\n\nAcceptance: `cargo xtask dist` on this Mac produces all four; `codesign -v`\npasses on the macOS ones; replace the size estimates in DESIGN §8.1 with the\nmeasured numbers.\n","status":"closed","priority":1,"issue_type":"chore","created_at":"2026-09-18T00:27:54.565846Z","created_by":"michel","updated_at":"2026-09-18T01:36:46.643553Z","closed_at":"2026-09-18T01:36:46.643290Z","close_reason":"cargo xtask dist builds 4 targets (zigbuild musl, native macOS), payload blob, complete Linux (appended) and macOS (embedded, ad-hoc signed, codesign -v ok), size report with budget; measured slim 473-602 KB, complete 1.10-1.21 MB; linux binaries verified in docker alpine (aarch64 native, x86_64 emulated)","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","infra","install"],"dependencies":[{"issue_id":"acs-5v9.23","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.565846Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.23","depends_on_id":"acs-5v9.20","type":"blocks","created_at":"2026-09-18T00:27:58.627402Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.24","title":"Manual verification matrix against real hosts","description":"Check the things automated tests cannot (DESIGN §9.1) and record results\nin `docs/VERIFICATION.md`.\n\n- TUIs: vim (tag jump, mouse), Claude Code, htop, less; OSC 52 copy; a\n  kitty-keyboard-protocol TUI (command key still works); local scrollback.\n- Drops: `pfctl` block, Wi-Fi switch, laptop sleep/wake; output lossless.\n- Install: macOS client -> Linux x86_64 and aarch64 hosts; Linux client ->\n  next hop.\n- Shared account: two identities, confirmation prompt.\n- `-i` key selection against a host that needs it (e.g. corello).\n","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-18T00:27:54.632672Z","created_by":"michel","updated_at":"2026-09-18T02:05:19.205874Z","closed_at":"2026-09-18T02:05:19.205710Z","close_reason":"scripts/e2e_ssh.sh + tests/e2e_ssh.rs over real ssh to a container host: install on first contact, -i selection, vim, OSC 52/8, kitty command key, mouse, killed connection, frozen host, --list — all 9 pass; results and the hand-only checks (real terminal rendering, Claude Code, real Wi-Fi/sleep, corello) recorded in docs/VERIFICATION.md. Added ConnectTimeout=10 to the session transport.","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","test"],"dependencies":[{"issue_id":"acs-5v9.24","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.632672Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.24","depends_on_id":"acs-5v9.12","type":"blocks","created_at":"2026-09-18T00:27:59.105449Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.24","depends_on_id":"acs-5v9.16","type":"blocks","created_at":"2026-09-18T00:27:58.721730Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.24","depends_on_id":"acs-5v9.18","type":"blocks","created_at":"2026-09-18T00:27:58.817298Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.24","depends_on_id":"acs-5v9.21","type":"blocks","created_at":"2026-09-18T00:27:58.911623Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.24","depends_on_id":"acs-5v9.23","type":"blocks","created_at":"2026-09-18T00:27:59.006328Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.25","title":"Docs and migration from dsh","description":"Finish the rollout (DESIGN §10).\n\n- `README.md`: install, usage, command keys, environment variables.\n- DESIGN.md `Status:` -> Built, and its CLAUDE.md index row, in the same\n  change.\n- Migration note: run `acs` alongside `dsh`; then replace the `dsh` function\n  in `~/.zshrc` with `alias dsh=acs` and drop the dtach dependency (the\n  dotfile change is the user's to make).\n","status":"closed","priority":2,"issue_type":"docs","created_at":"2026-09-18T00:27:54.702768Z","created_by":"michel","updated_at":"2026-09-18T02:06:23.526943Z","closed_at":"2026-09-18T02:06:23.526802Z","close_reason":"README.md (install, usage, command keys, ssh options, reconnect, shared accounts, environment, development scripts, dsh migration); DESIGN status Built; CLAUDE.md index rows for README/DESIGN/VERIFICATION and the test scripts; scripts/verify.sh passes","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","docs"],"dependencies":[{"issue_id":"acs-5v9.25","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.702768Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.25","depends_on_id":"acs-5v9.24","type":"blocks","created_at":"2026-09-18T00:27:59.210331Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-m9i","title":"Automatic semantic versioning (scripts/version-bump.sh)","description":"Bump the version automatically after each merge: PATCH for small fixes, MINOR for larger fixes and features, MAJOR only on explicit instruction (resets MINOR and PATCH). Reads unreleased commits since the last vX.Y.Z tag, updates Cargo.toml/Cargo.lock, pushes a chore(release) commit and tag to origin/main via a throwaway worktree. Run by the ship skill.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T02:52:48.289045Z","created_by":"michel","updated_at":"2026-09-18T02:56:34.924724Z","closed_at":"2026-09-18T02:56:34.924634Z","close_reason":"cargo xtask bump + scripts/version-bump.sh: PATCH small fixes, MINOR features/larger fixes (>=300 shipped lines, Semver trailer or semver label), MAJOR only via --major (resets minor/patch); release commit + annotated tag pushed atomically from a throwaway worktree; idempotent; docs/VERSIONING.md; unit tests + end-to-end lifecycle test","source_repo":"semver","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/semver","compaction_level":0,"original_size":0,"labels":["acs","infra"]}
{"id":"acs-mam","title":"Publish release binaries to GitHub Releases","description":"Attach per-platform archives (acs-<version>-<target>.tar.gz), SHA256SUMS and install notes to a GitHub Release for each vX.Y.Z tag, built from the exact tag. scripts/release-binaries.sh does it; scripts/version-bump.sh calls it after cutting a release so shipping publishes binaries. Packaging lives in cargo xtask package (tested).","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T03:13:45.205920Z","created_by":"michel","updated_at":"2026-09-18T03:17:18.448517Z","closed_at":"2026-09-18T03:17:18.448422Z","close_reason":"cargo xtask package (archives with acs + README, SHA256SUMS, NOTES.md) + scripts/release-binaries.sh (build tag in throwaway worktree, gh release create/upload) + version-bump.sh publishes new tags; tests for packaging; v0.1.0 dry run verified (checksums, macOS and Linux binaries run)","source_repo":"release-binaries","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/release-binaries","compaction_level":0,"original_size":0,"labels":["acs","infra"]}
{"id":"acs-xfk","title":"Add a YAML config file (/etc/acs, ~/.config/acs) with install_on_remote","description":"Add a YAML configuration file for the acs client.\n\nUser's ask: \"add support for a configuration file, stored as YAML, using the latest unix directory standard, so local in ~/.config/acs/config.yaml as well as a global /etc/acs/config.yaml equivalent. If the global one exists, it is read first, overridden by the local one. Individual parameters override global, dictionaries and arrays are additive.\"\n\n- Locations (XDG Base Directory): global `/etc/acs/config.yaml`, then local `$XDG_CONFIG_HOME/acs/config.yaml` (default `~/.config/acs/config.yaml`). Either may be missing.\n- Merge: the local file wins for scalars; dictionaries merge key by key (recursively); arrays concatenate (global entries first).\n- First option: `install_on_remote` (bool, default `true`). When false, the ACS-NEED path (src/install.rs) does not install; it exits with a message saying acs is missing on the host and how to install it.\n- `hosts`: a map of alias → list of entries `{host: string, user: string (optional), reachability_check: bool (default true)}`. This bead only parses and validates it; using it is the alias-resolution bead.\n- A malformed file is an error that names the file and line, not a silent default.\n- Nothing exists yet: no config code in src/, and today the only knobs are env vars (README \"Environment\").\n- Binary size matters (budget 1 MB slim, DESIGN §9; deps today are only libc and lexopt). Choose between a small YAML crate and a hand-written parser for the subset used, and record the size cost.\n\nDone when: both files are read and merged with those rules; `install_on_remote: false` stops a remote install; unit tests cover the merge rules, the defaults and error reporting; README and DESIGN document the file.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T14:23:35.734333Z","created_by":"michel","updated_at":"2026-09-18T14:54:06.164666Z","closed_at":"2026-09-18T14:54:06.164586Z","close_reason":"Implemented: YAML config (/etc/acs, XDG local) with merge rules, install_on_remote, hosts parsing; hand-written YAML subset (+17 KB)","source_repo":"beads-intake-config","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-config","compaction_level":0,"original_size":0,"labels":["acs","config"]}
{"id":"acs-iff","title":"Add acs config subcommands to edit the configuration","description":"Add `acs config …` subcommands to manage the configuration file from the command line.\n\nUser's ask: \"add options to the cli to allow configuration of acs through the command line, something like `acs config host add <alias> <hostname> --reachability-check`\".\n\nProposed shape (settle the details during implementation):\n\n- `acs config show`: prints the merged configuration and which file each value came from.\n- `acs config get <key>` and `acs config set <key> <value>`, e.g. `acs config set install_on_remote false`.\n- `acs config host add <alias> <host> [--user U] [--no-reachability-check]`: appends an entry, so repeated adds give an alias fallback hosts in order.\n- `acs config host remove <alias> [<host>]` and `acs config host list`.\n- Writes go to the local file (`~/.config/acs/config.yaml`); `--global` targets `/etc/acs/config.yaml` and needs the right permissions.\n\nNotes:\n\n- `config` becomes a reserved first argument. Today it would be read as a host name (src/cli.rs), so a host literally called `config` needs a way through, e.g. `acs -- config` or `user@config`. Document it.\n- Writing must keep the file readable and stable (no reordering of what it did not touch); comments are nice to keep but not required.\n\nDone when: every subcommand works against a temp HOME in tests; errors (unknown key, wrong type, missing alias) are clear; README documents it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T14:23:38.165184Z","created_by":"michel","updated_at":"2026-09-18T15:04:31.998311Z","closed_at":"2026-09-18T15:04:31.998233Z","close_reason":"Implemented: acs config show/get/set/unset/path and host list/add/remove, --global, comment-preserving validated atomic writes; config reserved as first arg","source_repo":"beads-intake-config","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-config","compaction_level":0,"original_size":0,"labels":["acs","cli","config"],"dependencies":[{"issue_id":"acs-iff","depends_on_id":"acs-xfk","type":"blocks","created_at":"2026-09-18T14:23:47.272385Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-ue2","title":"Resolve host aliases on connect with ping check and fallback hosts","description":"Resolve `acs <name>` through the configured host aliases, checking reachability and falling back to alternate hosts.\n\nUser's ask: \"whenever connecting to a host, use the name as an alias if it is defined in the configuration. When it is, connect to the 'hostname', with user 'user' if defined (otherwise SSH will use its config). If reachability_check is true, use icmp ping once to check if the host is up. If it is, use it; if it is not, check the alternate 'host' if available. If not, display an error message and exit.\"\n\n- Lookup: if the destination given to acs (without a `user@` part) is a key in `hosts`, try its entries in order.\n- An entry with `reachability_check: true` is pinged once: `ping -c 1` with a short timeout. The timeout flag differs between macOS (`-t`) and Linux (`-W`). The first entry that answers is used. An entry with `reachability_check: false` is taken without a check.\n- The chosen entry becomes the ssh destination: `user@host` if `user` is set, otherwise just `host`, so ~/.ssh/config decides the user.\n- None reachable: print which hosts were tried and exit with the unreachable code (255, src/client.rs `code::UNREACHABLE`).\n- Applies to every ssh call: session, `--list`, install. On a redial (src/reconnect.rs) the alias should be resolved again, so a fallback host is picked up after a network change. The master session stays the same only if the fallback is the same machine; say so in the docs.\n- A name that is not an alias keeps today's behaviour exactly.\n- `-v` should show which entry was chosen and why.\n\nDone when: tests cover alias hit and miss, fallback to the second entry, `reachability_check: false`, all-unreachable → error/255, and `user` handling (ping is injected so tests don't need ICMP); README documents it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T14:23:40.843666Z","created_by":"michel","updated_at":"2026-09-18T14:59:50.225731Z","closed_at":"2026-09-18T14:59:50.225629Z","close_reason":"Implemented: acs <alias> resolves through hosts with ping check (ACS_PING), fallback, user handling, exit 255 when none answers, re-resolved on redial","source_repo":"beads-intake-config","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-config","compaction_level":0,"original_size":0,"labels":["acs","client","config"],"dependencies":[{"issue_id":"acs-ue2","depends_on_id":"acs-xfk","type":"blocks","created_at":"2026-09-18T14:23:47.372937Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-3ej","title":"Add a curl | sh installer for acs (per-platform install dir)","description":"Add a one-line installer: `curl -fsSL <url> | sh` installs acs the way the platform expects.\n\nUser's ask: \"create an install script that someone can download which installs acs automatically according to the rules of the platform it is being installed on. Similar to other apps: `curl <something> | <some shell command>`.\"\n\nProposed design:\n\n- `scripts/install.sh`, POSIX sh. It is also published as a release asset by `scripts/release-binaries.sh`, so the stable URL is `https://github.com/michel-onstein/acs/releases/latest/download/install.sh`. The repo is public.\n- It maps `uname -s` / `uname -m` to one of the four release targets: macOS arm64/x86_64, Linux x86_64/aarch64. Anything else is a clear error.\n- It downloads `acs-<version>-<target>.tar.gz` and `SHA256SUMS` from the release (latest by default; `ACS_VERSION=0.2.0` pins one) and refuses to install on a checksum mismatch. It uses `curl` or `wget`, and `shasum -a 256` or `sha256sum`.\n- Install location follows each platform's rules: per-user `~/.local/bin` (XDG) by default, or `/usr/local/bin` when run as root or with `ACS_INSTALL_DIR`. It warns, with the line to add, when the directory is not on `PATH`.\n- Re-running it upgrades in place, and it prints the installed `acs --version`.\n\nToday install is manual: README \"Install\" has a curl + tar + install recipe, and the release assets exist since v0.1.0.\n\nDone when: the script passes shellcheck; tests run it against a real release in Alpine and Ubuntu containers and in a temp HOME on macOS (default dir, pinned version, checksum failure, unsupported platform); README leads with the one-liner.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T14:34:30.356987Z","created_by":"michel","updated_at":"2026-09-18T15:10:22.059364Z","closed_at":"2026-09-18T15:10:22.059260Z","close_reason":"Implemented: scripts/install.sh (curl|sh, wget fallback, SHA256SUMS check, versioned layout ~/.local or /usr/local, ACS_VERSION/ACS_INSTALL_DIR), published as a release asset; scripts/test_install.sh passes on macOS, Alpine and Ubuntu as root and user","source_repo":"beads-intake-install","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-install","compaction_level":0,"original_size":0,"labels":["acs","install"]}
{"id":"acs-fh5","title":"Check GitHub weekly for a newer acs release and show a message","description":"Check GitHub for a newer acs release once a week and tell the user.\n\nUser's ask: \"have the acs binary check for a new release on github once a week, if a new release is available display a message\".\n\nProposed design:\n\n- Only the local client checks; the remote roles (`_proxy`, `_master`, `_install`) never do.\n- At most once every 7 days, recorded in `$XDG_STATE_HOME/acs/update-check` (default `~/.local/state/acs/update-check`). It queries `https://api.github.com/repos/michel-onstein/acs/releases/latest` and compares `tag_name` to our version with semver.\n- acs has no HTTP/TLS client, and adding one would break the 1 MB size budget (DESIGN §9). So it shells out to `curl` with a short timeout (for example `-m 3`) and never blocks or delays connecting: run it in the background and show the result on the next start, or finish within the timeout before raw mode.\n- The message goes to stderr outside the session stream, e.g. `acs 0.3.0 is available (you have 0.2.0) — run: acs upgrade`. It is shown once per new version, not on every run.\n- Can be turned off with `ACS_NO_UPDATE_CHECK=1`, and with an `update_check: false` option once the YAML config file (acs-xfk) exists. Being offline or rate-limited stays silent.\n\nDone when: unit tests cover the interval, the version comparison and the \"shown once\" state; an integration test injects the API response (e.g. a fake curl on PATH); README documents it and how to turn it off.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T14:34:32.181828Z","created_by":"michel","updated_at":"2026-09-18T15:22:51.362564Z","closed_at":"2026-09-18T15:22:51.362466Z","close_reason":"Implemented: weekly background _update-check (SHA256SUMS via curl, 3 s), state in XDG_STATE_HOME, message once per new version before connecting, off via ACS_NO_UPDATE_CHECK=1 or update_check: false; silent offline","source_repo":"beads-intake-install","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-install","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-fh5","depends_on_id":"acs-uko","type":"blocks","created_at":"2026-09-18T14:34:41.921922Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-uko","title":"Add acs upgrade: self-upgrade from GitHub releases","description":"Add `acs upgrade`: replace the running acs with the latest (or a given) GitHub release.\n\nUser's ask: \"add ability to do a self upgrade (from github)\".\n\nProposed design:\n\n- `acs upgrade [--version X.Y.Z] [--check]`. `upgrade` becomes a reserved first argument, like `config` in acs-iff.\n- It finds the latest release (or the given one), downloads `acs-<version>-<own target>.tar.gz` and `SHA256SUMS` via `curl` (acs has no HTTP/TLS client; DESIGN §9 size budget), and verifies the checksum.\n- It replaces the binary behind `current_exe()` atomically: write a temp file next to it, then rename it into place, keeping the mode. If that directory is not writable (e.g. `/usr/local/bin`), it says to re-run with sudo.\n- It refuses a downgrade unless `--version` asks for it, and does nothing when already current. `--check` only reports.\n- Remote hosts need nothing: the next connection finds no binary for the new version and installs it (versioned install, DESIGN §8).\n- On macOS, a curl download carries no quarantine attribute; the ad-hoc signature must still verify after the rename.\n- The download-and-verify logic should match the one-line installer (same asset names), so the two agree.\n\nDone when: tests cover upgrading from a fake release server or injected curl (success, checksum mismatch, already current, unwritable directory), a real upgrade from v0.1.0 → latest is exercised once, and README documents `acs upgrade`.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T14:34:34.770077Z","created_by":"michel","updated_at":"2026-09-18T15:16:37.097018Z","closed_at":"2026-09-18T15:16:37.096920Z","close_reason":"Implemented: acs upgrade [--version X.Y.Z] [--check] via SHA256SUMS + curl/wget, checksum and --version sanity check, atomic replace (plain or versioned layout with links), sudo hint; real 0.1.0->0.2.0 upgrade verified on macOS (codesign valid) and Linux","source_repo":"beads-intake-install","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-install","compaction_level":0,"original_size":0,"labels":["acs","client","install"]}
{"id":"acs-q9t","title":"Fix payload install failing when the login shell prints to stdout","description":"Step 1 of the payload install plan (`gzip -dc > tmp`, src/install.rs ~L114) prints nothing, and `side_call` (src/install.rs ~L150) errors with \"unexpected reply while installing\" whenever stdout is non-empty and no line is exactly `ok`. Any `.bashrc` echo or motd script that writes to stdout for non-interactive sessions — the very noise the ACS-READY marker was designed to tolerate (DESIGN §3) — therefore fails the macOS → Linux self-install after the upload succeeded, leaving `acs.new.<token>` behind. The self-copy path only passes because the finisher happens to print `ok`.\n\nDone when the upload step is judged on exit status alone (or prints its own marker) and tests/install.rs has a case with a chatty login shell.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-18T15:14:21.073925Z","created_by":"michel","updated_at":"2026-09-18T15:28:29.489556Z","closed_at":"2026-09-18T15:28:29.489444Z","close_reason":"Fixed with regression tests (bugs-install branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-tb1","title":"Stop link_bin clobbering or downgrading ~/.local/bin/acs","description":"`link_bin` (src/install.rs ~L279) creates a symlink to the version just installed and renames it over `~/.local/bin/acs` unconditionally. Consequences: a hand-installed regular file there (what the release notes tell users to `install -m 755`) is replaced by a symlink into `~/.local/share/acs/<v>/`; an older client connecting to a host with a newer version installed silently downgrades the host's link (DESIGN §8 says it points at the *newest* installed version); a later prune of that version leaves the link dangling.\n\nDone when the link is only replaced if absent or a symlink into `~/.local/share/acs/`, and only when the target version is >= the one it currently points at, with a test for a pre-existing file and a pre-existing newer symlink.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-18T15:14:21.148801Z","created_by":"michel","updated_at":"2026-09-18T15:28:29.492889Z","closed_at":"2026-09-18T15:28:29.492809Z","close_reason":"Fixed with regression tests (bugs-install branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-u3c","title":"Fix master dropping the child's final output at exit (stale revents)","description":"The \"pty drained\" check (src/master.rs ~L453-458) decides from the revents of the poll that just returned: if the child has been reaped, POLLIN was requested and the pty did not report POLLIN, `pty_open = false` and the next iteration goes to `finish()` without reading the pty again. But `sys::poll` maps EINTR to `Ok(0)` with all revents zero, and SIGCHLD is installed without SA_RESTART, so the child's own exit interrupts the poll; `reap()` then sets `status` and the same iteration closes the pty. On Linux a slave write reaches the master's read queue asynchronously (flip buffer → workqueue), so `echo done; exit` can lose its last line. The same happens when poll returned for another fd and the child wrote and exited before `reap()` ran.\n\nDone when the drained decision requires a subsequent quiet poll of the pty *after* the status was observed (e.g. the full 50 ms timeout genuinely expiring), and a regression test exercises output-then-exit repeatedly.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-18T15:14:21.211006Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.354502Z","closed_at":"2026-09-18T15:39:58.354402Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-znr","title":"Bound dial and the HELLO→WELCOME handshake with a deadline","description":"`dial()` (src/client.rs ~L124-170) reads the ssh pipe with a plain blocking `read` — no poll, no deadline, the signal fd is not watched — and `serve()` uses `timeout = -1` and only starts `Liveness` after the first WELCOME (~L551, L639). ssh's own keepalive is disabled by design (ServerAliveInterval=0) and ConnectTimeout=10 only covers TCP connect. A redial whose connect succeeds but then stalls (half-alive host, firewall that accepts and blackholes, a wedged remote before WELCOME) hangs the client until Ctrl-C. The 10 s dead-link guarantee of DESIGN §5.3 has a hole exactly in the reconnect path. A login file with `printf` and no trailing newline (see the marker bead) triggers the same hang deterministically.\n\nDone when the marker wait and the handshake are covered by a deadline (poll-based, with the signal fd), a timed-out redial returns to backoff, and a test with a silent remote proves it.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-18T15:14:21.272611Z","created_by":"michel","updated_at":"2026-09-18T15:48:09.569464Z","closed_at":"2026-09-18T15:48:09.569363Z","close_reason":"Fixed with regression tests (bugs-client branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-qrn","title":"Pop the title stack and status line when the session ends during an outage","description":"After a link drop, `offline()` (src/reconnect.rs ~L207-219) draws the bottom status row and pushes the xterm title (`CSI 22;0 t`), setting `status_shown`. The pop (`clear_status`) only runs from `on_welcome` and the offline Detach path. If the redial gets `Error{NO_SESSION}`, `Exit`, or `Takeover` before any WELCOME (src/client.rs ~L487-529), `serve` returns and `run()` calls only `leave()` (~L301), which never pops. The terminal keeps the \"acs: host — connection lost\" title and the inverse-video status row after acs exits — a DESIGN §5.4 violation. tests/reconnect.rs only covers the detach-while-down path.\n\nDone when `leave()` (or those return paths) calls `clear_status` whenever `status_shown`, with a test for session-end-during-outage.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-18T15:14:21.335251Z","created_by":"michel","updated_at":"2026-09-18T15:48:09.572705Z","closed_at":"2026-09-18T15:48:09.572611Z","close_reason":"Fixed with regression tests (bugs-client branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-wza","title":"Fix proxy busy-spin after client EOF while input is backpressured","description":"In `relay()` (src/proxy.rs ~L217-225) once stdin hits EOF `stdin_open = false` and its pollfd gets `events = 0`, but poll reports POLLHUP on a closed pipe regardless of `events`. Every iteration then returns immediately from poll, reads 0, and loops, for as long as `to_master` cannot drain — which is exactly when the master has stopped reading (PTY_IN_HIGH_WATER, child not consuming input). Trigger: paste a lot into a program not reading stdin, then the ssh connection drops → the proxy spins at 100 % CPU until the child reads or the session ends.\n\nDone when the stdin pollfd uses `fd = -1` once closed (poll ignores negative fds), with a backpressure test in tests/proxy.rs.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-18T15:14:21.396697Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.365776Z","closed_at":"2026-09-18T15:39:58.365690Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-gkl","title":"Fix master spinning on pty POLLHUP while the ring is full","description":"With an active client stalled and the ring full (`room_before_overwrite(next) == 0`) the master polls the pty with `events = 0` (src/master.rs ~L384-395), but Linux `n_tty_poll` reports POLLHUP once the slave side closes, regardless of `events` and even with unread data. Poll then returns immediately, `read_pty` bails at `want == 0` (~L793-796), the drained heuristic does not apply because `asked` is false, `pump_output` cannot add (client at high water), and the loop spins hot until the client drains a chunk or the connection dies. Trigger: `acs host -- cat big.log` over a stalled link when cat finishes.\n\nDone when the pty fd is omitted from the poll set (or the loop sleeps) while `room == 0`, with a test covering hangup-while-stalled.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-18T15:14:21.458270Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.357636Z","closed_at":"2026-09-18T15:39:58.357562Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-d1v","title":"Ignore frames from a taken-over or detached (Closing) connection","description":"`handle()` (src/master.rs ~L553-591) has no check for `ConnState::Closing`. A Closing conn is not polled for POLLIN, but POLLHUP is always reported, so when the old client's proxy closes, `read_conn` decodes whatever was still buffered and applies it as for the active client: INPUT goes to the pty and advances `InputDedupe::written`, so the new client's first keystrokes at the same sequence are dropped as duplicates (DESIGN §5.1 says clients taking turns never collide); RESIZE resizes the new client's pty; KILL kills the session. Trigger: the old client types or resizes between the takeover and its exit on TAKEOVER, or its proxy holds unsent input when the link died.\n\nDone when Closing conns are drained without handling (or skipped in read_conn), with a test that a late INPUT/RESIZE/KILL from a taken-over conn is ignored and the new client's input_seq stays consistent.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-18T15:14:21.518918Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.360336Z","closed_at":"2026-09-18T15:39:58.360259Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-ov1","title":"Guard against tcgetpgrp returning 0 so hard_kill cannot kill the master","description":"Linux `tiocgpgrp` on a pty *master* fd returns `pid_vnr(real_tty->ctrl.pgrp)`, which is 0 when the slave has no foreground group (after TIOCNOTTY, after a hangup, or during the leader's exit between `disassociate_ctty()` and reaping). `hard_kill` (src/master.rs ~L886-888) only filters `p != ch.pid`, so `kill(-0, SIGKILL)` == `kill(0, SIGKILL)` signals the master's own process group — the master is a session leader after setsid, so it dies and takes the user's session with it. `start_kill` (~L871) and `resize` (~L772) hit the same value harmlessly. Rare (child ignores SIGHUP and dropped its ctty, or exits at the 3 s deadline) but the consequence is total.\n\nDone when every tcgetpgrp result is filtered with `p > 0`, with a unit test on the filter.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-18T15:14:21.580009Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.363019Z","closed_at":"2026-09-18T15:39:58.362941Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-rxw","title":"Only prune version directories under $HOME/.local/share/acs","description":"`versions_root()` (src/prune.rs ~L31-38) is `parent(parent(current_exe))` whenever the parent dir is named after the version, with no check that it lies under `$HOME`. A binary exec'd from the prelude's second candidate, `/usr/local/lib/acs/<v>/acs` (src/ssh.rs ~L107), therefore touches `/usr/local/lib/acs/<v>`, writes `/usr/local/lib/acs/.pruned`, and `remove_dir_all`s other users' system-wide versions older than 30 days that are not in *this user's* live set. Silent for a non-root user (errors ignored) but effective for root or wherever `/usr/local/lib` is user-writable (Homebrew on Intel macOS). DESIGN §8 says acs never writes there.\n\nDone when pruning only runs with `root == $HOME/.local/share/acs`, with a test that a system-path binary prunes nothing.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-18T15:14:21.641811Z","created_by":"michel","updated_at":"2026-09-18T15:28:29.495718Z","closed_at":"2026-09-18T15:28:29.495641Z","close_reason":"Fixed with regression tests (bugs-install branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-ljl","title":"Take the create lock before --list removes a stale socket","description":"`list()` (src/proxy.rs ~L317-325) removes a socket after ECONNREFUSED/ENOENT without holding `create_lock`, while master start (src/master.rs ~L304-315) does check → unlink → bind under that lock. Interleaving: list connects (refused, old stale file) → master A unlinks the stale file and binds a fresh socket → list unlinks A's *live* socket. A is unreachable for up to `rebind_every()` (60 s); any proxy in that window spawns master B; when A's `check_socket` runs it sees another inode and leaves it — A is orphaned forever with its shell still running.\n\nDone when the refused → unlink step takes `dir.create_lock(name)` (try_lock; skip if busy), with a test for list racing a bind.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-18T15:14:21.703859Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.371134Z","closed_at":"2026-09-18T15:39:58.371049Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-er6","title":"Drain queued output to the client before failing on a master write error","description":"In `relay()` (src/proxy.rs ~L266) `flush(m, &mut to_master)?` propagates EPIPE when the master closed between this iteration's `read(m)` — which just queued the EXIT frame into `to_client` — and the write. `session()` then errors out without `drain(STDOUT, &to_client)`, so the client sees \"link lost\" and later NO_SESSION instead of the child's exit status. Needs queued client input (a PING or INPUT) in the same iteration, so the window is narrow.\n\nDone when a write error to the master falls through to the drain path instead of `?`, with a test for EOF-with-pending-output ordering.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-18T15:14:21.764668Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.368456Z","closed_at":"2026-09-18T15:39:58.368384Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-14k","title":"Emit a newline before the ACS-READY/ACS-NEED marker so printf noise cannot swallow it","description":"`parse_marker` (src/proto.rs ~L636) requires the marker to start a line. A `printf \"foo\"` (no `\\n`) in a startup file makes the line `fooACS-READY 1` / `fooACS-NEED ...`, which is rejected. For ACS-NEED the client reports \"the connection closed before acs started\"; for ACS-READY the client blocks in `dial` waiting for a marker while the proxy blocks waiting for HELLO — a mutual hang with no timeout on either side (see the dial-deadline bead).\n\nDone when `ready_line()` and the prelude's `printf` (src/ssh.rs) emit a leading `\\n`, with a scanner test for noise without a trailing newline.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-18T15:14:21.825110Z","created_by":"michel","updated_at":"2026-09-18T15:28:29.498491Z","closed_at":"2026-09-18T15:28:29.498409Z","close_reason":"Fixed with regression tests (bugs-install branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-wxa","title":"Honour ACS_ESCAPE_TIMEOUT_MS in the offline command-key detector","description":"`client_escape()` (src/reconnect.rs ~L194-203) copies only ACS_ESCAPE_KEY into the detector config used while the link is down; `escape_config()` (src/client.rs ~L74-89) reads both ACS_ESCAPE_KEY and ACS_ESCAPE_TIMEOUT_MS. A user with a custom escape window who double-taps Ctrl-] d during an outage gets the hard-coded 400 ms window instead, so the timing silently differs while disconnected.\n\nDone when both sites share one config constructor and a test asserts the offline detector uses the configured window.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-18T15:14:21.886595Z","created_by":"michel","updated_at":"2026-09-18T15:48:09.575477Z","closed_at":"2026-09-18T15:48:09.575408Z","close_reason":"Fixed with regression tests (bugs-client branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-7zi","title":"List sessions on every host in the configuration","description":"Show the sessions on all configured hosts in one command.\n\nUser's ask: \"add a way to display all the sessions on all hosts that are in the configuration\".\n\nToday `acs <host> --list` (src/list.rs) lists one host. The configuration's `hosts:` aliases (DESIGN §7.2, §7.3; src/config.rs, src/alias.rs) name every host acs knows.\n\nProposed shape (settle during implementation):\n\n- `acs --list` with no host (or `acs list`, which would become a reserved first argument like `config` and `upgrade`) lists every alias.\n- Each alias is resolved the way a connection would be (ping check, fallback hosts), then asked via the same `_proxy --list` side call. Hosts are queried in parallel with a timeout, so one slow or down host does not hold up the rest.\n- Output: the existing table with a HOST (alias) column, or one block per alias. An unreachable host, or one without acs of our version, gets one line saying so rather than failing the whole command.\n- Exit status: 0 if every host answered, non-zero (for example 255) if any did not.\n\nDone when tests cover several aliases, including an unreachable one and one without acs installed, using the fake transport and ping; README and DESIGN document the command.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T19:06:25.080986Z","created_by":"michel","updated_at":"2026-09-18T21:45:01.911219Z","closed_at":"2026-09-18T21:45:01.911128Z","close_reason":"Implemented with tests: acs --list without a host lists every alias (PR #21, feat: list sessions on every configured host)","source_repo":"beads-intake-list-bell","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-list-bell","compaction_level":0,"original_size":0}
{"id":"acs-uyk","title":"Ring the terminal bell when Ctrl-] Ctrl-] arms command mode","description":"Ring the terminal bell when Ctrl-] Ctrl-] arms command mode, so the user knows the next key (d, x) is a command. Configurable, on by default.\n\nUser's ask: \"if it is not breaking our concept of a transparent stream, trigger the bell in the terminal when the command mode sequence has been typed: ctrl-] ctrl-]. Make it possible to configure this behavior, defaulting to on\".\n\nDoes it break the transparent stream? No, if done right. The bell is written by the client to the local terminal (like the reconnect status line, DESIGN §5.4). Nothing is added to the program's input, and the pty output bytes are unchanged. The one hazard is interleaving: BEL (0x07) also terminates OSC sequences, so writing it while the remote output is in the middle of an unterminated OSC/DCS would end that sequence early. The client already parses output to track terminal modes (src/modes.rs; src/keys.rs lexes escapes too); the bell should only be written at a ground-state boundary, deferred otherwise.\n\nProposed design:\n\n- Ring when the detector (src/keys.rs `Detector`) arms: the second Ctrl-] within the window, online and in the offline wait (src/reconnect.rs).\n- `command_bell: true|false` in the configuration (DESIGN §7.2; src/config.rs BOOLS, so `acs config set command_bell false` works), plus `ACS_COMMAND_BELL=0` in the environment.\n- Never ring inside a bracketed paste (the detector already ignores the key there).\n\nDone when tests show the BEL on the client's terminal after Ctrl-] Ctrl-], none with the setting off, and none written in the middle of an OSC sequence the program is emitting; DESIGN §6 and the README document it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T19:06:27.248670Z","created_by":"michel","updated_at":"2026-09-18T21:45:02.033082Z","closed_at":"2026-09-18T21:45:02.032961Z","close_reason":"Implemented with tests: bell on command-mode arm, deferred past OSC/DCS, command_bell / ACS_COMMAND_BELL (PR #22)","source_repo":"beads-intake-list-bell","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-list-bell","compaction_level":0,"original_size":0}
{"id":"acs-x1z","title":"Remove dsh references from the README and help","description":"Remove the dsh references from the README; the general public does not know what dsh is.\n\nUser's ask: \"remove 'It replaces the dsh shell function (ssh + dtach).' in the README.md file. In general we don't have to talk too much about dsh. The general public doesn't know what it is\".\n\nFound in README.md today:\n\n- line 10: \"It replaces the `dsh` shell function (`ssh` + `dtach`). The design is in [docs/DESIGN.md](docs/DESIGN.md).\" Drop the dsh sentence and keep the pointer to the design.\n- line 113: \"`-l` is `--list` (as in `dsh`)\". Keep the fact (`-l` means `--list`, not ssh's login option) without naming dsh.\n\nAlso check the other public surfaces for dsh: `acs --help` (src/cli.rs USAGE) and the release notes (xtask/src/package.rs). docs/DESIGN.md keeps its dsh history: that is design rationale (§1, §10), not user documentation.\n\nDone when README, --help and the release notes no longer mention dsh, and markdownlint passes.\n","status":"closed","priority":3,"issue_type":"docs","created_at":"2026-09-18T19:28:42.639035Z","created_by":"michel","updated_at":"2026-09-18T21:45:02.132260Z","closed_at":"2026-09-18T21:45:02.132182Z","close_reason":"Done: README no longer mentions dsh; --help and release notes had none (PR #24, docs: drop the dsh references from the README)","source_repo":"beads-intake-list-bell","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-list-bell","compaction_level":0,"original_size":0}
{"id":"acs-2ff","title":"Package acs for Homebrew with a tap updated on release","description":"Package acs for distribution with Homebrew.\n\nUser's ask: \"package the application for distribution with Homebrew\".\n\nToday acs ships as GitHub release archives (acs-<version>-<target>.tar.gz for macOS arm64/x86_64 and Linux x86_64/aarch64, plus SHA256SUMS), the one-line installer, and `acs upgrade` (docs/VERSIONING.md, scripts/release-binaries.sh).\n\nProposed shape:\n\n- A tap repository, `michel-onstein/homebrew-acs`, with `Formula/acs.rb` that installs the prebuilt release archive for the machine (on_macos/on_linux, on_arm/on_intel), with its sha256 from SHA256SUMS. `brew install michel-onstein/acs/acs`. A prebuilt binary keeps the complete build with its Linux payloads (DESIGN §8.1); building from source would give a slim build that cannot install other platforms' remotes.\n- The formula is updated by the release: scripts/release-binaries.sh (or `cargo xtask`) rewrites version, URLs and sha256s and pushes to the tap after publishing.\n- `brew test`: `acs --version` reports the version.\n- A Homebrew-installed acs should not replace itself: `acs upgrade` detects it runs from a Homebrew Cellar path and says `brew upgrade acs`, and the weekly update-check message suggests the same.\n- README \"Install\" lists `brew install`.\n\nDone when `brew install michel-onstein/acs/acs` installs the current release on macOS (and Linuxbrew), `brew test acs` passes, a release updates the formula automatically, and `acs upgrade` defers to brew for a brewed install.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T19:28:42.722475Z","created_by":"michel","updated_at":"2026-09-18T21:45:02.313737Z","closed_at":"2026-09-18T21:45:02.313664Z","close_reason":"Implemented with tests: tap michel-onstein/homebrew-acs, formula rendered and pushed on release, acs upgrade defers to brew (PR #27)","source_repo":"beads-intake-list-bell","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-list-bell","compaction_level":0,"original_size":0}
{"id":"acs-6k0","title":"Resolve user@host through the host's configured entries, as that user","description":"Resolve `user@alias` through the alias too, using `user` as the login name.\n\nUser's ask: \"if a user supplies 'me@devbox' as a destination, also check if 'devbox' is an alias, if so follow the normal flow and set 'user' to 'me'\".\n\nToday (src/alias.rs `resolve`): any destination containing `@` skips alias resolution on purpose, and DESIGN §7.3 and README (\"`me@devbox`, or any name that is not an alias, is used as given\") document `user@<alias>` as the way to reach a host whose name is also an alias. This change reverses that.\n\nProposed behaviour:\n\n- Split the destination at the last `@`: if the host part is an alias, resolve it normally (entries in order, ping check, fallback) and connect as `<user>@<entry host>`. The given user overrides every entry's `user`.\n- A redial resolves it again with the same user override.\n- Names that are not aliases behave as before.\n- The escape hatch goes away, so there needs to be another way to reach a host whose name equals an alias: for example its FQDN, or a documented form. Pick one and document it.\n- `-v` shows that the login name came from the command line.\n\nDone when tests cover `me@alias` (user override on the first and the fallback entry), `me@not-an-alias` unchanged, and the redial; DESIGN §7.3 and README are updated.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T19:28:44.658438Z","created_by":"michel","updated_at":"2026-09-18T21:45:02.224182Z","closed_at":"2026-09-18T21:45:02.224108Z","close_reason":"Implemented with tests: user@alias resolves through the alias as that user, redial keeps it (PR #25)","source_repo":"beads-intake-list-bell","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-list-bell","compaction_level":0,"original_size":0}
{"id":"acs-mbd","title":"Configure an ssh identity file per alias and per alias host","description":"Wanted, in the user's words: \"add the option to specify the SSH identity file in the configuration, either at alias level and/or at alias>host level. The identity configuration supplied through the commandline has precedent, then the one at alias>host level, followed by the alias level.\"\n\nToday the identity only comes from `-i <identity_file>` on the command line (src/cli.rs, forwarded by src/ssh.rs; repeatable, ssh tries them in order) or from ~/.ssh/config. Config `hosts` entries (src/config.rs `HostEntry`, `HOST_KEYS`) accept only `host`, `user` and `reachability_check`, and an alias is just a list of entries (or one entry), so there is nowhere to put an alias-wide setting yet.\n\nPrecedence, highest first: `-i` on the command line; `identity_file` on the chosen host entry; `identity_file` on the alias. Only the winning level is passed to ssh as `-i`. It applies to every ssh call that uses the resolved destination (session, --list, install, redial).\n\nOpen design points: the YAML shape for an alias-level setting, keeping today's list form valid (for example `devbox: {identity_file: …, hosts: [...]}`); whether one level may hold several files, like repeated `-i`; `~` expansion; how global and local files merge an alias-level value (local replaces, as with other settings); and `acs config host add --identity-file` plus `config show` / `host list` output.\n\nDone when the config validates and `acs config` edits the new key at both levels, the precedence is covered by tests, and DESIGN §7.2–7.4 and the README describe it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T19:36:00.001340Z","created_by":"michel","updated_at":"2026-09-18T21:45:02.405013Z","closed_at":"2026-09-18T21:45:02.404934Z","close_reason":"Implemented with tests: identity_file per host entry and per alias, -i > host > alias, acs config edits both (PR #30, which replaced #28)","source_repo":"beads-intake-ssh-identity","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-ssh-identity","compaction_level":0,"original_size":0}
{"id":"acs-ome","title":"Send Ctrl-L on reconnect to refresh the screen, configurable per alias","description":"Wanted, in the user's words: \"when reconnecting to a session, send a Ctrl-L to refresh the screen first thing, unless disabled in the configuration, globally or per alias.\"\n\nToday (DESIGN §5.2, §5.4): a fresh attach or a resume gap clears the local screen and the master SIGWINCHes the foreground group (src/master.rs `resize(.., force_redraw)`); a lossless resume redraws nothing unless a status line was shown while offline, and then the client sends two RESIZE frames (src/reconnect.rs `on_welcome`). Nothing sends Ctrl-L. Ctrl-L (0x0c) is input to the program, not a terminal operation: shells and most TUIs repaint on it, but a program reading raw input receives a form feed. That is why the off switch matters.\n\nConfig: a global boolean (for example `redraw_on_reconnect`, default true, next to `command_bell` in `KEYS`/`BOOLS` in src/config.rs) and the same key on an alias (`ALIAS_KEYS`, the alias-mapping form that `identity_file` introduced). The alias value wins over the global one. Probably also an `ACS_*` env override, as `ACS_COMMAND_BELL` has.\n\nOpen points: whether \"reconnecting\" means the automatic resume after a drop, a re-attach by a new client (`acs host session`), or both; how it sits with the existing SIGWINCH / double-RESIZE redraw (replace it or add to it); and sending it right after WELCOME as INPUT, so it goes through the resend/ACK path and is never typed twice.\n\nDone when the client sends Ctrl-L after reconnecting unless disabled, both config levels validate and `acs config` shows and edits them, tests cover the precedence and the on/off cases, and DESIGN §5.2/§5.4/§7.2 and the README describe it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-19T02:38:58.404114Z","created_by":"michel","updated_at":"2026-09-19T03:17:33.896913Z","closed_at":"2026-09-19T03:17:33.896816Z","close_reason":"Implemented with tests: Ctrl-L on any attach to an existing session, after resent input, never into an open paste; redraw_on_reconnect global and per alias, ACS_REDRAW_ON_RECONNECT (PR #32)","source_repo":"beads-intake-redraw-picker","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-redraw-picker","compaction_level":0,"original_size":0}
{"id":"acs-s0g","title":"Pick a detached session from a menu on a plain acs <host>","description":"Wanted, in the user's words: change the behaviour of a plain `acs [<user>@]<alias | hostname>`. Instead of always starting a new session, check for detached sessions on the host. If there are none, create a new one. If there are, present a list of the detached sessions, like --list, and allow selecting one with a single press of its number, or by moving a cursor with up/down (and the vim equivalents j/k). Enter connects to the selection. '.' expands the list to include attached sessions too, which can then be connected to with extra confirmation. 'x' on a session exits that session on the target, and the menu is updated. At the bottom of the list, an 'Exit' option leaves the menu and returns to the command line; Esc does the same at any point.\n\nVerified: today a plain `acs <host>` attaches to `main` (or `$ACS_DEFAULT_SESSION`), creating it if absent (DESIGN §4.4). §4.4 says outright that \"the default never depends on what else happens to exist\", and Decision 3 (§12) rests on it, so this reverses a recorded decision: update §4.4, §12 and the README's Use section with it. `acs <host> <name>`, `--new` and `--list` stay as they are. The session list already exists: `_proxy --list` sends each master STATUS (src/list.rs `query`, `render`), which gives name, attached/detached, who, idle time and command. Killing a session from outside it does not exist: today only Ctrl-] Ctrl-] `x` inside the session sends KILL. 'x' in the menu needs a new side call, for example `_proxy --kill <name>`, and probably a confirmation.\n\nOpen points: what \"create a new one\" names (`main` if absent, else the lowest free number as `--new` does); behaviour when stdin/stdout is not a tty (keep the old attach-or-create of `main`); the takeover confirmation for an attached session (reuse the BUSY/`--force` path, §4.5); an alias with several hosts resolves to one machine first (§7.3); Esc must be told apart from arrow-key escape sequences; the menu runs in raw mode and restores the terminal on every exit path; with more than nine sessions a single digit cannot address all of them.\n\nDone when a plain `acs <host>` creates with no detached sessions and otherwise shows the menu with number/arrow/j/k selection, Enter, '.', 'x', Exit and Esc working; tests cover the menu's key handling and the create/menu decision; and the docs describe the new default. Filed as one feature, not decomposed; the remote kill side call could be split out as its own bead.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-19T02:38:58.478628Z","created_by":"michel","updated_at":"2026-09-19T03:17:34.005301Z","closed_at":"2026-09-19T03:17:34.005219Z","close_reason":"Implemented with tests: plain acs <host> creates when nothing is detached, else a menu (digits, arrows/j/k, Enter, '.', 'x' via _proxy --kill, n, Esc); no tty keeps attach-or-create (PR #33)","source_repo":"beads-intake-redraw-picker","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-redraw-picker","compaction_level":0,"original_size":0}
{"id":"acs-o31","title":"Ping an alias's hosts in parallel, with a configurable 0.5 s deadline","description":"Wanted: when checking which of an alias's hosts is reachable, ping them in parallel instead of one after another, and make the ping deadline configurable, defaulting to 0.5 seconds.\n\nVerified (v0.5.0): `pick` in src/alias.rs loops over the alias's entries and pings each checked host in turn with `ping -c 1` and a fixed 2 s deadline (`ping`, src/alias.rs), stopping at the first that answers. Every host that does not answer adds up to 2 s before the next is tried: home/office/public entries where only the last answers take about 4 s to choose. It happens again on every redial (DESIGN §7.3), and nothing reachable costs 2 s × N before the error. `acs --list` with no host already runs aliases in parallel threads (src/list.rs), but each alias's hosts are still sequential.\n\nParallel, keeping the configured order as the priority: start every checked host's ping at once; take host k as soon as it has answered and every checked host before it has failed. A host with `reachability_check: false` still wins without a ping as soon as the hosts before it have failed. The result is exactly what the sequential loop would choose, in at most one deadline. Log lines (`-v`) and the \"tried …\" error keep the configured order.\n\nDeadline: a new setting, for example `reachability_timeout` (duration or milliseconds; default 0.5 s), global and probably per alias like `identity_file`, possibly with an `ACS_*` env override. Portability: macOS `ping -t` and BusyBox `-W` take whole seconds only (macOS `-W` is per-packet milliseconds; iputils `-W` takes fractions only in newer releases). So acs should enforce the deadline itself: start `ping -c 1` with a generous own timeout, and kill and reap the child when acs's deadline passes, counting that as no answer. `ACS_PING` (the test hook) keeps working.\n\nDone when the hosts are pinged concurrently with order-preserving selection, the deadline is configurable (validated, shown and edited by `acs config`) with a 0.5 s default, tests cover the selection order (a later host answering first must not beat an earlier host that answers within the deadline) and the timeout path, and DESIGN §7.2/§7.3 and the README describe it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-19T14:21:44.848126Z","created_by":"michel","updated_at":"2026-09-19T15:22:52.377615Z","closed_at":"2026-09-19T15:22:52.377487Z","close_reason":"Implemented: parallel pings with order-preserving selection and the reachability_timeout setting (default 500ms, global and per alias)","source_repo":"beads-intake-parallel-ping","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-parallel-ping","compaction_level":0,"original_size":0}
{"id":"acs-km8","title":"Draw the session menu's selection bar at one width for every row","description":"Wanted, in the user's words: \"in the session selection menu, make the bar that shows your current selection the same width across all the entries, covering the largest width needed among the entries.\"\n\nVerified (v0.5.0, src/menu.rs `Menu::render`): each selectable row is `\"{mark} {key}  {text}\"` and the cursor's row is written as `\\x1b[7m{text}\\x1b[0m`, so the reversed bar is exactly as long as that row's text. It is short on \"new session\" and \"exit\", and differs between sessions whose last `--list` column (the command) differs in length. It jumps in width as the cursor moves.\n\nFix: pad the cursor's row with spaces to the widest selectable row (sessions, new session, exit) currently shown, clipped at the terminal width (`cols`) as lines already are, so the bar never wraps. Measure in terminal columns rather than chars if a command can hold wide characters; `render` truncates with `chars().take(cols)` today.\n\nDone when the reversed bar has the same width on every row, equal to the widest entry (or the terminal width if that is narrower), with a unit test next to the existing render tests in src/menu.rs asserting the padded width on a short row (exit) and a long session row.\n","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-19T14:26:21.478654Z","created_by":"michel","updated_at":"2026-09-19T14:26:21.478654Z","source_repo":"beads-intake-menu-bar","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-menu-bar","compaction_level":0,"original_size":0}
{"id":"acs-68z","title":"Run the session menu and the attach over one ssh connection","description":"Wanted: a plain `acs <host>` makes two ssh connections, one to list sessions for the menu and one for the session, each paying a full TCP + key exchange + auth handshake. Reduce it to a single call to connect faster. The user's `acs -v corello` on v0.6.0 shows `ssh … _proxy --list` followed by `ssh -o ControlMaster=no -o ControlPath=none … _proxy --session main --mode attach-or-create`.\n\nVerified (v0.6.0): src/pick.rs `choose` lists with `list::query(.., Call::Side, ..)`, then the attach dials its own connection. The session call opts out of multiplexing on purpose (DESIGN §3: a reconnect must never wait on a dead multiplexer), so even `ControlMaster auto` + `ControlPersist` cannot share it. Ending a session with 'x' in the menu is a third kind of call, `_proxy --kill <name>` (pick.rs `end`, src/proxy.rs). The second handshake costs a few hundred ms, more through a jump host, and a hardware key needs two touches.\n\nThis revisits a recorded decision: DESIGN §4.4 says \"reusing one connection for both is not worth a second proxy mode.\" The implementing PR updates §4.4 (and §3/§4.3/§5.1 as needed) and says why the decision changed: connect latency and the double auth.\n\nSketch: a picker mode on the session connection, e.g. `_proxy --pick` with the usual transport options. The proxy first sends the session list as frames (the STATUS data `--list` gathers). The client decides on that list exactly as today (none detached: create; else the menu in menu.rs, unchanged). Menu actions travel on the same connection: '.' (refresh with attached sessions), 'x' (end a session, then a fresh list), new. The pick ends with the ordinary HELLO naming the session and mode (and `force` after a confirmed takeover); the proxy then connects to or starts that master and relays as now. Leaving the menu (Esc/exit) closes the connection cleanly.\n\nUnchanged: redials (they know the session and use the plain session call), `acs <host> <name>`, `--new`, `--list`, and the non-tty fallback, which are already a single call. Keep the install path working: a host without acs answers ACS-NEED on the pick call as on any other, so install, then continue. The ACS-READY marker and the dial deadlines (§5.3) apply to the pick phase. Mind the menu's idle time: the connection sits open while the user reads it, with no liveness pings before WELCOME.\n\nDone when `acs -v <host>` with detached sessions shows one ssh invocation from list to attach, 'x' and '.' work without extra ssh calls, the proto/proxy changes have unit tests and an end-to-end test (scripts/e2e_ssh.sh or the in-process tests) covers pick → attach on one connection, and DESIGN/README describe it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-19T15:37:36.012548Z","created_by":"michel","updated_at":"2026-09-19T15:53:20.497994Z","closed_at":"2026-09-19T15:53:20.497904Z","close_reason":"Implemented in PR #40 (1dc2233): _proxy --pick runs the session menu, x and the attach over one ssh connection","source_repo":"beads-intake-single-dial","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-single-dial","compaction_level":0,"original_size":0}
