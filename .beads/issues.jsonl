{"id":"acs-08m","title":"Check the remote binary is owned by the user and not other-writable before exec","description":"Security, hardening a predictable exec path. ssh.rs prelude execs $HOME/.local/share/acs/<version>/acs whenever it is executable, and install.rs creates that directory with a bare mkdir -p under the remote umask. Nothing verifies the directory or the binary is owned by the user and not group- or other-writable.\n\nOn a host where $HOME or ~/.local/share is group-writable (umask 002 with a shared group, a common lab or edge-device setup) another local user plants a binary at that exact path and every later `acs <host>` execs it as the victim. It is the generic writable-home problem, but acs adds a predictable, always-exec'd path and never checks it.\n\nDone when the prelude refuses a candidate whose directory or file is not owned by the user or is writable by group or other, falling back to the install path with a clear message, and a test covers a loosened directory.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-20T04:36:01.671522Z","created_by":"michel","updated_at":"2026-09-20T06:58:14.276117Z","closed_at":"2026-09-20T06:58:14.276015Z","close_reason":"Implemented in #132: the prelude checks ownership and writability before exec","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-0n8","title":"A network hint is committed then dropped where the caller cannot act on it","description":"`NetWatch::changed()` commits the new network set (`*held = now`) and returns\ntrue. Two callers then drop that answer:\n\n- `serve()` discards it when `!welcomed` — a change during the handshake, which\n  acs-xo1 covers as a feature gap;\n- `offline()` discards it when `EARLY_EVERY` says the last early redial was too\n  recent.\n\nEither way the hint is **consumed**. The next identical hint answers \"not a\nchange\", because the set it would compare against has already moved. So the\nchange is not deferred, it is lost, and the only thing that recovers it is a\ncovering deadline — the dial timeout in the first case, the backoff in the\nsecond.\n\nBoth sites are deliberate and their comments say so, and both are covered\ntoday. This is filed because it is the **only** way a hint can be lost, and\nthat makes it the standing suspect whenever something that should have reacted\nto a network change did not. acs-ryz hit exactly that shape: its one\nunreproducible failure was `a_network_change_ends_a_frozen_link_without_the_dead_timeout`,\nseen once and then not again in 40 isolated plus 24 full-binary runs. That pass\ndeliberately did **not** add a repeat-the-hint loop to the test, on the correct\nreasoning that a repeat would answer \"not a change\" and so would only have\nlooked like a fix — which is itself the evidence that this shape is reachable.\n\nThe fix is probably to commit the set only where the answer is acted on, or to\ncarry an explicit \"pending change\" the next poll can re-offer. Both are small;\nthe care needed is in not re-introducing the storm acs-6p8 fixed, where every\nroute message triggered a redial.\n\nNote the interaction: `EARLY_EVERY` exists to stop a flapping interface\nredialling repeatedly, and deferring rather than dropping must not undo that —\na change deferred past the flap should collapse into one redial, not queue\nseveral.\n\nDone when a hint the caller could not act on is either not committed or is\nre-offered on the next poll, a test drives the drop path directly (both the\nhandshake and the EARLY_EVERY case), a flapping interface still produces one\nredial rather than several, and the comments at both sites say what now\nhappens.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-24T07:44:34.667970Z","created_by":"michel","updated_at":"2026-09-24T17:33:46.808280Z","closed_at":"2026-09-24T17:33:46.807742Z","close_reason":"Fixed in PR #199 (491c637): changed() returns Option<Change<'_>> and Change::acted() commits the set, so a hint the caller could not use is offered again. #[must_use] puts the danger at the site that acts. A flap is still one redial, by construction","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-0pr","title":"Check e2e_ssh.sh's inputs beyond the source stamp: a missing client binary and a partial dist","description":"acs-gb4 (PR #170, 2d0a719) made `scripts/e2e_ssh.sh --no-build` refuse a\n`dist/` that was not built from the current tree, by comparing a content hash\nwritten to `dist/source.stamp`. Three smaller input checks around it were left\nalone; none is urgent and all three are the same shape — a bad input that\nsurfaces later as a confusing test failure rather than immediately as a clear\nmessage.\n\n**1. The client binary is never checked to exist.** The script sets\n`ACS_E2E_CLIENT=\"$root/$client\"` and lets `cargo test` fail later with whatever\nthe harness says about a missing file. The stamp catches the common case — no\n`dist/` at all means no stamp, which is a mismatch and a refusal — but a\n`dist/` built with `--targets` that omits the host's target passes the stamp\ncheck and then fails obscurely.\n\n**2. `cargo xtask dist --targets …` writes a full-tree stamp for a partial\ndist.** The stamp says \"built from this tree\", which is true, and says nothing\nabout *which* targets were built — which is how case 1 gets through. If partial\ndists ever become routine, the stamp should record the target list too. Doing 1\nproperly may well be enough on its own; decide whether 2 is worth it or whether\nit is speculative.\n\n**3. `--allow-stale-dist` without `--no-build` is silently ignored**, because\nthe build path wins. Harmless and documented in the usage line, but a usage\nerror would be more honest than a flag that does nothing.\n\nDone when a missing or wrong-target client binary is refused with a message\nnaming the file and how to get it, the `--allow-stale-dist`-without-`--no-build`\ncase is decided (error or documented no-op), and whichever of these gets a\ncheck also gets a test in `tests/scripts.rs` beside the acs-gb4 one.","notes":"Friction worth folding in, from acs-br2 (PR #208): **`scripts/e2e_ssh.sh\n--no-build` cannot be used from a fresh worktree.** `dist/` does not exist\nthere, so the stamp check refuses and the first e2e run in any new worktree\npays a full `cargo xtask dist` (~45s of cross-building four targets).\n\nThat is correct behaviour — acs-gb4 built the refusal deliberately, and a\nmissing `dist/` really is a mismatch — but it is the common case for this\nrepo's workflow, where nearly all work happens in a fresh worktree. Every\nagent-driven pass this run hit it.\n\nIt fits this bead because it is the same question: what should `--no-build` do\nabout an input it cannot use? The answer for a *stale* dist is \"refuse\". The\nanswer for an *absent* one might reasonably be \"build it anyway and say so\",\nwhich is what the person meant, rather than an error they must then work\naround.\n\nWorth considering alongside this bead's other items whether the volume/target\ndirectory could be shared across worktrees of the same repo, which would remove\nthe cost rather than the message.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-23T22:13:33.419016Z","created_by":"michel","updated_at":"2026-09-25T20:56:50.264278Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-14k","title":"Emit a newline before the ACS-READY/ACS-NEED marker so printf noise cannot swallow it","description":"`parse_marker` (src/proto.rs ~L636) requires the marker to start a line. A `printf \"foo\"` (no `\\n`) in a startup file makes the line `fooACS-READY 1` / `fooACS-NEED ...`, which is rejected. For ACS-NEED the client reports \"the connection closed before acs started\"; for ACS-READY the client blocks in `dial` waiting for a marker while the proxy blocks waiting for HELLO — a mutual hang with no timeout on either side (see the dial-deadline bead).\n\nDone when `ready_line()` and the prelude's `printf` (src/ssh.rs) emit a leading `\\n`, with a scanner test for noise without a trailing newline.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-18T15:14:21.825110Z","created_by":"michel","updated_at":"2026-09-18T15:28:29.498491Z","closed_at":"2026-09-18T15:28:29.498409Z","close_reason":"Fixed with regression tests (bugs-install branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-1gy","title":"The system-wide install path now works, but nothing dials into it and nothing prunes it","description":"acs-6w9 (PR #190, ae86191) made `/usr/local/lib/acs/<version>/acs` genuinely\nusable: an administrator populates it as root and every user's prelude will now\nexecute it. That closed the bug. It also turned a dead code path into a live\none, and two things that did not matter while it was dead now do.\n\n**1. No end-to-end coverage.** `scripts/e2e_ssh.sh` only ever exercises the\n`$HOME` candidate. The system candidate is covered by prelude unit tests and by\n`tests/multiuser.rs` running the prelude directly as root — good coverage of the\n*decision*, none of the *path*. Nothing dials a real ssh connection into a host\nwhere **only** `/usr/local/lib/acs/<v>/acs` exists and confirms a session comes\nup on it without a first-contact install. That is the scenario an administrator\nwho sets this up is actually buying, and it is the one thing untested.\n\nThe e2e container is the natural place: a variant host with the binary installed\nsystem-wide and `$HOME` empty. Worth checking what `acs _install` does if it\nfinds the system copy already good — it should not install a second one.\n\n**2. Nobody prunes it.** `src/prune.rs` correctly never touches\n`/usr/local/lib/acs` — it is not ours to delete, and a user cannot remove root's\nfiles anyway. But now that versions accumulate there for real, there is no\ndocumented story for who reclaims a stale one. An administrator who populates it\nonce per release, as DESIGN §8 suggests, gets a directory that grows forever with\nnothing saying so.\n\nThis does not need code: it may be a paragraph in DESIGN §8 telling the\nadministrator that pruning the system path is theirs to do, and perhaps a line\nin `acs prune`'s output noting a system version it can see but will not remove.\nDecide which, rather than leaving it unsaid.\n\nDone when a real ssh dial into a host carrying only the system-wide install\nattaches without installing anything, and DESIGN §8 says who is responsible for\nremoving old versions from the system path — with `acs prune` mentioning what it\ndeliberately leaves alone if that is the chosen answer.","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-24T05:35:45.708973Z","created_by":"michel","updated_at":"2026-09-24T05:35:45.708973Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-28b","title":"Chmod the payload-path binary to 0755 explicitly instead of relying on the umask","description":"install::write_exe (src/install.rs:282-294) relies on OpenOptions::mode(0o755), which the remote umask masks (umask 077 gives 0700), whereas the self-copy path (install.rs:126) does an explicit chmod 755. Only matters if the per-user directory is ever shared, but the two paths disagree and tests/install.rs asserts 0755 only on the self-copy path.\n\nDone when write_exe sets the mode after writing and the install test asserts 0755 on the payload path under a restrictive umask.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-19T21:46:59.322387Z","created_by":"michel","updated_at":"2026-09-19T23:15:31.328068Z","closed_at":"2026-09-19T23:15:31.327978Z","close_reason":"Implemented in #95 (eeb5969)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-2dc","title":"Timing windows in the files acs-ryz did not reach, and a diagnostic that cannot explain itself","description":"acs-ryz (PRs #194, 50fc179 and #195, 3ce7f21) cleared the reconnect, alias and\nmaster suites. Three windows of the same class sit in files it did not reach,\nplus one diagnostic gap that made its single unexplained failure unexplainable.\n\n**The windows:**\n\n- `host_aliases::a_host_slower_than_the_deadline_is_passed_over` — two: the\n  fallback host's ping must answer inside the alias's own 5 s, and\n  `took < 25 s` is an upper bound on elapsed time. 8 failures of 63 at extreme\n  load.\n- `client::resize_reaches_the_program` — a 200 ms settle between `resize()` and\n  the `\\r`. Exactly acs-7wu's shape. It also went red once during acs-ryz\n  (`timed out waiting \"ready\"`) and passed alone.\n- `alias::tests::ping_is_killed_at_the_deadline` — `start.elapsed() < 800ms`,\n  an upper bound a loaded host can break.\n\nUpper bounds are the harder half: a lower bound is safe under contention\n(starving the machine only makes the event later), an upper bound is exactly\nwhat contention breaks. Where the assertion really is about elapsed time,\nacs-cu8's move applies — keep it, widen the slack until it is out of reach of\nrealistic load. Where it is standing in for \"this did not fall back to the slow\npath\", it should say that instead.\n\n**The diagnostic gap:** `assert_consecutive` / `numbers()` in\n`tests/reconnect.rs` cannot distinguish a **torn** number from **lost** output.\nacs-ryz's one unexplained burst reported `lost or repeated output around\n174 → 17`, where the `17` looks like half of a `175` split mid-write rather\nthan a gap in the stream. As written, the helper turns a display bug into what\nlooks like a protocol bug, and the next person will chase the wrong thing.\nPrinting the surrounding bytes on failure would settle it in one run.\nacs-z22 — a `-v` note on fd 2 splitting a frame on fd 1 — is the obvious\nsuspect, and that connection can only be made if the bytes are visible.\n\nDone when none of the three windows can be lost to realistic load, each still\ngoes red for what it covers (show it), and `assert_consecutive` prints enough\nof the surrounding stream on failure to tell a torn write from a lost one.","notes":"**acs-z22 (PR #201, da1368c) does NOT close the torn-vs-lost half of this\nbead**, and that pass was explicit about it so nobody reads #201 as having\nfixed it.\n\nA boundary there means \"not inside an escape sequence, an OSC/DCS string, or a\nmulti-byte UTF-8 character\". `#175#` is plain ASCII, so **every** byte position\nin it is a boundary — a note raised while the stream sits between `#17` and\n`5#` still goes out there, and `numbers()` still sees `17` where `175` was.\nacs-z22 removed escape-sequence and UTF-8 tearing, not line tearing. Holding\nnotes to a newline instead would mean holding them indefinitely in a\nfull-screen program, which is worse than the disease.\n\nSo the fix stays on the test side, and that pass suggested the shape: teach\n`numbers()` to **stitch across an `acs: …` line** the way it already stitches\nacross the status line's `ESC 7 … ESC 8` span. That turns the `174 → 17`\nmystery into a parse detail rather than a reported protocol failure, which is\nwhat this bead's diagnostic half is asking for.\n\nTwo more for the timing half of this bead, both in `tests/client.rs`:\n\n- `single_escape_press_and_other_keys_reach_the_program` — two fixed sleeps\n  gating an `od -N 4` read. Timed out twice under full-parallel macOS load\n  during acs-z22 (`timed out waiting for \"armed\"` — the program's *first* byte\n  never arrived), 12/12 green alone. Not acs-br2, and not acs-z22's doing: that\n  test runs no `-v`, so the changed path is never taken.\n- The empty-output shape already noted here has now been seen in a fourth test\n  in that file, which strengthens the case that `tests/client.rs` has a startup\n  problem rather than a slowness problem.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-24T07:44:49.697012Z","created_by":"michel","updated_at":"2026-09-25T19:21:40.929161Z","closed_at":"2026-09-25T19:21:40.928924Z","close_reason":"Fixed in PR #205 (661e1dc): three windows removed or turned into lower bounds, and numbers()/assert_consecutive moved to tests/common and taught to stitch across a note plus drop a trailing torn fragment. CORRECTION: the empty-output shape is slowness, not a startup stall — 86 failures at T=30s became 6 at T=300s, and 850/850 green with the I/O pressure removed; five survivors were clients SIGKILLed externally. No product bug","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-2ff","title":"Package acs for Homebrew with a tap updated on release","description":"Package acs for distribution with Homebrew.\n\nUser's ask: \"package the application for distribution with Homebrew\".\n\nToday acs ships as GitHub release archives (acs-<version>-<target>.tar.gz for macOS arm64/x86_64 and Linux x86_64/aarch64, plus SHA256SUMS), the one-line installer, and `acs upgrade` (docs/VERSIONING.md, scripts/release-binaries.sh).\n\nProposed shape:\n\n- A tap repository, `michel-onstein/homebrew-acs`, with `Formula/acs.rb` that installs the prebuilt release archive for the machine (on_macos/on_linux, on_arm/on_intel), with its sha256 from SHA256SUMS. `brew install michel-onstein/acs/acs`. A prebuilt binary keeps the complete build with its Linux payloads (DESIGN §8.1); building from source would give a slim build that cannot install other platforms' remotes.\n- The formula is updated by the release: scripts/release-binaries.sh (or `cargo xtask`) rewrites version, URLs and sha256s and pushes to the tap after publishing.\n- `brew test`: `acs --version` reports the version.\n- A Homebrew-installed acs should not replace itself: `acs upgrade` detects it runs from a Homebrew Cellar path and says `brew upgrade acs`, and the weekly update-check message suggests the same.\n- README \"Install\" lists `brew install`.\n\nDone when `brew install michel-onstein/acs/acs` installs the current release on macOS (and Linuxbrew), `brew test acs` passes, a release updates the formula automatically, and `acs upgrade` defers to brew for a brewed install.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T19:28:42.722475Z","created_by":"michel","updated_at":"2026-09-18T21:45:02.313737Z","closed_at":"2026-09-18T21:45:02.313664Z","close_reason":"Implemented with tests: tap michel-onstein/homebrew-acs, formula rendered and pushed on release, acs upgrade defers to brew (PR #27)","source_repo":"beads-intake-list-bell","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-list-bell","compaction_level":0,"original_size":0}
{"id":"acs-2zj","title":"Warn when the latest release reported is older than the last one seen","description":"Security, update freeze. upgrade::decide correctly refuses to install an older release unless --version pins it, so a straight rollback is blocked. But an attacker controlling the channel, or a stale mirror, can serve an old but genuine SHA256SUMS indefinitely: decide reports nothing to do, the weekly check stays silent, and the user sits on a vulnerable version with no signal. There is no freshness or expiry metadata.\n\nupdate_check's State already persists the last latest it saw, so the comparison is nearly free.\n\nDone when a reported latest older than the stored one produces a visible warning rather than silence, the stored value is not overwritten by the older one, and a test covers the regression.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-20T04:36:01.360836Z","created_by":"michel","updated_at":"2026-09-20T06:58:14.724498Z","closed_at":"2026-09-20T06:58:14.724382Z","close_reason":"Implemented in #134: a channel offering an older release is reported once","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-3ej","title":"Add a curl | sh installer for acs (per-platform install dir)","description":"Add a one-line installer: `curl -fsSL <url> | sh` installs acs the way the platform expects.\n\nUser's ask: \"create an install script that someone can download which installs acs automatically according to the rules of the platform it is being installed on. Similar to other apps: `curl <something> | <some shell command>`.\"\n\nProposed design:\n\n- `scripts/install.sh`, POSIX sh. It is also published as a release asset by `scripts/release-binaries.sh`, so the stable URL is `https://github.com/michel-onstein/acs/releases/latest/download/install.sh`. The repo is public.\n- It maps `uname -s` / `uname -m` to one of the four release targets: macOS arm64/x86_64, Linux x86_64/aarch64. Anything else is a clear error.\n- It downloads `acs-<version>-<target>.tar.gz` and `SHA256SUMS` from the release (latest by default; `ACS_VERSION=0.2.0` pins one) and refuses to install on a checksum mismatch. It uses `curl` or `wget`, and `shasum -a 256` or `sha256sum`.\n- Install location follows each platform's rules: per-user `~/.local/bin` (XDG) by default, or `/usr/local/bin` when run as root or with `ACS_INSTALL_DIR`. It warns, with the line to add, when the directory is not on `PATH`.\n- Re-running it upgrades in place, and it prints the installed `acs --version`.\n\nToday install is manual: README \"Install\" has a curl + tar + install recipe, and the release assets exist since v0.1.0.\n\nDone when: the script passes shellcheck; tests run it against a real release in Alpine and Ubuntu containers and in a temp HOME on macOS (default dir, pinned version, checksum failure, unsupported platform); README leads with the one-liner.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T14:34:30.356987Z","created_by":"michel","updated_at":"2026-09-18T15:10:22.059364Z","closed_at":"2026-09-18T15:10:22.059260Z","close_reason":"Implemented: scripts/install.sh (curl|sh, wget fallback, SHA256SUMS check, versioned layout ~/.local or /usr/local, ACS_VERSION/ACS_INSTALL_DIR), published as a release asset; scripts/test_install.sh passes on macOS, Alpine and Ubuntu as root and user","source_repo":"beads-intake-install","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-install","compaction_level":0,"original_size":0,"labels":["acs","install"]}
{"id":"acs-4i2","title":"The real PF_ROUTE and netlink watcher has no end-to-end test, and now carries more weight","description":"acs-6p8 (PR #180, f196120) made a kernel hint insufficient on its own: a redial\nhappens only when `netmatch::local_networks()` shows the machine's own networks\nactually moved. That is the right contract, and it puts more weight on a path\nnothing tests end to end.\n\nWhat is covered today:\n\n- `platform_watcher_opens` — the socket opens.\n- `route_messages_matter` — the byte filter accepts the right message types.\n- the new unit and integration tests — the *decision*, driven through\n  `ACS_NETWATCH_FIFO` and `ACS_NETWATCH_NETS`.\n\nWhat is not covered anywhere: that a **real** roam, wake or VPN transition on\nmacOS `PF_ROUTE` or Linux netlink actually emits a message the filter accepts\n**and** moves an address that `local_networks()` reports. Both halves have to\nbe true for the feature to work, and both are assumptions about the platform\nrather than about acs. If a macOS release changed which messages accompany a\nWi-Fi roam, or if a VPN came up without moving any address `LocalNet::usable`\naccepts, the feature would go quietly dead: no error, no log, just a session\nthat waits out the backoff instead of redialling at once.\n\nThe acs-6p8 pass wrote this into `docs/VERIFICATION.md` as a by-hand item, which\nis the honest short-term answer. This bead is about whether it can stop being\nby hand.\n\nHard part, and why it is P3 rather than P2: the test needs the machine's real\nnetwork to change, which CI cannot do and a developer will not do casually.\nOptions to weigh — a Linux container where an interface can genuinely be\ncreated, addressed and torn down (netns makes this cheap and would cover the\nnetlink half properly); a macOS test gated behind an env var that a person sets\nwhen they are about to roam; or accepting it stays by hand and instead logging\nloudly under `-v` when a hint arrives and is judged not-a-change, so the silent\nfailure at least leaves a trace.\n\nDone when either the platform half is exercised automatically on at least one\nplatform — a real interface change, not an injected file — or the decision not\nto is recorded with the `-v` trace that makes a quietly dead watcher visible,\nand VERIFICATION.md's by-hand item reflects whichever way it went.","notes":"Re-weighted P3 to P2 after acs-ft1 (PR #182, ecd5d5e).\n\nThe untested platform path is now load-bearing twice over. acs-6p8 made it\ndecide whether an offline wait is cut short; acs-ft1 makes it decide whether a\n*connected, healthy* session is probed and, if the host does not answer within\nACS_NETCHECK_MS, declared dead at ~2s instead of ~10s.\n\nSo a watcher that quietly stopped emitting — a macOS release changing which\nmessages accompany a roam, a VPN that comes up without moving an address\nLocalNet::usable accepts — no longer merely delays a redial. It silently\nremoves the mechanism that makes a wake-from-sleep or a Wi-Fi switch recover\nin 1-2s, and there is nothing in the logs to say so: the absence of a hint\nlooks exactly like a network that did not change.\n\nThat is the argument for the `-v` trace option in the description. Even if the\nplatform half stays a by-hand check, a line when a hint arrives and is judged\nnot-a-change would distinguish \"the watcher is working and nothing moved\" from\n\"the watcher is dead\", which today are indistinguishable from outside.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-24T01:56:20.914698Z","created_by":"michel","updated_at":"2026-09-24T06:00:09.314135Z","closed_at":"2026-09-24T06:00:09.313982Z","close_reason":"Done in PR #192 (9bc0b5c), both outcomes: tests/netns.rs drives the real kernel via ip link/addr under CAP_NET_ADMIN in test_linux.sh, shown red two ways; and a -v trace makes a dead watcher visible on macOS, where silence across a roam is now evidence","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-4km","title":"Verify the uploaded binary on the remote before executing it","description":"Security, self-attested integrity. The remote install script is `set -e; mkdir -p <dir>; cat > <tmp>; chmod 755 <tmp>; exec <tmp> _install --finish --token <t> --sha256 <digest>` (install.rs). Verified by reading it: the uploaded file is made executable and RUN, and the digest the client computed is then checked by the very binary being checked, which hashes its own current_exe(). A substituted binary simply skips the check and prints ok, which is all side_call looks for. In the payload path the appended blob is only structurally parsed by Payloads::parse and never checksummed.\n\nAttack: anyone who can replace the file between the cat and the exec (a second person on a shared account, a compromised sshd, a remote whose $HOME is writable by others) gets their code run and then installed at ~/.local/share/acs/<v>/acs, which every later acs connection execs.\n\nAlready good, so do not redo it: the token is 64 bits from /dev/urandom, write_exe uses create_new (O_EXCL), and parse_finish rejects a non-hex token.\n\nDone when the client verifies the digest before the binary is executed (a shasum/sha256sum call on the remote, or reading the file back) and the payload blob is digested too, with a test that a tampered upload is refused.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-20T04:36:00.016782Z","created_by":"michel","updated_at":"2026-09-20T05:14:17.952799Z","closed_at":"2026-09-20T05:14:17.952715Z","close_reason":"Implemented in #113: the remote shell checks the digest before the upload can run","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-4pv","title":"Ping IPv6 alias hosts on macOS, where ping is IPv4-only","description":"alias::ping_with (src/alias.rs:237-252) runs the system ping and takes status.success() as reachable. macOS ping is IPv4-only: `ping -c 1 -t 2 -- ::1` exits 68 \"cannot resolve\", so an IPv6 literal or a name with only AAAA records can never be chosen.\n\nScenario: aliases: nas: [{host: \"fd00:1::20\"}] or an IPv6-only LAN name. With -v acs logs \"does not answer ping within 500ms\" every time and exits 255, or falls back to the wrong entry, although ssh would connect fine. prefer_local_network explicitly matches IPv6 networks (README, netmatch tests with fd00:1::5/64), so a user on an IPv6 LAN sees \"is on the local network fd00:1::/64\" followed by \"does not answer ping\". Linux iputils ping is dual-stack, so this is macOS-specific.\n\nDone when an IPv6 literal (or a ping exit 68 on macOS) uses ping6, or the README documents reachability_check: false for such hosts, with a test for the IPv6 literal path.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-19T21:46:58.807658Z","created_by":"michel","updated_at":"2026-09-19T22:32:06.715562Z","closed_at":"2026-09-19T22:32:06.715483Z","close_reason":"Implemented in #79 (1fea487)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-4w8","title":"Carry a half-read frame over the proxy's HELLO hand-off to the relay","description":"proxy::attach (src/proxy.rs:280-285) re-encodes only complete frames left in dec (`while let Ok(Some(m)) = dec.next_msg()`); bytes of an incomplete frame (dec.pending() > 0) are discarded, and relay_fds then reads fresh from stdin, forwarding the remainder of that frame to the master with its head missing. The master's decoder desyncs, answers BAD_REQUEST/Closing, and the client exits with code::ERROR.\n\nLatent today: the client sends nothing between HELLO and WELCOME (stdin and SIGWINCH are gated on welcomed, client.rs:885-903). Any future pipelining after HELLO (e.g. sending the unacked resend with the HELLO) would hit it. Decoder::take_rest() exists for exactly this and pick.rs uses it.\n\nDone when attach appends dec.take_rest() to the bytes forwarded to the master, with a proxy test that splits a RESIZE across the hand-off.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-19T21:46:59.388669Z","created_by":"michel","updated_at":"2026-09-19T23:19:40.024176Z","closed_at":"2026-09-19T23:19:40.024082Z","close_reason":"Implemented in #97 (4996762)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-55v","title":"Bound the key lexer string state so command mode cannot be suppressed","description":"Security, denial of the detach key. keys.rs enters Lex::Str on ESC ] , ESC P or ESC _ from stdin and then forwards every byte raw without calling token(), so Ctrl-] is not recognised, command mode cannot arm and Detach cannot fire. The only way out is the 100 ms idle timeout in tick, which never fires while bytes keep arriving.\n\nAttack: a malicious remote sets a multi-megabyte clipboard with OSC 52 and queries it back in a loop. The terminal streams one enormous continuously-arriving reply, the user's Ctrl-] Ctrl-] d is swallowed into it and forwarded to the attacker instead, and the session cannot be detached without killing the terminal. DESIGN §6.3 documents the timeout as the mitigation for the Alt+_ case only; the adversarial case is not considered.\n\nRelated to the already-filed Alt+_ lexer bug, which is the accidental version of the same state.\n\nDone when the string state is bounded by total bytes as well as idle time, the command key is recognised even inside it (or the state is abandoned on a cap), and a test streams an unending OSC reply and still detaches.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-20T04:36:00.652472Z","created_by":"michel","updated_at":"2026-09-20T05:46:06.616463Z","closed_at":"2026-09-20T05:46:06.616260Z","close_reason":"Implemented in #121: the string state is bounded by bytes as well as by silence","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-5em","title":"Let a fork set at build time which repo updates come from","description":"User's words: \"is it possible at release time to specify which github repo updates are coming from? This is needed in case someone wants to fork\".\n\nToday it is not. The update source can only be changed at **runtime, per environment**, never baked into the binary a fork ships.\n\nVerified (v0.11.4):\n\n- `release::DEFAULT_RELEASES_URL` (src/release.rs:16) is a plain hardcoded const, `https://github.com/michel-onstein/acs/releases`.\n- `release::releases_url()` (src/release.rs:35) lets `ACS_RELEASES_URL` override it, and that is honoured by `acs upgrade` (upgrade.rs:273) and the weekly update check (update_check.rs:220), and separately by scripts/install.sh:25-26. The guards from acs-95w stay as they are: https only unless `--allow-insecure-url` is passed on the command line, and ignored outright when the real and effective uid differ.\n- There is no config-file setting for it: `local_networks` and friends are in `config::KEYS`, a releases URL is not.\n- `build.rs` already emits `cargo:rustc-env=ACS_TARGET`, so the compile-time mechanism needed here is in place and used.\n\nSo a fork has two bad options: patch the const (a merge conflict on every upstream sync) or tell every one of its users to export `ACS_RELEASES_URL` forever.\n\nChange: let the default be chosen at build time, falling back to today's constant.\n\n```rust\n// build.rs\nprintln!(\"cargo:rerun-if-env-changed=ACS_DEFAULT_RELEASES_URL\");\nif let Ok(u) = std::env::var(\"ACS_DEFAULT_RELEASES_URL\") {\n    println!(\"cargo:rustc-env=ACS_DEFAULT_RELEASES_URL={u}\");\n}\n\n// src/release.rs\npub const DEFAULT_RELEASES_URL: &str = match option_env!(\"ACS_DEFAULT_RELEASES_URL\") {\n    Some(u) => u,\n    None => \"https://github.com/michel-onstein/acs/releases\",\n};\n```\n\nThis is not a loosening of acs-95w. The runtime guards exist because the environment is attacker-controllable across a privilege boundary; a compiled-in value is chosen by whoever builds the binary, who already decides what it does. The sudo and https rules on the runtime override are untouched.\n\nThe other places a fork must still change, which this bead should cover or explicitly leave out:\n\n| Where | What | Overridable today |\n| --- | --- | --- |\n| src/release.rs:16 | update check, `acs upgrade` | env only |\n| src/install.rs:85 `INSTALLER` | the `curl ... \\| sh` line in the \"not installed\" message (install.rs:96) | no, hardcoded |\n| scripts/install.sh:25 | the installer's own default | `ACS_RELEASES_URL` |\n| xtask/src/formula.rs:9 `REPO` | Homebrew formula URLs | no |\n| xtask/src/package.rs:31,35 | install instructions written into NOTES.md | no |\n| scripts/update-tap.sh:30 | which tap to push | `ACS_TAP_REPO` (already done) |\n\n`update-tap.sh` already anticipated this; nothing else did. `INSTALLER` is only used in a message, so it is cosmetic, but it sends a fork's users upstream to install and should derive from the same base.\n\nInteracts with acs-o9v (sign release artifacts), which is parked on a signing-scheme decision: if a fork publishes its own releases, the key embedded in the binary has to be the fork's key too. Whatever mechanism lands here for the URL is the natural one for the key, so o9v should be designed with this in mind rather than hardcoding one key. Not a blocking dependency in either direction — they are coupled by design, not by order.\n\nNothing in docs/ covers forking at all: a grep for \"fork\" over docs/ and README.md finds only `forkpty`.\n\nDone when the default releases URL can be set at build time and falls back to today's value when unset, `INSTALLER` derives from the same base rather than being its own const, a test covers both the unset and the set case, and a short \"Forking\" section in docs/VERSIONING.md lists every knob a fork must set (the build-time URL, `ACS_TAP_REPO`, the xtask formula/package strings) so the list above does not have to be rediscovered.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-20T16:03:19.295578Z","created_by":"michel","updated_at":"2026-09-23T18:39:28.702583Z","closed_at":"2026-09-23T18:39:28.702344Z","close_reason":"Implemented in PR #153 (be220ed): ACS_DEFAULT_RELEASES_URL at build time, installer() derives from it, Forking section in VERSIONING.md","source_repo":"test-single-scalar-local-networks","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/test-single-scalar-local-networks","compaction_level":0,"original_size":0}
{"id":"acs-5v9","title":"acs v1: single-binary persistent ssh sessions","description":"Implement `acs` as designed in `docs/DESIGN.md`: one Rust binary that is the\nlocal client, the per-connection remote proxy and the per-session remote\nmaster, replacing the `dsh` shell function (ssh + dtach).\n\nInvariants every child must preserve:\n\n- Unfiltered stream: pty bytes reach the local terminal verbatim; keystrokes\n  reach the pty verbatim except the command key (DESIGN §2, §6).\n- Transport is the system `ssh`; no ssh-side configuration (DESIGN §3).\n- Lossless resume after a network drop (DESIGN §5.2).\n\nDone when every child is closed and DESIGN.md's Status reads Built.\n","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-09-18T00:27:52.940842Z","created_by":"michel","updated_at":"2026-09-18T02:06:23.678518Z","closed_at":"2026-09-18T02:06:23.678406Z","close_reason":"All 25 children closed: acs v1 implemented, tested on macOS and Linux, verified end to end over real ssh","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs"]}
{"id":"acs-5v9.1","title":"Scaffold the acs crate: size-tuned profile, role dispatch, verify script","description":"Create the Cargo project the rest of the plan builds on (DESIGN §9).\n\n- `Cargo.toml`: release profile `opt-level = \"z\"`, `lto = true`,\n  `codegen-units = 1`, `panic = \"abort\"`, `strip = true`. Dependencies kept\n  minimal: `rustix` (or `nix`) and `lexopt`; no async runtime, no clap.\n- `src/main.rs`: role dispatch — default client; hidden `_proxy`, `_master`,\n  `_install`, `_version`. Stubs are fine.\n- Module skeleton per DESIGN §9 layout (`proto.rs`, `client/`, `master/`,\n  `proxy.rs`, `install.rs`) and an `xtask/` workspace member placeholder.\n- `scripts/verify.sh`: `cargo fmt --check`, `cargo clippy -- -D warnings`,\n  `cargo test`, `npx markdownlint-cli2` on `**/*.md`.\n- Flip DESIGN.md `Status:` to \"In progress\" and its CLAUDE.md index row, in\n  the same change (repo convention).\n\nAcceptance: `scripts/verify.sh` passes; `cargo zigbuild --release --target\nx86_64-unknown-linux-musl` and `aarch64-unknown-linux-musl` build; the slim\nbinary size for each target is recorded in the PR/commit message as the\nbaseline against the < 1 MB budget.\n","status":"closed","priority":1,"issue_type":"chore","created_at":"2026-09-18T00:27:53.032841Z","created_by":"michel","updated_at":"2026-09-18T00:34:15.862017Z","closed_at":"2026-09-18T00:34:15.861911Z","close_reason":"Crate scaffold, size profile, role dispatch, verify script; slim release: x86_64-musl 320280 B, aarch64-musl 297928 B, aarch64-darwin 286080 B","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","infra"],"dependencies":[{"issue_id":"acs-5v9.1","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.032841Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.10","title":"Master: session protocol, takeover, resume, resize, kill","description":"The master's side of the protocol (DESIGN §4.2, §5.2, §5.3).\n\n- HELLO handling with attach / create / attach-or-create; WELCOME with\n  instance id and resumed / gap / fresh.\n- One active client: a new attach sends TAKEOVER to the previous one.\n- Output: pty reads go into the ring; DATA frames from the client's offset;\n  backpressure while attached (stop reading the pty when unsent bytes would\n  be overwritten); ring wraps freely while detached.\n- Input: INPUT dedupe by seq, ACK.\n- RESIZE via `TIOCSWINSZ`; on a fresh attach force a redraw by signalling\n  `tcgetpgrp(pty)` with SIGWINCH even when the size is unchanged.\n- PING/PONG; DETACH; KILL = SIGHUP to the child's process group, SIGKILL\n  after 3 s; EXIT{status} to the client on child exit.\n\nTests (framed test client over the unix socket): resume from an offset\nyields exactly the missed bytes; offset beyond the ring yields gap; a new\ninstance yields fresh; takeover; KILL kills a process group that ignores\nSIGHUP; backpressure engages with a stalled client and not when detached.\n","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T00:27:53.654336Z","created_by":"michel","updated_at":"2026-09-18T00:58:36.205856Z","closed_at":"2026-09-18T00:58:36.205695Z","close_reason":"Master protocol verified by 10 integration tests: exact resume replay, gap and fresh attach, same-identity silent takeover vs BUSY + force, KILL SIGHUP then SIGKILL after 3s, backpressure for a stalled client and none while detached, resize, forced redraw on fresh attach, input dedupe","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","remote"],"dependencies":[{"issue_id":"acs-5v9.10","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.654336Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.10","depends_on_id":"acs-5v9.2","type":"blocks","created_at":"2026-09-18T00:27:55.951511Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.10","depends_on_id":"acs-5v9.3","type":"blocks","created_at":"2026-09-18T00:27:56.051540Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.10","depends_on_id":"acs-5v9.9","type":"blocks","created_at":"2026-09-18T00:27:55.858819Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.11","title":"Proxy: connect-or-spawn master and relay frames","description":"`acs _proxy <session> <mode> --proto N` (DESIGN §3, §4.3).\n\n- Resolve the socket directory; connect; on ENOENT/ECONNREFUSED unlink a\n  stale socket and start the master (create modes only), waiting for its\n  readiness report.\n- Write `ACS-READY <proto>` to stdout before the first frame.\n- Check the protocol version in the client's HELLO; on mismatch answer\n  ERROR telling the user to finish or kill the session.\n- Splice stdin/stdout <-> socket until either side closes.\n\nTests: stale socket replaced; two proxies racing to create converge on one\nmaster; mismatched protocol version yields ERROR.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.724896Z","created_by":"michel","updated_at":"2026-09-18T01:04:28.366113Z","closed_at":"2026-09-18T01:04:28.366020Z","close_reason":"proxy.rs: connect-or-start (retries when another master wins), ACS-READY, HELLO proto check, --new naming under dir lock, splice relay, --list; 7 integration tests via stdio incl. stale socket, racing proxies, mismatch, missing session, --new numbering, dropped client detaches","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","remote"],"dependencies":[{"issue_id":"acs-5v9.11","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.724896Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.11","depends_on_id":"acs-5v9.2","type":"blocks","created_at":"2026-09-18T00:27:56.141554Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.11","depends_on_id":"acs-5v9.6","type":"blocks","created_at":"2026-09-18T00:27:56.240509Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.11","depends_on_id":"acs-5v9.9","type":"blocks","created_at":"2026-09-18T00:27:56.336048Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.12","title":"Session listing (--list) via STATUS","description":"`acs <host> --list` (DESIGN §4.3, §4.5).\n\n- Master answers STATUS with name, attached/detached, attached identity (or\n  last attached), creator identity, created-at, idle time, child command,\n  size, acs version.\n- `acs _proxy --list` enumerates `<dir>/*.sock`, queries each, reports and\n  removes stale sockets; no `ps` parsing.\n- Client prints an aligned table; `no sessions` when empty.\n\nTests: listing with attached, detached and stale sessions; output format.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T00:27:53.791059Z","created_by":"michel","updated_at":"2026-09-18T01:17:04.433440Z","closed_at":"2026-09-18T01:17:04.433303Z","close_reason":"list.rs: side call to _proxy --list, STATUS_REPLY frames rendered as NAME/STATE/WHO/IDLE/AGE/COMMAND table, 'no sessions', not-installed case; tests: unit table + integration with attached, detached and stale socket","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client","remote"],"dependencies":[{"issue_id":"acs-5v9.12","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.791059Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.12","depends_on_id":"acs-5v9.10","type":"blocks","created_at":"2026-09-18T00:27:56.432085Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.12","depends_on_id":"acs-5v9.11","type":"blocks","created_at":"2026-09-18T00:27:56.523191Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.13","title":"Client terminal: raw mode, guaranteed restore, signals","description":"Local tty handling (DESIGN §7).\n\n- Raw mode with dtach's termios flags; the original termios restored on\n  every exit path: normal return, SIGHUP/SIGTERM/SIGINT, and panic (drop\n  guard plus signal handler, `panic = \"abort\"`).\n- Self-pipe for signals; SIGWINCH surfaced as a resize event.\n- Single-threaded `poll` loop skeleton with timers (escape timeout, pings).\n\nTests: termios flag computation as a unit test; a pty-based test that the\nterminal is restored after the client is killed by a signal and after a\npanic.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.862722Z","created_by":"michel","updated_at":"2026-09-18T00:50:08.491164Z","closed_at":"2026-09-18T00:50:08.491056Z","close_reason":"tty.rs RawMode (dtach flags, suspend/resume) + emergency restore via panic hook and SIGTERM/HUP/QUIT/INT handlers; sys.rs termios/pty/poll/pipe/signal self-pipe/peer_uid/wait wrappers; pty-based tests prove restore after SIGTERM and after panic","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.13","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.862722Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.13","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:55.454697Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.14","title":"Integration test harness: local transport, pty runner, producers","description":"Shared test infrastructure for everything end-to-end (DESIGN §9.1).\n\n- Temp `ACS_SOCKET_DIR` and temp `HOME` per test.\n- Run the client under a pty with `--transport-cmd` pointing at the built\n  binary's `_proxy` (no ssh).\n- Deterministic producers (numbered lines, binary patterns incl. escape\n  sequences) and a byte-exact capture of the client's stdout.\n- A framed test client for driving the master/proxy directly.\n- Transport killer to simulate a network drop mid-stream.\n\nAcceptance: a smoke test attaches, runs a producer, and asserts byte\nequality of output; harness usable from `cargo test`.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.934679Z","created_by":"michel","updated_at":"2026-09-18T01:05:49.359008Z","closed_at":"2026-09-18T01:05:49.358738Z","close_reason":"tests/common: Remote fixture (temp HOME with versioned install, socket dir, pid-recording transport wrapper to cut links), pty Client runner (keys, resize, wait_for, exit code), binary pattern producer; smoke tests prove prelude->proxy->master->pty chain byte-exact and ACS-NEED","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","test"],"dependencies":[{"issue_id":"acs-5v9.14","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.934679Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.14","depends_on_id":"acs-5v9.10","type":"blocks","created_at":"2026-09-18T00:27:56.708246Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.14","depends_on_id":"acs-5v9.11","type":"blocks","created_at":"2026-09-18T00:27:56.615671Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.14","depends_on_id":"acs-5v9.7","type":"blocks","created_at":"2026-09-18T00:27:56.804669Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.15","title":"Client: ssh transport, handshake and byte-exact session","description":"The client's session path without reconnect (DESIGN §3, §4.4, §5, §6, §7).\n\n- Spawn ssh via the builder; sync on `ACS-READY` (discard noise, show it\n  under `-v`); detect `ACS-NEED` and hand off to install.\n- HELLO with TERM, COLORTERM, size; DATA written verbatim to stdout while\n  tracking the last offset; stdin through the command-key detector into\n  INPUT with seq; RESIZE on SIGWINCH.\n- `d`: DETACH, restore terminal (with observer resets), print\n  `acs: detached from <host>/<name> - reattach with: acs <host> <name>`,\n  exit 0. `x`: KILL, wait for EXIT, exit with the child's status.\n- On creating a session print `acs: new session '<name>' on <host>` on\n  stderr before raw mode.\n- Exit codes per DESIGN §7.\n\nTests (harness): attach-or-create, byte-exact output incl. escape sequences,\ndetach and re-attach, `x` ends the remote session, EXIT status propagated,\nnew-session message.\n","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T00:27:54.004739Z","created_by":"michel","updated_at":"2026-09-18T01:09:38.116376Z","closed_at":"2026-09-18T01:09:38.116230Z","close_reason":"client.rs: dial with marker sync, HELLO/WELCOME, raw mode, verbatim DATA with offset tracking, detector->INPUT with seq, RESIZE, d/x, new-session and reattach messages, mode resets, exit codes; 11 pty tests incl. 300 KB byte-exact output, detach/reattach, x, exit status, single-escape passthrough","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.004739Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9.13","type":"blocks","created_at":"2026-09-18T00:27:56.988175Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9.14","type":"blocks","created_at":"2026-09-18T00:27:56.896223Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9.4","type":"blocks","created_at":"2026-09-18T00:27:57.080490Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9.5","type":"blocks","created_at":"2026-09-18T00:27:57.169988Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9.7","type":"blocks","created_at":"2026-09-18T00:27:57.261053Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.15","depends_on_id":"acs-5v9.8","type":"blocks","created_at":"2026-09-18T00:27:57.360296Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.16","title":"Client: liveness, automatic reconnect and lossless resume","description":"Resume after network drops (DESIGN §5.2-5.4).\n\n- PING after 3 s of silence; link dead after 10 s without a frame: kill ssh\n  and redial with backoff 1 s -> 30 s, reset after a connection that lasted\n  30 s. `--no-reconnect` disables.\n- HELLO with resume{instance, offset}; handle resumed / gap (clear + redraw)\n  / fresh (\"session was restarted\").\n- Resend unacked input; drop keys typed while the link is known down, but\n  keep command mode working (`d` detaches while down; `x` reports it needs\n  the link).\n- Status while disconnected: nothing for < 10 s; then one bottom-row line\n  with save/restore cursor and the xterm title stack; one forced redraw\n  after a resume that followed a status line.\n- Cooked mode while ssh authenticates on redial (password / key touch).\n- TAKEOVER ends the client with a message and the reattach hint.\n\nTests (harness): kill the transport mid-stream and assert the client's\nstdout equals the producer's output byte for byte; input sent before the\ndrop is not duplicated; ring overflow while disconnected yields gap\nhandling; `--no-reconnect` exits on drop.\n","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T00:27:54.075054Z","created_by":"michel","updated_at":"2026-09-18T01:15:55.463071Z","closed_at":"2026-09-18T01:15:55.462952Z","close_reason":"reconnect.rs: Liveness (PING after 3s, dead after 10s), Backoff 1s->30s with reset, redial with resume, unacked resend, keys dropped while down but d works and x explains, status line + title stack, redraw via resize pair; 7 pty tests (cut, freeze, input once, --no-reconnect, detach while down, x needs link, gap) + unit tests; daemons close inherited fds","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.16","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.075054Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.16","depends_on_id":"acs-5v9.15","type":"blocks","created_at":"2026-09-18T00:27:57.464472Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.16","depends_on_id":"acs-5v9.3","type":"blocks","created_at":"2026-09-18T00:27:57.559623Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.17","title":"Client: immediate redial on network change","description":"Stretch goal from DESIGN §5.3: skip the backoff wait when the default route\nchanges (Wi-Fi switch, wake from sleep) — route socket / SCNetworkReachability\non macOS, netlink on Linux — where it is cheap to observe.\n\nTests: the reconnect scheduler redials immediately on an injected\n\"network changed\" event.\n","status":"closed","priority":3,"issue_type":"feature","created_at":"2026-09-18T00:27:54.142691Z","created_by":"michel","updated_at":"2026-09-18T01:40:22.774873Z","closed_at":"2026-09-18T01:40:22.774739Z","close_reason":"netwatch.rs: PF_ROUTE (address/interface messages only, ARP churn ignored) on macOS, NETLINK_ROUTE link/addr groups on Linux, FIFO injection for tests; offline wait redials at once on a change (max once per 2s) and resets backoff; unit tests + integration test (20s backoff bypassed)","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.17","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.142691Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.17","depends_on_id":"acs-5v9.16","type":"blocks","created_at":"2026-09-18T00:27:57.654190Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.18","title":"Shared accounts: client identity and confirmed takeover","description":"Accident protection when several people use one remote account (DESIGN §4.5).\n\n- Client identity `<local user>@<local hostname>`, overridable with\n  `ACS_IDENTITY`, sent in HELLO; the master records attached and creator\n  identities.\n- Cross-identity attach gets BUSY{identity, since}; the client asks\n  `take over? [y/N]` on the terminal, retries HELLO with `force` on yes;\n  without a terminal it fails; `--force` skips the question.\n- Same identity takes over silently (own dropped connection, own second\n  terminal).\n\nTests (harness): same identity silent; different identity BUSY; force\ntakes over; non-tty without `--force` fails.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T00:27:54.213017Z","created_by":"michel","updated_at":"2026-09-18T01:18:22.691743Z","closed_at":"2026-09-18T01:18:22.691619Z","close_reason":"Client identity (ACS_IDENTITY or user@host) in HELLO; BUSY answered with a cooked-mode [y/N] prompt naming who and since when, --force skips, non-tty declines; same identity silent takeover; ACS_DEFAULT_SESSION; 5 pty tests","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client","remote"],"dependencies":[{"issue_id":"acs-5v9.18","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.213017Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.18","depends_on_id":"acs-5v9.10","type":"blocks","created_at":"2026-09-18T00:27:57.848530Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.18","depends_on_id":"acs-5v9.15","type":"blocks","created_at":"2026-09-18T00:27:57.747247Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.19","title":"Multi-user integration tests in a Linux container","description":"Prove isolation between Unix accounts (DESIGN §4.5, §9.1) in a Linux\ncontainer with two users (docker/podman; the macOS host cannot switch uids\nwithout root).\n\n- Another user's pre-created `/tmp/acs-<uid>` (squatted, and symlinked) is\n  refused.\n- A connection from another uid to a master's socket is dropped.\n- Two users' `main` sessions are independent.\n- The whole `cargo test` suite also runs on Linux musl in the container.\n\nAcceptance: one script (`scripts/test_linux.sh`) runs it end to end.\n","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-18T00:27:54.284950Z","created_by":"michel","updated_at":"2026-09-18T01:57:26.611781Z","closed_at":"2026-09-18T01:57:26.611566Z","close_reason":"tests/multiuser.rs (squatted dir, symlinked dir, foreign peer uid dropped by master, independent per-user main) + scripts/test_linux.sh running the full suite on Linux musl and the multi-user tests as root with alice/bob; all pass. The Linux run found and fixed a master backpressure stall and a replaced-binary spawn failure.","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","security","test"],"dependencies":[{"issue_id":"acs-5v9.19","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.284950Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.19","depends_on_id":"acs-5v9.18","type":"blocks","created_at":"2026-09-18T00:27:57.945274Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.19","depends_on_id":"acs-5v9.6","type":"blocks","created_at":"2026-09-18T00:27:58.051922Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.2","title":"Frame codec, message types and ACS-READY marker","description":"Implement the wire protocol of DESIGN §5.1 in `proto.rs`.\n\n- Frame: `type u8 | len u32 BE | payload`. Hard maximum frame length\n  (e.g. 1 MiB) rejected as a protocol error.\n- Incremental decoder that accepts arbitrary read boundaries.\n- All message types: HELLO (incl. client identity, `force`, TERM, COLORTERM,\n  size, optional resume{instance, offset}), WELCOME (resumed/gap/fresh,\n  created), BUSY, DATA (u64 offset), INPUT (u64 seq), ACK, RESIZE, PING/PONG,\n  DETACH, KILL, EXIT, TAKEOVER, STATUS/STATUS_REPLY, ERROR.\n- `PROTO_VERSION` constant; the `ACS-READY <proto>\\n` marker and the\n  `ACS-NEED <os> <arch>` line (DESIGN §3, §8): emit and a scanner that\n  discards arbitrary preceding noise.\n\nTests: round-trip every message; decode with the input split at every byte\nboundary; oversize and unknown-type frames error; marker found after\nshell-rc noise and across split reads.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.100672Z","created_by":"michel","updated_at":"2026-09-18T00:36:31.095955Z","closed_at":"2026-09-18T00:36:31.095852Z","close_reason":"proto.rs: frames, all message types, incremental decoder, ACS-READY/ACS-NEED scanner; 9 unit tests","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","proto"],"dependencies":[{"issue_id":"acs-5v9.2","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.100672Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.2","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:54.778527Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.20","title":"Payload set: format, ELF trailer and Mach-O embed","description":"Carry slim remote binaries inside a complete binary (DESIGN §8.1).\n\n- Payload set format: index of (target triple, compressed size, SHA-256,\n  gzip bytes).\n- ELF: trailer `[slim][P entries][index][magic + index offset]`, read back\n  through `current_exe()`; writer used by xtask and by `_install --finish`.\n- Mach-O: `embed-payloads` cargo feature using `include_bytes!` of a path\n  supplied at build time.\n- API: `own_target()`, `is_complete()`, `payload_for(target)`.\n\nTests: build a synthetic trailer and read it back; truncated or corrupt\ntrailer means slim, never a crash.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:54.356588Z","created_by":"michel","updated_at":"2026-09-18T01:20:14.979366Z","closed_at":"2026-09-18T01:20:14.979230Z","close_reason":"payload.rs: blob format (gz entries + index + footer), build/parse, ELF trailer via current_exe, embed-payloads feature via include_bytes (build.rs ACS_PAYLOADS_FILE), own target (build.rs ACS_TARGET), uname->target map; dependency-free sha256.rs with FIPS vectors; corrupt/truncated input never panics","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","install"],"dependencies":[{"issue_id":"acs-5v9.20","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.356588Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.20","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:55.553831Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.21","title":"Remote self-install: ACS-NEED, versioned install, _install --finish","description":"Install `acs` on a remote on first contact (DESIGN §8, §8.1, §4.5).\n\n- On `ACS-NEED <os> <arch>` map to a target. Case 1 (own ELF target):\n  stream own file with `cat`. Case 2 (Linux target in P): stream gz(slim_r)\n  to `gzip -dc > ~/.local/share/acs/<v>/acs.new.<token>`, then run\n  `acs.new.<token> _install --finish --sha256 <digest>` with P on stdin,\n  which appends the trailer, verifies the digest, renames to `acs`, and\n  repoints `~/.local/bin/acs` (temp symlink + rename). Case 3 (macOS other\n  arch): clear manual-install message.\n- Random token per install so concurrent installs converge; missing `gzip`\n  gives a clear error; side calls keep the user's ssh multiplexing; redial\n  after install.\n\nTests (harness, local \"remote\" with temp HOME): install from slim fails\ncleanly with the `cargo xtask dist` hint; complete binary installs and the\ninstalled copy is byte-identical to the dist output for that target;\nconcurrent same-version installs converge; two versions coexist and each\nserves its own client.\n","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T00:27:54.429364Z","created_by":"michel","updated_at":"2026-09-18T01:36:46.414425Z","closed_at":"2026-09-18T01:36:46.414306Z","close_reason":"install.rs: plan (self-copy for own target; gzip'd slim + payload blob for other Linux targets; clear errors otherwise), side calls, _install --finish (sha256 check, new file since running exe is ETXTBSY, atomic rename, ~/.local/bin symlink swap), once per run; --version lists installable targets; 6 integration tests incl. payload path via fake uname, concurrent installs, corrupt upload; verified dist binaries in docker","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","install"],"dependencies":[{"issue_id":"acs-5v9.21","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.429364Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.21","depends_on_id":"acs-5v9.11","type":"blocks","created_at":"2026-09-18T00:27:58.336139Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.21","depends_on_id":"acs-5v9.15","type":"blocks","created_at":"2026-09-18T00:27:58.242296Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.21","depends_on_id":"acs-5v9.20","type":"blocks","created_at":"2026-09-18T00:27:58.146198Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.22","title":"Prune unused remote versions","description":"Housekeeping from DESIGN §8: `_proxy` touches its version directory on\nstart; version directories untouched for 30 days with no running master of\nthat version (per STATUS) are removed at the next proxy start.\n\nTests: old unused version pruned; old version with a live master kept;\ncurrent version never pruned.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-18T00:27:54.497216Z","created_by":"michel","updated_at":"2026-09-18T01:38:28.593295Z","closed_at":"2026-09-18T01:38:28.592936Z","close_reason":"prune.rs: proxy touches its version dir; at most daily (.pruned marker) removes other version dirs untouched for 30 days unless a live master reports that version (STATUS); unit test with backdated mtimes + integration test via proxy start","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","install"],"dependencies":[{"issue_id":"acs-5v9.22","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.497216Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.22","depends_on_id":"acs-5v9.12","type":"blocks","created_at":"2026-09-18T00:27:58.533734Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.22","depends_on_id":"acs-5v9.21","type":"blocks","created_at":"2026-09-18T00:27:58.434063Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.23","title":"cargo xtask dist: slim builds, payloads, complete binaries, size budget","description":"Release build pipeline (DESIGN §8.1, §9).\n\n1. Slim builds for `aarch64-apple-darwin`, `x86_64-apple-darwin` (cargo) and\n   `x86_64-unknown-linux-musl`, `aarch64-unknown-linux-musl`\n   (`cargo zigbuild`).\n2. gzip -9 the Linux slim builds into the payload set.\n3. Complete Linux binaries by appending the trailer; complete macOS binaries\n   by rebuilding with `--features embed-payloads`, then ad-hoc `codesign`.\n4. Output `dist/<target>/acs` plus a size report; fail if slim > 1 MB or\n   complete > 2 MB.\n\nAcceptance: `cargo xtask dist` on this Mac produces all four; `codesign -v`\npasses on the macOS ones; replace the size estimates in DESIGN §8.1 with the\nmeasured numbers.\n","status":"closed","priority":1,"issue_type":"chore","created_at":"2026-09-18T00:27:54.565846Z","created_by":"michel","updated_at":"2026-09-18T01:36:46.643553Z","closed_at":"2026-09-18T01:36:46.643290Z","close_reason":"cargo xtask dist builds 4 targets (zigbuild musl, native macOS), payload blob, complete Linux (appended) and macOS (embedded, ad-hoc signed, codesign -v ok), size report with budget; measured slim 473-602 KB, complete 1.10-1.21 MB; linux binaries verified in docker alpine (aarch64 native, x86_64 emulated)","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","infra","install"],"dependencies":[{"issue_id":"acs-5v9.23","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.565846Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.23","depends_on_id":"acs-5v9.20","type":"blocks","created_at":"2026-09-18T00:27:58.627402Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.24","title":"Manual verification matrix against real hosts","description":"Check the things automated tests cannot (DESIGN §9.1) and record results\nin `docs/VERIFICATION.md`.\n\n- TUIs: vim (tag jump, mouse), Claude Code, htop, less; OSC 52 copy; a\n  kitty-keyboard-protocol TUI (command key still works); local scrollback.\n- Drops: `pfctl` block, Wi-Fi switch, laptop sleep/wake; output lossless.\n- Install: macOS client -> Linux x86_64 and aarch64 hosts; Linux client ->\n  next hop.\n- Shared account: two identities, confirmation prompt.\n- `-i` key selection against a host that needs it (e.g. corello).\n","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-18T00:27:54.632672Z","created_by":"michel","updated_at":"2026-09-18T02:05:19.205874Z","closed_at":"2026-09-18T02:05:19.205710Z","close_reason":"scripts/e2e_ssh.sh + tests/e2e_ssh.rs over real ssh to a container host: install on first contact, -i selection, vim, OSC 52/8, kitty command key, mouse, killed connection, frozen host, --list — all 9 pass; results and the hand-only checks (real terminal rendering, Claude Code, real Wi-Fi/sleep, corello) recorded in docs/VERIFICATION.md. Added ConnectTimeout=10 to the session transport.","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","test"],"dependencies":[{"issue_id":"acs-5v9.24","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.632672Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.24","depends_on_id":"acs-5v9.12","type":"blocks","created_at":"2026-09-18T00:27:59.105449Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.24","depends_on_id":"acs-5v9.16","type":"blocks","created_at":"2026-09-18T00:27:58.721730Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.24","depends_on_id":"acs-5v9.18","type":"blocks","created_at":"2026-09-18T00:27:58.817298Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.24","depends_on_id":"acs-5v9.21","type":"blocks","created_at":"2026-09-18T00:27:58.911623Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.24","depends_on_id":"acs-5v9.23","type":"blocks","created_at":"2026-09-18T00:27:59.006328Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.25","title":"Docs and migration from dsh","description":"Finish the rollout (DESIGN §10).\n\n- `README.md`: install, usage, command keys, environment variables.\n- DESIGN.md `Status:` -> Built, and its CLAUDE.md index row, in the same\n  change.\n- Migration note: run `acs` alongside `dsh`; then replace the `dsh` function\n  in `~/.zshrc` with `alias dsh=acs` and drop the dtach dependency (the\n  dotfile change is the user's to make).\n","status":"closed","priority":2,"issue_type":"docs","created_at":"2026-09-18T00:27:54.702768Z","created_by":"michel","updated_at":"2026-09-18T02:06:23.526943Z","closed_at":"2026-09-18T02:06:23.526802Z","close_reason":"README.md (install, usage, command keys, ssh options, reconnect, shared accounts, environment, development scripts, dsh migration); DESIGN status Built; CLAUDE.md index rows for README/DESIGN/VERIFICATION and the test scripts; scripts/verify.sh passes","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","docs"],"dependencies":[{"issue_id":"acs-5v9.25","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:54.702768Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.25","depends_on_id":"acs-5v9.24","type":"blocks","created_at":"2026-09-18T00:27:59.210331Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.3","title":"Resume buffers: output ring with u64 offsets and input ack tracking","description":"Data structures behind lossless resume (DESIGN §4.2, §5.2).\n\n- Output ring: append bytes, monotonically increasing u64 offsets,\n  configurable capacity (default 1 MiB); `read_from(offset)` returns\n  data / Gap (overwritten) / Future (invalid). Tracks the unsent watermark of\n  the attached client so the master can apply backpressure.\n- Input tracking: client-side queue of sent-but-unacked bytes (push, ack up\n  to seq, iterate for resend); master-side dedupe that writes only the part\n  of a resent INPUT beyond the last written seq.\n\nTests: wrap-around; gap detection exactly at the boundary; resend that\npartially overlaps what the master already wrote is not duplicated.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.169668Z","created_by":"michel","updated_at":"2026-09-18T00:37:51.144158Z","closed_at":"2026-09-18T00:37:51.144059Z","close_reason":"resume.rs: OutputRing (u64 offsets, gap/future, backpressure room), Unacked (client resend/rebase), InputDedupe (master); WELCOME gains input_seq; 9 unit tests","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","proto"],"dependencies":[{"issue_id":"acs-5v9.3","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.169668Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.3","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:54.868377Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.4","title":"Command-key detector: Ctrl-] Ctrl-] then d (detach) / x (exit)","description":"Pure state machine for command mode (DESIGN §6.1-6.3), fed stdin bytes plus\nan injected clock; outputs bytes to forward, actions (Detach, Exit) and the\nnext deadline.\n\n- First Ctrl-] held up to 400 ms; a lone Ctrl-] or Ctrl-] + other key is\n  forwarded as typed. Ctrl-] Ctrl-] within the window enters command mode;\n  `d` / `x` act; any other key, or 2 s of nothing, forwards all held bytes.\n- Recognise all encodings: legacy `0x1D`; kitty `CSI 93;5u` including event\n  types `:1/:2/:3`; modifyOtherKeys `CSI 27;5;93~`. Consume whole escape\n  sequences (never match inside one); buffer sequences split across reads;\n  release events follow their press (forwarded or swallowed together).\n- Bracketed paste (`CSI 200~` ... `CSI 201~`) passes through untouched.\n- Escape key and window configurable (`ACS_ESCAPE_TIMEOUT_MS`).\n\nTests: table-driven `(bytes, timings) -> (forwarded, action)` covering every\nrule above, including a pasted `^]^]d` and a terminal reply (`ESC`-prefixed)\narriving while a Ctrl-] is held.\n","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T00:27:53.238448Z","created_by":"michel","updated_at":"2026-09-18T00:41:34.276971Z","closed_at":"2026-09-18T00:41:34.276874Z","close_reason":"keys.rs Detector: legacy/kitty/modifyOtherKeys, releases, split reads, bracketed paste, passive replies/mouse, timeouts, configurable key; 20 table tests","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.4","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.238448Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.4","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:54.958716Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.5","title":"Terminal mode observer and reset-on-detach sequences","description":"Passive scanner over the output stream that records which terminal modes the\nremote program has turned on, and produces the matching resets for detach,\nexit, or an abandoned reconnect (DESIGN §6.4). It must never modify, delay or\nreorder the stream.\n\nModes: mouse (1000/1002/1003/1006/1015), bracketed paste (2004), alternate\nscreen (47/1047/1049), cursor visibility (25), focus events (1004), kitty\nkeyboard stack (`CSI > flags u` / `CSI < u`), modifyOtherKeys (`CSI > 4;N m`).\n\nTests: each sequence split at every byte boundary; reset output matches the\nrecorded state and is empty when nothing was set.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T00:27:53.306553Z","created_by":"michel","updated_at":"2026-09-18T00:42:52.682352Z","closed_at":"2026-09-18T00:42:52.682227Z","close_reason":"modes.rs ModeObserver: DEC modes (mouse, paste, alt screen, cursor, focus, DECCKM, 2026), kitty stack/set, modifyOtherKeys, keypad, scroll region; ordered reset; 7 tests incl. every split point","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.5","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.306553Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.5","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:55.051390Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.6","title":"Session names and per-uid socket directory","description":"Everything about where sessions live (DESIGN §4.1, §4.4, §4.5).\n\n- Name validation `[A-Za-z0-9._-]`; default `main` or `$ACS_DEFAULT_SESSION`.\n- Directory: `$ACS_SOCKET_DIR` or `/tmp/acs-<getuid()>` (never `$USER`,\n  never `$TMPDIR`). Create `0700`; on existing path `lstat`: must be a real\n  directory, not a symlink, owned by our uid, mode `0700` — else refuse with\n  an error naming the owner and suggesting `ACS_SOCKET_DIR`.\n- `sun_path` length check with a clear error.\n- `flock` helpers for the per-session creation lock and the directory lock\n  used by `--new` to pick the lowest free number.\n\nTests: symlinked dir refused; wrong mode refused; ownership check via an\ninjectable stat; too-long path; `--new` numbering with gaps and under\nconcurrent callers.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.376975Z","created_by":"michel","updated_at":"2026-09-18T00:44:42.301306Z","closed_at":"2026-09-18T00:44:42.301213Z","close_reason":"session.rs: name rules, ACS_DEFAULT_SESSION, /tmp/acs-<uid> or ACS_SOCKET_DIR with symlink/not-dir/foreign-owner refusal and mode tightening, sun_path check, create/dir flocks, lowest free number; sys.rs libc wrappers; 13 tests","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","remote","security"],"dependencies":[{"issue_id":"acs-5v9.6","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.376975Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.6","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:55.157206Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.7","title":"ssh command builder, option passthrough and remote prelude","description":"Build every ssh invocation (DESIGN §3, §7.1, §8).\n\n- Session calls: transport options first (`-T -e none -o ControlMaster=no\n  -o ControlPath=none -o ServerAliveInterval=0`), then the user's `-i`, `-p`,\n  `-J`, `-F`, `-o` in the order given, then `[user@]host`, then the remote\n  command. Side calls (`--list`, install) omit the transport options so they\n  keep the user's multiplexing.\n- ssh binary from `--ssh` / `ACS_SSH`, default `ssh` on PATH.\n- Hidden `--transport-cmd <cmd>` that replaces ssh with a local command, for\n  the integration harness (DESIGN §9.1).\n- Remote prelude (POSIX sh) with the client version baked in: try\n  `~/.local/share/acs/<v>/acs` then `/usr/local/lib/acs/<v>/acs`, `exec\n  _proxy <session> <mode>`, else print `ACS-NEED $(uname -s) $(uname -m)`.\n  Everything interpolated is shell-quoted (`-- command...` args are\n  arbitrary).\n\nTests: argv snapshots for session vs side calls; precedence (a user\n`-o ControlMaster=auto` comes after ours); quoting of hostile command args.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.441531Z","created_by":"michel","updated_at":"2026-09-18T00:45:57.041307Z","closed_at":"2026-09-18T00:45:57.041184Z","close_reason":"ssh.rs: Transport argv (transport opts first for session calls, side calls keep multiplexing, user -i/-p/-J/-F/-o in order, -- before destination, ACS_SSH, --transport-cmd hook), sh_quote, versioned prelude run under sh -c; WELCOME carries session name; 7 tests incl. executing the prelude","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.7","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.441531Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.7","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:55.256081Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.8","title":"Command-line parsing (dsh-compatible)","description":"`acs [ssh options] [user@]<host> [session] [-l|--list] [--new]\n[--no-reconnect] [--force] [-v] [-- command...]` with `lexopt` (DESIGN §4.4,\n§7, §7.1).\n\n- `-r` accepted and ignored (reconnect is the default); `-l` is `--list`;\n  ssh's `-l <login>` rejected with a hint to use `user@host` or\n  `-o User=`.\n- Invalid combinations rejected: `--list` with a session or `--new`;\n  `--new` with a session name.\n- `ACS_DEFAULT_SESSION` resolves the default session name.\n\nTests: table of argv -> parsed config / error message, including every\n`dsh` invocation form.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.509783Z","created_by":"michel","updated_at":"2026-09-18T00:47:37.357212Z","closed_at":"2026-09-18T00:47:37.357087Z","close_reason":"cli.rs: dsh-compatible parsing with lexopt; ssh -i/-p/-J/-F/-o passthrough in order; -r ignored; -l = --list with user@host hint; --new/--force/-v/--no-reconnect/--ssh/--transport-cmd; -- command; ACS_DEFAULT_SESSION; 8 tests","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-5v9.8","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.509783Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.8","depends_on_id":"acs-5v9.1","type":"blocks","created_at":"2026-09-18T00:27:55.357958Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-5v9.9","title":"Master: daemon lifecycle, pty, child and peer-uid check","description":"`acs _master` process lifecycle (DESIGN §4.2, §4.5).\n\n- Started by the proxy: double-fork, `setsid`, stdio to `/dev/null`; take the\n  session creation lock; bind the socket **before** spawning the child;\n  report readiness over a pipe (dtach's statusfd) so the proxy never races\n  the bind.\n- Child: `$SHELL -l` in `$HOME`, or the `-- command...` given; env `TERM`,\n  `COLORTERM` from HELLO, `ACS_SESSION=<name>`.\n- Reap the child on SIGCHLD, keep its wait status; unlink the socket on exit.\n- Every minute re-check the socket path and re-bind if it was removed\n  (tmpfiles cleanup).\n- On every accepted connection verify the peer uid (`SO_PEERCRED` on Linux,\n  `getpeereid` on macOS) and drop foreign uids.\n- Instance id: random u64 minted at creation.\n\nTests: start a master in a temp `ACS_SOCKET_DIR` running `/bin/sh -c`; two\nconcurrent starts produce one master; child exit status observed; socket\nremoved on exit and re-bound after deletion.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-18T00:27:53.580611Z","created_by":"michel","updated_at":"2026-09-18T00:55:32.577451Z","closed_at":"2026-09-18T00:55:32.577350Z","close_reason":"master.rs: spawn() with readiness pipe on fd 3, fork+setsid daemon, creation lock around check+bind, child on pty with setsid/TIOCSCTTY and TERM/COLORTERM/ACS_SESSION, SIGCHLD reaping, socket unlink on exit and re-bind after deletion, peer-uid check; tests: exit status, concurrent starts, rebind, missing session, protocol mismatch","source_repo":"design-doc","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/design-doc","compaction_level":0,"original_size":0,"labels":["acs","remote"],"dependencies":[{"issue_id":"acs-5v9.9","depends_on_id":"acs-5v9","type":"parent-child","created_at":"2026-09-18T00:27:53.580611Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.9","depends_on_id":"acs-5v9.2","type":"blocks","created_at":"2026-09-18T00:27:55.764769Z","created_by":"michel","metadata":"{}","thread_id":""},{"issue_id":"acs-5v9.9","depends_on_id":"acs-5v9.6","type":"blocks","created_at":"2026-09-18T00:27:55.660447Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-68z","title":"Run the session menu and the attach over one ssh connection","description":"Wanted: a plain `acs <host>` makes two ssh connections, one to list sessions for the menu and one for the session, each paying a full TCP + key exchange + auth handshake. Reduce it to a single call to connect faster. The user's `acs -v corello` on v0.6.0 shows `ssh … _proxy --list` followed by `ssh -o ControlMaster=no -o ControlPath=none … _proxy --session main --mode attach-or-create`.\n\nVerified (v0.6.0): src/pick.rs `choose` lists with `list::query(.., Call::Side, ..)`, then the attach dials its own connection. The session call opts out of multiplexing on purpose (DESIGN §3: a reconnect must never wait on a dead multiplexer), so even `ControlMaster auto` + `ControlPersist` cannot share it. Ending a session with 'x' in the menu is a third kind of call, `_proxy --kill <name>` (pick.rs `end`, src/proxy.rs). The second handshake costs a few hundred ms, more through a jump host, and a hardware key needs two touches.\n\nThis revisits a recorded decision: DESIGN §4.4 says \"reusing one connection for both is not worth a second proxy mode.\" The implementing PR updates §4.4 (and §3/§4.3/§5.1 as needed) and says why the decision changed: connect latency and the double auth.\n\nSketch: a picker mode on the session connection, e.g. `_proxy --pick` with the usual transport options. The proxy first sends the session list as frames (the STATUS data `--list` gathers). The client decides on that list exactly as today (none detached: create; else the menu in menu.rs, unchanged). Menu actions travel on the same connection: '.' (refresh with attached sessions), 'x' (end a session, then a fresh list), new. The pick ends with the ordinary HELLO naming the session and mode (and `force` after a confirmed takeover); the proxy then connects to or starts that master and relays as now. Leaving the menu (Esc/exit) closes the connection cleanly.\n\nUnchanged: redials (they know the session and use the plain session call), `acs <host> <name>`, `--new`, `--list`, and the non-tty fallback, which are already a single call. Keep the install path working: a host without acs answers ACS-NEED on the pick call as on any other, so install, then continue. The ACS-READY marker and the dial deadlines (§5.3) apply to the pick phase. Mind the menu's idle time: the connection sits open while the user reads it, with no liveness pings before WELCOME.\n\nDone when `acs -v <host>` with detached sessions shows one ssh invocation from list to attach, 'x' and '.' work without extra ssh calls, the proto/proxy changes have unit tests and an end-to-end test (scripts/e2e_ssh.sh or the in-process tests) covers pick → attach on one connection, and DESIGN/README describe it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-19T15:37:36.012548Z","created_by":"michel","updated_at":"2026-09-19T15:53:20.497994Z","closed_at":"2026-09-19T15:53:20.497904Z","close_reason":"Implemented in PR #40 (1dc2233): _proxy --pick runs the session menu, x and the attach over one ssh connection","source_repo":"beads-intake-single-dial","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-single-dial","compaction_level":0,"original_size":0}
{"id":"acs-6f5","title":"Specify an ssh local port forward on the command line, mirroring ssh -L","description":"User's words: \"Make it possible that ssh port forward can be specified on command line, mirroring ssh '-L'\".\n\nToday `-L` is rejected outright: the accepted ssh options are exactly `-i -p -J -F -o` (src/cli.rs:162), and any other short option falls through to src/cli.rs:192 and errors with `unknown option -L`.\n\n## It is already possible, badly\n\n`-o` is an unvalidated pass-through — acs never inspects the value — so this works right now:\n\n```sh\nacs -o LocalForward=\"8080 localhost:80\" box\n```\n\nThat matters for scoping: this bead is **not** about making forwarding possible, it is about making it correct and ergonomic. Three reasons it still deserves doing, in order of weight:\n\n**1. The `-o` route is actually wrong, not merely ugly.** `user_opts` is shared by all three `Call` kinds (src/ssh.rs:96-123). `Call::Session` is the user's shell, but `Call::Side` (`acs list <host>`, remote install, the session menu) and `Call::Batch` (`acs list` over every alias, §7.3) build their argv from the same `user_opts`. A `LocalForward` given via `-o` is therefore handed to every one of those ssh processes as well, so several concurrent ssh children try to bind the same local port. ssh's default `ExitOnForwardFailure=no` means each one warns and carries on, so it is noisy and wrong rather than fatal. A real `-L` can be emitted for `Call::Session` only.\n\n**2. No validation.** A malformed `-o LocalForward=` spec is discovered by ssh, per dial, mid-reconnect-loop, as a line of ssh stderr on a terminal acs is otherwise keeping clean.\n\n**3. Ergonomics and muscle memory.** acs replaces `ssh`+`dtach`; `-L` is the spelling people already know, and `-o LocalForward=\"8080 localhost:80\"` requires knowing that the config-file form uses a space where `-L` uses a colon.\n\n## Verified about the code (v0.11.4)\n\n- **The argv is built in exactly one place**, `Transport::argv()` at src/ssh.rs:96-123, layout `<ssh> <fixed opts for the Call> <user_opts…> [-i <key>] -- <destination> <remote command>`. Every call site funnels through it (src/client.rs:357, src/install.rs:210, src/list.rs:61,142, src/pick.rs:54,463).\n- **Order is load-bearing.** ssh keeps the *first* value it sees, which is why acs puts its own transport options before the user's (docs/DESIGN.md:876-883). A forward must be emitted with or after `user_opts`, not before.\n- **Reconnect needs no work.** `reconnect::run()` (src/reconnect.rs:60-169) clones `ClientArgs` once at :75 and re-dials through the same builder each pass (:76-88 → client.rs:357), so the argv is rebuilt from scratch every dial. A forward stored on `Transport` is re-applied on every reconnect for free.\n- **But the forward does drop during a redial.** The old ssh child is dead, so the listening socket is released and rebound. Worth one line in the docs, and it is the argument for `-o ExitOnForwardFailure=yes` being considered — a rebind that loses a race (a stale TIME_WAIT, a second acs on the same port) otherwise leaves a live session with a silently dead forward.\n- **No duplicate bead.** Every \"forward\" hit in src/ is keystroke forwarding (src/keys.rs:62, src/proxy.rs:287); `acs-aps` is ssh *agent* forwarding, a different thing. A grep for LocalForward/tunnel/socks over src/, docs/, README.md and tests/ finds nothing, and all 107 records in .beads/issues.jsonl were checked.\n\n## Implementation sketch\n\n1. src/cli.rs:162 — accept `Short('L')`, repeatable, and validate the `[bind:]port:host:hostport` spec with a real error. USAGE at src/cli.rs:20-51 needs the entry too.\n2. src/ssh.rs:51-66 — a new `Transport` field (`local_forwards: Vec<String>`) rather than stuffing `user_opts`, precisely so it can be gated.\n3. src/ssh.rs:96-123 — emit `-L <spec>` after `user_opts`, **only when `call == Call::Session`**.\n4. Optional, and the reason to look at acs-c9d: a `local_forwards` list setting, global and per alias, mirroring `local_networks` exactly (Config field config.rs:129, `Option` field on Alias config.rs:99, `KEYS` :166, `ALIAS_KEYS` :199, a type const beside `NETWORKS` :211, a `*_for(alias)` accessor like :509, and a branch in config_cmd::typed at config_cmd.rs:93). Per-alias is the natural home for \"this host always needs its database forwarded\". `acs-mbd` (identity file per alias) is the precedent for a per-alias ssh-affecting setting.\n5. Docs: docs/DESIGN.md §3 (:133-152) and §7.1 (:840-883), README.md:173-182.\n\n## Scope decisions to make when taking this\n\n- **`-R` and `-D` as well, or `-L` only?** The user asked for `-L`. `-R` and `-D` are the same plumbing and the same gating; doing all three at once costs little more than one. Decide explicitly rather than by omission.\n- **Runtime add/remove is out of scope.** Command mode (docs/DESIGN.md:694-745, dispatch at src/keys.rs:487) could plausibly grow a key, but there is no channel to reconfigure a *running* ssh: acs disables ssh's `~C` escape with `-e none` (src/ssh.rs:10) and disables the control socket (`ControlMaster=no`, `ControlPath=none`). Changing a forward would mean tearing the link down and redialling. Command mode also has no prompt or line editor, so there is nowhere to type `8080:localhost:80`. Note it as a non-goal.\n- **Does `-L` suppress the `-o LocalForward` path?** No reason to; `-o` stays the unvalidated escape hatch. But the Session-only gating means the two behave differently, which the docs should say out loud.\n\nDone when `acs -L 8080:localhost:80 box` forwards the port, the flag is repeatable, a bad spec is a clear acs error before any ssh is spawned, the forward is passed to the session ssh but **not** to the `Call::Side` and `Call::Batch` calls, it survives a reconnect, unit tests cover the argv for all three `Call` kinds plus spec parsing good and bad, and DESIGN §7.1 and the README document it along with the brief drop across a redial.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-20T16:06:05.091544Z","created_by":"michel","updated_at":"2026-09-23T18:57:44.760837Z","closed_at":"2026-09-23T18:57:44.760649Z","close_reason":"Implemented in PR #155 (27cbc1e): repeatable -L, validated before any dial, emitted only for Call::Session, survives a redial","source_repo":"test-single-scalar-local-networks","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/test-single-scalar-local-networks","compaction_level":0,"original_size":0}
{"id":"acs-6k0","title":"Resolve user@host through the host's configured entries, as that user","description":"Resolve `user@alias` through the alias too, using `user` as the login name.\n\nUser's ask: \"if a user supplies 'me@devbox' as a destination, also check if 'devbox' is an alias, if so follow the normal flow and set 'user' to 'me'\".\n\nToday (src/alias.rs `resolve`): any destination containing `@` skips alias resolution on purpose, and DESIGN §7.3 and README (\"`me@devbox`, or any name that is not an alias, is used as given\") document `user@<alias>` as the way to reach a host whose name is also an alias. This change reverses that.\n\nProposed behaviour:\n\n- Split the destination at the last `@`: if the host part is an alias, resolve it normally (entries in order, ping check, fallback) and connect as `<user>@<entry host>`. The given user overrides every entry's `user`.\n- A redial resolves it again with the same user override.\n- Names that are not aliases behave as before.\n- The escape hatch goes away, so there needs to be another way to reach a host whose name equals an alias: for example its FQDN, or a documented form. Pick one and document it.\n- `-v` shows that the login name came from the command line.\n\nDone when tests cover `me@alias` (user override on the first and the fallback entry), `me@not-an-alias` unchanged, and the redial; DESIGN §7.3 and README are updated.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T19:28:44.658438Z","created_by":"michel","updated_at":"2026-09-18T21:45:02.224182Z","closed_at":"2026-09-18T21:45:02.224108Z","close_reason":"Implemented with tests: user@alias resolves through the alias as that user, redial keeps it (PR #25)","source_repo":"beads-intake-list-bell","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-list-bell","compaction_level":0,"original_size":0}
{"id":"acs-6k2","title":"Install acs and start the session on one ssh connection","description":"A new release ships after every merge, so every host pays first contact once per release: the dial that gets ACS-NEED, two install calls, then a redial — four handshakes without the user's multiplexing. After ACS-NEED, the prelude could read the payload from stdin on the same connection, finish the install and exec the proxy, so the whole thing is one handshake.\nTouches ssh.rs prelude, install.rs, client.rs connect_and_serve, DESIGN §8/§8.1. The SHA-256 check before chmod +x must stay. The upload itself still dominates on slow links.\n","status":"open","priority":3,"issue_type":"feature","created_at":"2026-09-23T19:29:10.464209Z","created_by":"michel","updated_at":"2026-09-23T19:29:10.464209Z","source_repo":"beads-intake-latency","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-latency","compaction_level":0,"original_size":0}
{"id":"acs-6p8","title":"Find the early-redial window: the client redials with a 120s backoff configured","description":"Seen during the acs-trw pass (PR #176, 2579994), on a cold full-parallel\n`cargo test --workspace` — compiling plus 24 test binaries at once. Three tests\nfailed together:\n\n- `detach_works_while_the_link_is_down`\n- `exit_needs_the_link`\n- `a_session_ending_during_an_outage_leaves_no_status_behind`\n\nIn each, the client had entered a **redial** although `ACS_BACKOFF_MS` was\n**120000**, so the command keys typed while offline were dropped as \"typed\nduring a redial\". Every later run was green — on the changed and on the\nunchanged code alike — so this is not acs-trw's change.\n\n**Why this is filed as a bug rather than as flakiness.** Those three tests all\nsit behind acs-7wu (#174, 24b281a), which widened exactly this backoff to 120 s\nprecisely so that a loaded machine could not reach the end of the wait. If a\nredial happens anyway, the wait is being cut short by something other than\nelapsed time, and load alone does not explain it. The candidates, in rough\norder of suspicion:\n\n- **netwatch.** `offline()` watches for a network change and redials at once\n  when it sees one; a spurious or self-inflicted wake (an interface flapping,\n  or the test harness's own socket activity at 24-binary parallelism) would\n  produce exactly this.\n- **The backoff wait waking early** — a condvar or poll with a deadline that\n  returns spuriously and is treated as expiry rather than re-waited.\n- A takeover or session-end path re-entering `offline()` and restarting the\n  attempt counter.\n\nIf it is netwatch, it is also a **user-visible** bug and not merely a test\nproblem: a laptop whose interfaces flap would redial repeatedly during an\noutage, dropping every key typed offline — which is the thing DESIGN §5.2\npromises not to do.\n\nInteracts with acs-iyq (redial at once on the first attempt after a drop): that\nbead deliberately shortens the first wait, so whoever takes it must not absorb\nthis window into the intended behaviour and hide it.\n\nDone when the cause is identified and named, the wait cannot end before its\ndeadline except for a genuine network change, a regression test covers the\nspecific wake path found (driving it rather than waiting for load to produce\nit), and the three tests above pass repeatedly under cold full-parallel\n`cargo test --workspace`, which is how it was seen.","notes":"Diagnosis from the acs-iyq pass (PR #178, f565134), by reading the code rather\nthan reproducing. It eliminates two of the three candidates in the description:\n\n- **Candidate 2 (wait waking early) is NOT present.** offline() re-checks\n  `now >= deadline` at the top of its loop, so a spurious poll return is\n  re-waited, never treated as expiry.\n- **Candidate 3 (a restarted counter) is NOT present.** `backoff` is created\n  once outside run()'s loop and is only reset()/spent(); no takeover or\n  session-end path re-enters offline() with a fresh counter.\n- **That leaves candidate 1, netwatch** — and `Offline::NetworkChanged` is the\n  only path that can end the wait before its deadline.\n\nWhy it would present exactly as reported: the suite does not isolate the client\nfrom the developer's real network. `NetWatch::new()` opens a PF_ROUTE socket on\nmacOS unless ACS_NETWATCH_FIFO is set, and only `a_network_change_redials_at_once`\nsets it. `route_messages_matter` accepts RTM_NEWADDR/DELADDR/IFINFO, which a\nlaptop emits on Wi-Fi roams, VPN/utun churn and container interfaces. The FIRST\nsuch event in a wait is always taken (`last_early` starts None), so the client\nredials at once and keys typed at it are dropped as \"typed during a redial\" —\nsporadically, on changed and unchanged code alike.\n\nReady-made isolation knob: ACS_NETWATCH_FIFO pointing at a path that does not\nexist makes NetWatch::new() return None (the `.ok()?` on the open); a FIFO\nnobody writes gives the same isolation while keeping the code path live.\n\nIMPORTANT: that isolation was written into the harness during the acs-iyq pass\nand then deliberately REVERTED, because applying it wholesale would hide the\nsymptom this bead has to reproduce. Isolate the tests that are not about\nnetwatch, but keep a reproduction of the real wake.\n\nacs-iyq did not touch this window: `spent()` keeps a network change costing\nexactly one dial, as before. Its three affected tests now use\n`refuse_one_dial()` to enter the wait deterministically, but once in the wait\nthey are exposed to a netwatch wake exactly as they were.","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-23T23:19:34.245296Z","created_by":"michel","updated_at":"2026-09-24T01:55:50.747209Z","closed_at":"2026-09-24T01:55:50.746946Z","close_reason":"Fixed in PR #180 (f196120): NetWatch::changed() answered 'did the kernel say something' rather than 'did the network change'. A hint now counts only when netmatch::local_networks() differs from the previous hint. Reproduced deterministically and shown red before the fix","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-6w9","title":"The prelude's candidate check kills the system-wide install path and mistakes a directory for a binary","description":"Three defects in the prelude's candidate handling, found during acs-gov\n(PR #188, 595cdc7) while working on the same function. Grouped because one\npass over `acs_safe` and its caller fixes all three.\n\n**1. The system-wide install path is unusable by anyone but its owner (the\nserious one).** `acs_safe` requires `uid == $(id -u)`. DESIGN §8 describes\n`/usr/local/lib/acs/<version>/acs` as \"an optional system-wide location an\nadministrator can populate once for all users\" — but an administrator\npopulates it **as root**, so every non-root user's prelude refuses it and\nfalls through to a per-user install. The second candidate is therefore dead\nweight today: it can only ever be used by whoever owns it, which defeats the\nentire point of a shared location.\n\nThis is pre-existing, from acs-08m, and it is a documented feature that does\nnot work rather than a security hole. The fix needs a judgement: a root-owned\nfile that is not group- or other-writable is arguably **more** trustworthy\nthan one's own, not less — but \"owned by root or by me\" is a wider rule than\nacs-08m wrote, and its reasoning deserves re-reading before it is widened.\nWhatever is decided, DESIGN §8 and the check must agree; today they do not.\n\n**2. `[ -x \"$b\" ]` is true for a directory.** If a candidate path were a\ndirectory with mode 755, the prelude would `exec` it, `sh` would exit 126, and\n**no ACS-NEED marker would be printed** — so the client sees a silent failure\ninstead of a request to install. A `[ -f \"$b\" ]` alongside closes it. Cheap,\nand the failure mode is bad out of proportion to the cause.\n\n**3. The prelude clobbers unprefixed shell variables** — `u`, `b`, and now\n`p`, `n`, `t`, `w` after acs-gov. Harmless as plain shell variables, but if\nany of those names is *exported* in the ssh environment, the exec'd acs and\nthe user's remote shell inherit the changed value. `u` and `b` are\npre-existing; acs-gov kept the new names consistent rather than half-prefixing\nthem, deliberately, leaving the whole set to be renamed together.\n\nDone when the system-wide candidate is usable by a non-owner under a rule that\nacs-08m's threat model supports and DESIGN §8 states, a directory on a\ncandidate path yields ACS-NEED rather than a silent exit 126, the prelude's\nvariables cannot collide with an exported environment, and each has a test —\nincluding one that actually populates the system-wide path as root, which\nscripts/test_linux.sh is the place to do.","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-24T05:01:39.989152Z","created_by":"michel","updated_at":"2026-09-24T05:35:32.709745Z","closed_at":"2026-09-24T05:35:32.709522Z","close_reason":"Fixed in PR #190 (ae86191): an acceptable owner is the invoking uid or root, for the file and every directory on the path; the mode test is unchanged, so a root-owned file in a user-writable directory is still refused. [ -f ] added so a directory yields ACS-NEED; prelude variables are acs_-prefixed. DESIGN §8 and the check now agree","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-721","title":"Create temp files with O_EXCL and full randomness instead of the process id","description":"Security, symlink and predictable-name attacks. Several temp paths are predictable and opened in a way that follows symlinks: upgrade.rs uses .acs-upgrade.<getpid()> with File::create (O_TRUNC, follows links) and 32-bit randomness elsewhere, update_check.rs uses .update-check.<getpid()>, scripts/install.sh uses \"$2.new.$$\" with cp and ln -s, and xtask/src/bump.rs uses /tmp/acs-release-<pid>-<version>.\n\nThis matters because both the upgrade and the installer tell the user to re-run under sudo into /usr/local/bin and /usr/local/lib/acs, which on Intel Homebrew and hand-managed hosts are routinely group- or user-writable. Pre-creating the predictable name as a symlink turns the root-run create or cp into an arbitrary-file overwrite, then chmod 755.\n\nAlso worth narrowing while here: upgrade::link_candidates adds <dir>/acs for every directory on PATH and relink writes into any that currently points at the running exe.\n\nDone when every temp file is created O_EXCL with full-width randomness (never the pid), the installer uses install -m 755 rather than cp plus chmod, and a test or at least a comment records why.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-20T04:36:00.758547Z","created_by":"michel","updated_at":"2026-09-20T05:54:24.069476Z","closed_at":"2026-09-20T05:54:24.069379Z","close_reason":"Implemented in #123: random_token and create_new at every temp path","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-7k7","title":"Decode frames just read before judging liveness, so a stdout stall is not a dead link","description":"The client loop (src/client.rs:959-972) reads the link into dec and immediately calls liveness.tick(), but liveness.heard() is only updated in the decode loop at the top of the next iteration (client.rs:734), so heard reflects the previous iteration. Normally microseconds, but write_output -> sys::write_all(STDOUT) (client.rs:657-674) blocks for as long as the terminal is not reading (emulator stalled, flow control). After a stall over 10 s the next poll returns at once, the new frames are read, and tick sees now >= heard + dead_after, so lost(link): a needless ssh kill, redial, status line and Ctrl-L although PONGs were sitting in the pipe.\n\nDone when the client decodes after the read before ticking (or treats a readable link as heard), with a test that delays stdout consumption past the liveness deadline and asserts no redial.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-19T21:46:59.064321Z","created_by":"michel","updated_at":"2026-09-19T22:54:37.326542Z","closed_at":"2026-09-19T22:54:37.326464Z","close_reason":"Implemented in #87 (d99d1d4)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-7wu","title":"Finish the timing work in tests/reconnect.rs: two elapsed bounds and the stalled-terminal settle","description":"What acs-o8h (PR #164, f4f2522) deliberately left, having been scoped to the\nthree tests acs-o8h named. Both items were found by sweeping the whole file.\n\n**1. Two more `elapsed() < 5 s` assertions, each with only 4x discrimination.**\n\n- `detach_works_while_the_link_is_down` (~:217)\n- `a_network_change_redials_at_once` (~:408), which also has\n  `wait_connections(2, Duration::from_secs(5))`\n\nEach is distinguishing \"at once\" from \"after the 20 s backoff\", so the\nassertion is genuinely about elapsed time and should stay an elapsed\nassertion — but 5 s against 20 s is a narrow margin on a loaded machine. The\ncheap fix keeps the discrimination and widens the slack: raise `ACS_BACKOFF_MS`\nto 120000 and the bound to `T`, giving 6x the room for the same statement.\nacs-o8h used exactly this move in `the_escape_window_is_the_same_offline`.\n\n**2. `a_stalled_terminal_is_not_a_dead_link`'s settle.**\n\n`sleep(8 s)` with the terminal paused, then `sleep(500 ms)`, then\n`assert_eq!(remote.transport_pids().len(), 1)` and `!contains(\"reconnecting\")`.\n\nIts failure direction is the safe one — the 8 s is a *stimulus* that must\nexceed `ACS_DEAD_MS` (800 ms), which load only lengthens, and the 500 ms is a\nsettle before a **negative** assertion, so a stalled host makes it pass rather\nthan go red. So this is loss of sensitivity, not a random gate failure: a real\nregression could slip through, which is why it is worth doing and why it is\nnot urgent.\n\nacs-o8h looked for an event-based settle and did not find a clean one: the\nclient emits nothing at the 400 ms-800 ms scale while offline, and waiting for\npost-pause output matches the flood of buffered bytes drained on unpause rather\nthan newly produced ones. Doing it properly means giving the stimulus command a\ncounter so the test can wait for a specific later value — which perturbs a\ncarefully tuned acs-7k7 reproduction, and is the reason it was left alone.\n\nDone when the two elapsed bounds keep their discrimination with room to spare\nunder load, the stalled-terminal settle waits for something observable rather\nthan 500 ms, the acs-7k7 reproduction still reproduces (show it), and each\nstill goes red for the regression it covers.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-23T20:35:27.959159Z","created_by":"michel","updated_at":"2026-09-23T22:52:40.715151Z","closed_at":"2026-09-23T22:52:40.714957Z","close_reason":"Fixed in PR #174 (24b281a): the two elapsed bounds keep their discrimination with a 120s backoff and a T bound, and the stalled-terminal settle waits for a RESUMED marker produced after the unpause. The acs-7k7 reproduction survives — 5/5 red with liveness.heard() removed. Note the two elapsed asserts are now documentation rather than gates; the surrounding waits are what fire","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-7zb","title":"Offer n and x in the session menu only on a row they act on","description":"User's words: \"when the session list is presented. When the selected line is not a session but the 'exit' option, do not allow the 'n' key, nor display the option in the status bar. Same is true for the 'x' key.\"\n\nWhat the code does now (src/menu.rs). rows() builds [sessions..., New (single-host menu only), Exit]. With the cursor on Exit:\n\n- n (menu.rs:328) matches (false, _) in a single-host menu and returns Choice::New { host: 0 }, so it really does make a new session from the exit row. In the every-host menu the (true, _) arm only sets a note.\n- x (menu.rs:313) is already a no-op off a session row (`if let Row::Session(..)`), but silently: nothing tells the user why.\n- The key bar (menu.rs:447-450) is one fixed string, \"1-9, or up/down jk and Enter: attach   .: <all|detached only>   x: end   n: new<there>   Esc: leave\", drawn the same whatever the cursor is on.\n\nDone when the bar lists only the keys that act on the row under the cursor (no \"x: end\" or \"n: new\" on Exit), n on the exit row does nothing instead of creating a session, and tests/menu.rs covers both: the bar's text on the exit row, and n on the exit row leaving the menu unchanged.\n\nOpen question for whoever takes it: the New row has the same shape (x does nothing there, and \"Enter: attach\" is wrong on both New and Exit). Worth deciding whether the rule is \"hide keys that do not apply to this row\" in general, or just the exit row the user named.\n\nFollows acs-s0g, which built the menu.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-20T00:19:26.396639Z","created_by":"michel","updated_at":"2026-09-20T00:30:31.286738Z","closed_at":"2026-09-20T00:30:31.286632Z","close_reason":"Implemented in #104: the key bar is built per row and n/x act only where they apply","source_repo":"beads-intake-menu-and-local-nets","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-menu-and-local-nets","compaction_level":0,"original_size":0}
{"id":"acs-7zi","title":"List sessions on every host in the configuration","description":"Show the sessions on all configured hosts in one command.\n\nUser's ask: \"add a way to display all the sessions on all hosts that are in the configuration\".\n\nToday `acs <host> --list` (src/list.rs) lists one host. The configuration's `hosts:` aliases (DESIGN §7.2, §7.3; src/config.rs, src/alias.rs) name every host acs knows.\n\nProposed shape (settle during implementation):\n\n- `acs --list` with no host (or `acs list`, which would become a reserved first argument like `config` and `upgrade`) lists every alias.\n- Each alias is resolved the way a connection would be (ping check, fallback hosts), then asked via the same `_proxy --list` side call. Hosts are queried in parallel with a timeout, so one slow or down host does not hold up the rest.\n- Output: the existing table with a HOST (alias) column, or one block per alias. An unreachable host, or one without acs of our version, gets one line saying so rather than failing the whole command.\n- Exit status: 0 if every host answered, non-zero (for example 255) if any did not.\n\nDone when tests cover several aliases, including an unreachable one and one without acs installed, using the fake transport and ping; README and DESIGN document the command.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T19:06:25.080986Z","created_by":"michel","updated_at":"2026-09-18T21:45:01.911219Z","closed_at":"2026-09-18T21:45:01.911128Z","close_reason":"Implemented with tests: acs --list without a host lists every alias (PR #21, feat: list sessions on every configured host)","source_repo":"beads-intake-list-bell","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-list-bell","compaction_level":0,"original_size":0}
{"id":"acs-8ax","title":"Honour the command keys while a dial is in flight, not only during the offline wait","description":"The offline wait is where command keys are honoured. While a dial is actually\nin flight the client is in cooked mode for ssh's prompts and flushes stdin\n(acs-qty), so `Ctrl-] Ctrl-] d` during a dial does nothing — and a dial against\na dead network can take ten seconds.\n\nacs-iyq (PR #178, f565134) made this sharper rather than causing it. Before,\na drop bought you ~1 s of offline wait in which `d` detached; now the first\nredial goes at once, so the dead window starts the instant a drop is noticed.\nThat is the right trade for latency — a detach during a dial was never\nreliable — but it turns \"mostly works\" into \"never works\" for the first\nattempt, and DESIGN §5.2 now has to say so.\n\nThe user-visible shape: the link drops, you want out, you type the detach you\nhave typed a hundred times, and nothing happens until the dial fails.\n\nWhat makes this awkward, and why it is a feature rather than a bug: raw mode is\nsuspended precisely so ssh can prompt for a passphrase or a host key, and\nacs-qty deliberately discards typed-ahead input on resume so a password does\nnot end up echoed or delivered to the program. Honouring command keys mid-dial\nmeans reading stdin while ssh may also want it. Options worth weighing:\n\n- only honour them while ssh has not yet asked for anything (the common case:\n  key auth, no prompt), falling back to today's behaviour once it prompts;\n- keep the detector fed from a separate path that does not consume the bytes\n  ssh needs;\n- accept the drop of typed-ahead but scan it for the escape sequence first.\n\nWhichever way, the acs-qty guarantee must not regress: a passphrase typed at\nan ssh prompt must never reach the program or the screen.\n\nDone when Ctrl-] Ctrl-] d detaches during an in-flight dial in the common\nno-prompt case, acs-qty's regression test still passes unchanged, a test covers\na detach during a dial, and DESIGN §5.2 and §6.1 say when command keys are live.","status":"open","priority":3,"issue_type":"feature","created_at":"2026-09-24T01:14:51.972254Z","created_by":"michel","updated_at":"2026-09-24T01:14:51.972254Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-8kv","title":"Cover -L in the ssh e2e suite, which the Alpine image currently forbids","description":"`-L` landed in acs-6f5 (PR #155, 27cbc1e) with unit tests for the argv of all\nthree `Call` kinds and for spec parsing, plus a **by-hand** demonstration\nagainst the e2e container: a real forward bound, the container's ssh banner\nread back through it, no forward on the `Call::Side` call, and the forward\nrebound after the session's ssh was killed. That hand check is recorded as a\nrow in docs/VERIFICATION.md and is the only end-to-end evidence there is.\n\nIt is by hand because `scripts/e2e/Dockerfile` inherits Alpine's sshd default\n`AllowTcpForwarding no`, so `scripts/e2e_ssh.sh` cannot exercise a forward at\nall. The hand check needed the setting flipped inside the running container.\n\nThe gating logic is exactly the part worth having a permanent test for: the\n`call == Call::Session` condition in `ssh::argv` is what stops `acs list` and\nthe remote-install side calls from each trying to bind the same local port.\nA unit test asserts the argv; only an e2e test asserts that a real ssh does\nwhat the argv implies.\n\nDone when the e2e image enables `AllowTcpForwarding` (sshd_config in\nscripts/e2e/Dockerfile), `scripts/e2e_ssh.sh` gains a case that forwards a port\nthrough an acs session and reads real bytes back through it, asserts the same\nport is *not* bound by an `acs list` side call, and docs/VERIFICATION.md's `-L`\nrow moves from a by-hand result to an automated one.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-23T18:57:55.632876Z","created_by":"michel","updated_at":"2026-09-23T20:03:38.638018Z","closed_at":"2026-09-23T20:03:38.637866Z","close_reason":"Implemented in PR #162 (caa3854): e2e_14 forwards a port, reads the container's real ssh banner through it, asserts the acs list side call never binds it, and rebinds after a redial; proven red two ways","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-95w","title":"Reject a non-https ACS_RELEASES_URL and ignore it under elevated privileges","description":"Security, update integrity. release.rs takes the release base from ACS_RELEASES_URL with no scheme validation, so http:// and file:// are accepted (scripts/test_install.sh uses file:// deliberately). It is read by `acs upgrade`, by the detached `acs _update-check`, and by scripts/install.sh. Because the sums come from that same base, integrity checking is bypassed entirely, and the download is then executed.\n\nAttack: a local user who can influence the environment of a privileged run (sudo -E, a sudoers env_keep, a poisoned shell rc, a CI step) points it at their own server and gets code execution as the upgrading user. The docs themselves suggest `sudo acs upgrade`. It also downgrades a mirror to cleartext for a network attacker.\n\nDone when a non-https base is refused unless an explicit opt-out flag is passed, the variable is ignored when the effective uid differs from the real uid, the test suite's file:// use goes through the opt-out, and a test covers each case.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-20T04:36:00.147391Z","created_by":"michel","updated_at":"2026-09-20T05:26:04.069325Z","closed_at":"2026-09-20T05:26:04.069226Z","close_reason":"Implemented in #115: https required unless flagged, and ignored under a privilege mismatch","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-9jv","title":"Two small traps in the test harness: a private master-log path and a cached transport script","description":"Two minor hygiene items in `tests/common/mod.rs`, found while fixing acs-fzx\n(PR #166, 80553cb). Neither is broken today; both are the shape of thing that\nis cheap now and annoying later.\n\n**1. `master_log_file()` is private, so tests cannot assert the exact path.**\n`harness::remote_env_helpers_compose_in_any_order` can only assert that\n`ACS_MASTER_LOG` *ends with* `master.log`, because the path it should equal is\nnot reachable from a test. A weak assertion like that passes while a path bug\nsits under it.\n\n**2. `Remote::transport()` caches the script on first call and never rewrites\nit.** Safe as things stand, because every knob the script reads lives in a\nseparate file that the script sources at run time — which is exactly why\nnobody will notice when it stops being safe. The first setting that needs to\nchange the script *body* will silently no-op whenever the script was already\nmaterialised, and the symptom will be a test that configures something and\nsees no effect, with nothing anywhere saying why.\n\nDone when the master-log path is assertable from a test and the exact path is\nasserted, and `transport()` either rewrites when its inputs change or fails\nloudly (a debug assert and a comment naming the constraint is enough) if it is\ncalled after the script exists with different inputs.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-23T20:42:53.065714Z","created_by":"michel","updated_at":"2026-09-23T20:42:53.065714Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-9n3","title":"Reuse an acs-owned ssh master for first connections and side calls","description":"A full ssh handshake (TCP, kex, auth, channel open: ~6-8 RTTs, plus a hardware-key touch) is paid on every acs <host>, detach-then-reattach included. §3 sets ControlMaster=no because a dead multiplexer hangs a reconnect; that hazard is the redial's, not a fresh start's.\nProposal: acs uses its own ControlPath + ControlPersist (not the user's) for the first connection and side calls only; a redial never multiplexes and runs ssh -O exit on the stale master first; if no marker arrives through the master within ~2 s, fall back to a direct dial. Revisits DESIGN §3 and needs a decision entry in §12. Wait for the per-phase timings to show the handshake is worth it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-23T19:29:00.311398Z","created_by":"michel","updated_at":"2026-09-24T03:38:35.204085Z","closed_at":"2026-09-24T03:38:35.203909Z","close_reason":"Implemented in PR #184 (cc1ab77): acs keeps its own master under a per-uid 0700 /tmp/acs-mux-<uid>, joined by first connections and side calls, never by redials or batch. Measured 43-52ms to 12-19ms to ACS-READY on loopback. Multi-user isolation tested as root","source_repo":"beads-intake-latency","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-latency","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"acs-9n3","depends_on_id":"acs-pgn","type":"blocks","created_at":"2026-09-23T19:29:23.108314Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-9yv","title":"local_networks tests the wrong address: replace it with a caller-side per-host setting","description":"User's words: \"the current implementation does not do what it's intended for.. File a bead to both remove current implementation and implement the correct one\". On why a host entry cannot carry the setting: \"The idea is that by specifying the local_networks on a host, the host will get preferred if the calling host is in any of the local_networks\".\n\n`local_networks` (acs-c9d, shipped in v0.11.0) tests the wrong address. It is a **target-side** filter when it needed to be a **caller-side** gate, and the difference is not cosmetic: as built, a configured network ranks a host first *wherever you are*.\n\n## The defect\n\n`prefer_local_network` on its own is correct and should stay. Its list comes from `getifaddrs`, so it is **self-gating**: the networks in it change as the machine moves, and \"the host's address is on a network I am directly attached to\" is a true statement about locality however you test it.\n\n`local_networks` appends static entries to that same list (src/alias.rs:50):\n\n```rust\nn.local.extend(config.local_networks_for(alias).value.iter().copied());\n```\n\nand the match tests the **host's** resolved addresses against it (src/alias.rs:207, `addrs` from `resolve(&host, …)`):\n\n```rust\non_net[i] = netmatch::matching(&network.local, &addrs);\n```\n\nSo appending a static network destroys exactly the gating that made the interface-derived list meaningful. With `local_networks: [172.16.0.0/16]`, a host at 172.16.8.2 is ranked first from a coffee shop on 10.0.0.0/8 just as it is from the office, because the only address consulted is the host's. The caller's own address is never tested — the machine's interfaces merely *contribute entries to the list*.\n\nThe damage is bounded but real: the mis-ranked host still has to answer its ping, so you pay a `reachability_timeout` (500 ms default) and fall through to the right host. The ordering is simply wrong and the ping gate hides it.\n\n## Why it got through review\n\nacs-c9d's worked example — O at 172.16.1.65, T at 172.16.8.2, `local_networks: [172.16.0.0/16]` — gives the **same answer under both readings**: under the built rule T's address is in the /16, under the intended one O's address is. The one example in the bead, the README (README.md:345-353), DESIGN §7.3 and the tests could not tell the two apart. acs-c9d even flagged the ambiguity in as many words — \"the user's phrasing, 'local for this host', reads as per alias (and maybe per host entry), while the example is about what O believes\" — and then resolved it the other way.\n\n## Remove\n\nNot in production, so a clean break: no deprecated alias, no compatibility branch, no migration path. An existing config with a global or per-alias `local_networks:` should become a plain unknown-key error.\n\n- src/config.rs: the `Config.local_networks` field (:129), `Alias.local_networks` (:99), `local_networks` from `KEYS` (:166) and `ALIAS_KEYS` (:199), the `NETWORKS` const (:211), `local_networks_for` (:510), the `networks_mut`/`networks_setting` arms (:420, :427) and the alias merge (:753).\n- src/config.rs parsing: `networks_value` (:627), `parse_networks` (:649), `format_networks` (:659) — or keep them, since the new setting parses the same CIDR list; decide rather than delete blindly.\n- src/config_cmd.rs: the `NETWORKS` branch in `typed` (:93), `networks_node` (:76), the `Seq` join in `value_text` (:108), and the alias-settings help text (:42, :47).\n- src/alias.rs:50 — the `extend` that is the defect itself.\n- Tests: the acs-c9d round-trip test `local_networks_read_as_a_list_globally_and_per_alias` (src/config.rs:1419), `a_configured_network_counts_as_local_in_either_family` (src/alias.rs:833), `networks_alone_do_nothing_without_prefer_local_network` (src/alias.rs:894), and the CLI test in tests/config_cmd.rs:162. Note that the single-scalar cases added in #137 go with them — that PR tested the parsing, which may survive, not the semantics, which do not.\n- Docs: README.md:319-320 and :345-353, docs/DESIGN.md:906, :935, :1011-1032, :1180, :1184.\n\n## Implement\n\n`local_networks` becomes a **host-entry** setting (`HOST_KEYS`, src/config.rs:180, today `host, user, reachability_check, identity_file, persist, prefer`): a list of CIDR networks, IPv4 and IPv6, meaning *prefer this entry when the calling machine is on one of these*.\n\nThe test inverts. Instead of `matching(nets, host_addrs)`, it is the client's own `getifaddrs` addresses against the entry's list. `netmatch::LocalNet::contains` already masks both sides and handles both families (netmatch.rs:28-40), so the arithmetic is there; what changes is which value is passed as the address and which as the network. A caller-side hit needs no name resolution at all, which also makes it cheaper than the current path.\n\nRanking (`alias::rank`, src/alias.rs:215-224) keeps its shape — local first, then `prefer`, then configured order — with \"local\" now meaning *either* the interface-derived match *or* a caller-side `local_networks` hit.\n\n## Scope decisions to make when taking this\n\n- **Does the new setting require `prefer_local_network: true`?** Today `local_networks` is inert without it (`.then(network)` short-circuits, src/alias.rs:48). An explicit per-host statement arguably should stand on its own; the counter-argument is one switch governing all locality preference. Decide and document.\n- **Is there still a global or per-alias form?** Probably not: \"prefer *which* host when I am in network X\" does not parse without naming the host, which is why per-entry is the right home. Say so explicitly rather than leaving the absence unexplained.\n- **Several entries match.** Configured order, or the longest prefix? `rank` is a stable sort, so configured order is the no-work answer.\n- **`-v` wording.** The existing line is \"<alias>: <host> is on the local network <net>\" (src/alias.rs:133), which describes the old direction. The caller-side one reads more like \"this machine is on <net>, so <host> is preferred\".\n\n## Done when\n\nA host entry accepts `local_networks`, that entry is ranked first exactly when one of **this machine's own** addresses falls in one of its networks, a global or per-alias `local_networks` is an unknown-key error, `prefer_local_network`'s interface-derived behaviour is unchanged, a bad CIDR is still a clear config error, and tests cover IPv4 and IPv6 plus the case the old design got wrong — **a host inside the configured network that is NOT preferred because the caller is somewhere else**, which is the regression test for this bead. README and DESIGN §7.3 describe the caller-side direction in a sentence that could not also describe the target-side one.\n\nSupersedes acs-c9d; extends acs-sia.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-22T21:32:34.331808Z","created_by":"michel","updated_at":"2026-09-22T22:20:35.560024Z","closed_at":"2026-09-22T22:20:35.559801Z","close_reason":"Fixed in #148: local_networks is now a caller-side per-host setting","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-ad5","title":"Compare pre-release identifiers numerically in release::compare","description":"release::compare (src/release.rs:84-93) uses (Some(x), Some(y)) => x.cmp(y), so 0.3.0-rc10 < 0.3.0-rc2 (\"rc10\".cmp(\"rc2\") is Less). Semver compares dot-separated numeric identifiers numerically. Impact is limited because find_asset refuses any archive whose version has a \"-\", so releases are never pre-releases; only decide()/should_link() for locally built -dev/-rc versions are affected. Related: archive_target(\"armv7-unknown-linux-musleabihf\") (release.rs:27-32) yields the non-existent armv7-unknown-linux-musl; harmless while no armv7 archive is published.\n\nDone when identifiers are split on \".\" and numeric ones compared as numbers, with a unit test for rc2 < rc10.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-19T21:46:59.127616Z","created_by":"michel","updated_at":"2026-09-19T22:59:06.765537Z","closed_at":"2026-09-19T22:59:06.765420Z","close_reason":"Implemented in #89 (a3f0478)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-aps","title":"Scrub the inherited ssh and agent environment from the session shell","description":"Security, credential reuse inside a shared account. master.rs start_child removes only ACS_SOCKET_DIR and ACS_MASTER_LOG, so the session shell inherits the creating ssh login's SSH_AUTH_SOCK, SSH_CONNECTION, SSH_CLIENT, KRB5CCNAME, DISPLAY, XDG_SESSION_ID and DBUS_SESSION_BUS_ADDRESS, keeps them for the life of the session, and never refreshes them for later attachers.\n\nSo alice creates a session with agent forwarding, bob attaches on the same account, and bob's shell uses alice's forwarded agent for onward ssh as long as alice's connection lives. Everyone else's shell meanwhile reports alice's laptop in SSH_CONNECTION and DISPLAY. No privilege boundary is crossed, since the same uid could read /proc anyway, but acs hands it over invisibly and permanently.\n\nDone when the forwarding and session variables are scrubbed from the child environment, or re-exported per attach from the attaching connection, whichever DESIGN §4.5 prefers, with the choice documented and a test.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-20T04:36:01.485363Z","created_by":"michel","updated_at":"2026-09-20T06:58:07.943332Z","closed_at":"2026-09-20T06:58:07.943227Z","close_reason":"Implemented in #130: the session shell inherits no ssh or agent environment","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-b4f","title":"Split EOF on the link by ssh's exit status, the last place a sibling is dropped for nothing","description":"acs-n1m (PR #197, bec3939) made the shared master survive a `LinkEnd::Channel`\nloss — a decode error, where bytes demonstrably arrived a moment earlier. Every\nother reason still ends the master, deliberately: silence is ambiguous, and a\nwedged master stranding a user is worse than a dropped sibling.\n\n`LinkEnd::Transport` — EOF on the link — is where most remaining collateral\ncomes from, and it is the one blunt case that could be sharpened with evidence\nrather than guesswork. `Link` holds the `Child`, and OpenSSH has an exit-status\ncontract: **255 means ssh's own failure** (the connection), anything else is\nthe remote command's status (our channel). A bounded reap on EOF would let acs\ntell those apart and keep the master in the second case.\n\nTwo reasons this is P4 rather than urgent, both from that pass:\n\n- **It would almost never fire.** A clean channel end arrives as a\n  `Msg::Exit`/`Msg::Error` frame — an `Outcome::Exit`, which never reaches the\n  `-O exit` at all — long before EOF. The EOF path is mostly reached when\n  something really did die.\n- **It needs a decision this bead has to make**: what an *unreaped* child means.\n  A bounded `try_wait` loop can time out, and \"ssh has not exited yet\" is a\n  third answer the current two-way rule has no room for. Defaulting it to\n  \"end the master\" keeps acs-n1m's bias; say so explicitly rather than letting\n  the timeout decide by accident.\n\nDo not let the reap block the redial. The whole point of acs-iyq (#178) is that\nthe first attempt after a drop goes at once; a reap that waits on a wedged ssh\nwould give that back.\n\nDone when EOF with a non-255 ssh exit keeps the master, EOF with 255 or an\nunreaped child ends it, the reap cannot delay the redial measurably, a test\ncovers all three, and acs-n1m's `a_channel_that_breaks_leaves_the_master_and_the_sibling_alone`\nplus acs-9n3's `a_master_that_does_not_answer_is_ended_and_the_dial_made_again`\nboth still pass.","status":"open","priority":4,"issue_type":"feature","created_at":"2026-09-24T08:34:57.463694Z","created_by":"michel","updated_at":"2026-09-24T08:34:57.463694Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-br2","title":"a_network_change_ends_a_frozen_link_without_the_dead_timeout fails on cold full-parallel Linux, cause unknown","description":"Seen **twice**, both times on a **cold full-parallel Linux** run, both with the\nsame message:\n\n    1 connections, waiting for 1 more than the 1 there were\n\n- First during acs-ryz (PRs #194/#195), which then could not reproduce it in 40\n  isolated plus 24 full-binary runs and said so honestly rather than claiming a\n  fix.\n- Again during acs-0n8 (PR #199, 491c637), on the first `scripts/test_linux.sh`\n  run. Follow-ups were 3/3 green alone in the container, 5/5 green for the whole\n  parallel `reconnect` suite there, and a clean full `test_linux.sh`.\n\n**The leading theory is now eliminated.** acs-ryz suspected a swallowed network\nhint; acs-0n8 fixed exactly that and the failure still happened. The path this\ntest exercises is a *welcomed* link, where acs-0n8 is semantically identical\n(netcheck, then `acted()`), so that change can only have made this shape less\nlikely, never more.\n\nWhat the test does: `SIGSTOP`s the transport so nothing flows and nothing\ncloses, with `ACS_DEAD_MS=60000` so only the netcheck can end the link, then\nexpects one more connection. The failure is that the second connection never\narrives inside `T`.\n\nRemaining candidates, from the acs-0n8 pass:\n\n- **The netcheck PING's write to a SIGSTOPped transport.** A stopped process\n  does not drain its pipe; if the write blocks or is swallowed, the deadline\n  that should follow it may never be armed. This is the most specific theory\n  and the one to test first.\n- **Plain starvation in the Docker VM** on a cold run, where the compile and\n  30-odd test binaries land at once. If so it belongs with acs-2dc rather than\n  here.\n\nNote the environment matters: both sightings were Linux-in-a-VM, cold, fully\nparallel. It has not been seen on macOS. Reproduce it there — `test_linux.sh`\nafter a `docker volume rm` of the target volume is the closest thing to the\nconditions — before theorising further.\n\nDone when the cause is named rather than suspected, the test either cannot lose\nthat race or the product bug behind it is fixed with its own regression test,\nand a cold full-parallel `scripts/test_linux.sh` passes repeatedly — say how\nmany times.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-24T17:33:58.952516Z","created_by":"michel","updated_at":"2026-09-25T20:56:19.898988Z","closed_at":"2026-09-25T20:56:19.898757Z","close_reason":"Fixed in PR #208 (28ee038): a real product bug. Liveness::tick_at cleared the netcheck probe on any byte arriving after the PING, but frames already in flight were written before the network moved. Only the PONG for that PING answers now. Reproduced deterministically 9/9 red, 0/8 after — not environmental, so not acs-hsw","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-c9d","title":"Let prefer_local_network take explicit networks counted as local","description":"User's words: \"as an extension to 'prefer_local_network' add the ability to specify one or more networks, ipv4 or ipv6 that are considered local for this host. As an example, the target T is at 172.16.8.2 and the origin O is on 172.16.1.65, it would not select T. However after specifying that 172.16.0.0/16 is local to T, O would match and use this host.\"\n\nToday (acs-sia, DESIGN §7.3) the only networks that count are the ones this machine's own interfaces are on: netmatch::local_networks() reads getifaddrs and builds a LocalNet { addr, prefix } per address, alias::resolve filters to `!n.local.is_empty()`, and netmatch::matching() ranks a host first when one of its resolved A/AAAA addresses falls in one of those. So O on 172.16.1.65/24 never matches T at 172.16.8.2: the interface prefix is too narrow, although both are on the same site.\n\nWanted: a configured list of CIDR networks, IPv4 and IPv6, added to the machine's own for that match. With 172.16.0.0/16 configured, O ranks T as local and uses it.\n\nNotes for whoever takes it:\n\n- netmatch::LocalNet and contains() already do the arithmetic for both families, and usable() rejects a zero prefix, so the matching side needs little more than parsing \"addr/prefix\" into a LocalNet.\n- The config side is the new part: every setting today is a scalar (bool or duration; see config.rs ALIAS_BOOLS and the tables config_cmd.rs uses for `acs config set`), so this is the first list-valued one. It needs a type in Setting, a parser with a real error for a bad CIDR, printing in `acs config show`, and a decision on `acs config set` syntax for a list.\n- Decide the scope and say so in the doc: the user's phrasing, \"local for this host\", reads as per alias (and maybe per host entry), while the example is about what O believes. Global plus per-alias matches how prefer_local_network already resolves (config.rs:464), and per-entry is the third option.\n\nDone when the networks can be configured, they are merged with the interface-derived ones for the ranking, a bad CIDR is a clear config error, netmatch and alias tests cover an IPv4 and an IPv6 case that only match through a configured network, and README plus DESIGN §7.3 document it.\n\nExtends acs-sia.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-20T00:19:26.470722Z","created_by":"michel","updated_at":"2026-09-20T00:49:57.080092Z","closed_at":"2026-09-20T00:49:57.079953Z","close_reason":"Implemented in #106 (v0.11.0): the local_networks setting, global and per alias","source_repo":"beads-intake-menu-and-local-nets","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-menu-and-local-nets","compaction_level":0,"original_size":0}
{"id":"acs-ciz","title":"Quote the version in ssh::prelude and remote_candidates","description":"Security, latent command injection. ssh.rs remote_candidates interpolates the version into \"$HOME/.local/share/acs/{version}/acs\" with no quoting, and prelude puts that inside double quotes, where $, backtick and backslash are still live. Its sibling args all go through sh_quote, and install.rs does sh_quote the version, so this is an inconsistency rather than a deliberate exception. Verified by reading both.\n\nNot exploitable today: every caller passes the compile-time crate::VERSION. But both functions are pub, take &str, and document no constraint, so one future caller passing a configured or remote-supplied version makes it remote code execution through both shell layers.\n\nDone when the version is quoted the same way the other arguments are (or the function takes a validated type), with a test using a hostile version string.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-20T04:36:00.902049Z","created_by":"michel","updated_at":"2026-09-20T06:07:32.821663Z","closed_at":"2026-09-20T06:07:32.821570Z","close_reason":"Implemented in #125: the version is quoted, and the prelude no longer re-wraps it","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-cu8","title":"scripts/test_linux.sh is red on main for three unrelated reasons","description":"`scripts/test_linux.sh` — the suite run on Linux in a container, one of the\nfive checks CLAUDE.md lists — currently fails on `main`. Found during the\nacs-6p8 pass (PR #180, f196120), which verified all three against a pristine\n`origin/main` exported with `git archive` into a temp dir and run in the same\ncontainer: **all three reproduce without that change**.\n\nThree independent causes:\n\n1. **`signature::tests::*` (6 tests) — `ssh-keygen: No such file or directory`.**\n   The `rust:alpine` container installs only `musl-dev`, and the signature\n   tests shell out to `ssh-keygen -Y`. Either `apk add openssh-keygen` in the\n   script, or skip those tests when `ssh-keygen` is absent — but skipping\n   silently would hide a real failure, so prefer installing it.\n2. **`a_stalled_terminal_is_not_a_dead_link` hard-codes\n   `transport_pids().len() == 1`.** In the container the remote has to install\n   acs first, which spends two extra transport calls, so it is 3. The count\n   should be taken from a baseline captured at the start of the test, not\n   from the literal 1. The acs-6p8 pass hit exactly this trap in its own new\n   test and fixed it that way, so the pattern to copy is in that commit.\n3. **`frozen_link_is_declared_dead_and_replaced` — \"declared dead too early\".**\n   A wall-clock floor (`>= 500 ms`) the container misses. This is the class\n   acs-o6x (#158), acs-o8h (#164) and acs-7wu (#174) have been retiring from\n   `tests/reconnect.rs`; it wants the same treatment.\n\nWhy P2 rather than P3: a gate that is red for reasons nobody is tracking stops\nbeing read, and this one is the only thing in the repo that runs the suite on\nLinux and as root for the multi-user isolation check. Everything else runs on\nmacOS. The longer it stays red, the more likely a genuine Linux-only or\npermissions regression lands unnoticed behind the three known failures.\n\nDone when `scripts/test_linux.sh` passes on `main`, each of the three causes is\nfixed rather than skipped or suppressed (or, where skipping is right, the skip\nis loud and conditional on the missing binary), and the two reconnect tests\nstill go red for the regressions they cover — show it.","notes":"Likely root cause of most of the container failures, found during acs-9n3\n(PR #184, cc1ab77), which ran the whole Linux suite with --no-fail-fast on its\nbranch AND on a pristine origin/main export in the same container. The same\nset fails on both; main actually failed slightly more. Beyond the three causes\nin the description, the failing set includes `proxy`, `host_aliases`, `list`,\n`menu`, `timing`, `update_check` and `upgrade`.\n\n**`Remote::installed()` symlinks the host binary into the fake remote's HOME,\nand in `rust:alpine` the prelude's `acs_safe` check rejects it** — so every\nharness test in the container pays an extra first-contact install dial it does\nnot pay on macOS. That single difference plausibly explains `list`, `menu`,\n`host_aliases` and `proxy` together, and it is the same trap that made\n`a_stalled_terminal_is_not_a_dead_link` wrong (cause 2 here): assertions that\nhard-code an absolute dial or transport count are wrong on any remote that\nmust install acs first.\n\nTwo consequences for whoever takes this:\n\n- Fix `installed()` for the container and a batch of these may go green at\n  once. Try that before fixing them one by one.\n- Where a count is genuinely being asserted, count from a baseline captured at\n  the start of the test rather than from a literal. acs-9n3 did this in\n  tests/mux.rs — it made its assertions property-based for exactly this reason\n  and they pass 6/6 in the container.","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-24T01:56:05.327670Z","created_by":"michel","updated_at":"2026-09-24T04:12:58.686456Z","closed_at":"2026-09-24T04:12:58.686260Z","close_reason":"Fixed in PR #186 (a5d3274): 36 failures to 0. Root cause was Remote::installed() symlinking the binary, which the prelude's acs_safe refuses on Linux; the rest was a missing ssh-keygen in the image. src/ untouched","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-d1v","title":"Ignore frames from a taken-over or detached (Closing) connection","description":"`handle()` (src/master.rs ~L553-591) has no check for `ConnState::Closing`. A Closing conn is not polled for POLLIN, but POLLHUP is always reported, so when the old client's proxy closes, `read_conn` decodes whatever was still buffered and applies it as for the active client: INPUT goes to the pty and advances `InputDedupe::written`, so the new client's first keystrokes at the same sequence are dropped as duplicates (DESIGN §5.1 says clients taking turns never collide); RESIZE resizes the new client's pty; KILL kills the session. Trigger: the old client types or resizes between the takeover and its exit on TAKEOVER, or its proxy holds unsent input when the link died.\n\nDone when Closing conns are drained without handling (or skipped in read_conn), with a test that a late INPUT/RESIZE/KILL from a taken-over conn is ignored and the new client's input_seq stays consistent.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-18T15:14:21.518918Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.360336Z","closed_at":"2026-09-18T15:39:58.360259Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-d7x","title":"The release key is validated at build time and its twin the URL is not","description":"An asymmetry noted during acs-okz (PR #212, 9ce69b0), which made the key's\nbuild-time validation testable and in doing so made the gap visible.\n\n**`ACS_DEFAULT_RELEASE_KEY` is validated** — one line, a known key type, a\nbase64 body of at least 16 characters — and a bad value **fails the build**.\n\n**`ACS_DEFAULT_RELEASES_URL` is not.** It is trimmed and has a trailing `/`\nstripped, and otherwise taken as given. That is deliberate (\"taken as given\",\nper acs-5em), and for a URL there is less to check. But the consequence is that\na fork which sets both and **mistypes the URL** gets a silently unreachable\nupdate channel rather than a build error — the binary builds, ships, and only\nfails at a user's `acs upgrade`, which is the worst place to find out.\n\nA cheap check would catch the realistic mistakes: a scheme that is not https\n(the runtime override already refuses non-https, so a build-time value that is\nhttp is almost certainly a slip), an obviously unparseable value, or an empty\nhost. Not a validator for URLs in general — just enough that a typo is a build\nerror like the key's is.\n\n**Second, smaller item on the same check:** the key's body test is shape-only.\n`ssh-ed25519 AAAAAAAAAAAAAAAA` passes, and so does `ssh-rsa <an ed25519 body>` —\nnothing base64-decodes the blob or checks its embedded type string against the\ndeclared one. That fails closed (every release is then refused) and is the\n\"wrong-but-well-formed\" case DESIGN already accepts, so it is not a hole. But a\ndecode-and-compare would catch a truncated or mismatched paste at build time\nrather than at the user's upgrade — the same argument as the URL above.\n\nNote acs-x57 may change what these values feed: if the build's URL is\nsubstituted into the packaged `scripts/install.sh`, an unvalidated value starts\nreaching a shell script, which raises the stakes. Check what that bead landed\nbefore deciding how much validation is enough.\n\nDone when a mistyped releases URL fails the build the way a mistyped key does,\nwith the same shape of message, and the decision about how far to decode the\nkey body is recorded in DESIGN §9 either way.","notes":"**The stakes are NOT raised by acs-x57 (PR #214, 1d00169)** — resolving the\nquestion this bead flagged as open.\n\nThat pass does substitute the build's releases URL into the packaged\n`scripts/install.sh`, but it emits both the URL and the key as **single-quoted\nPOSIX shell words**, with `'` escaped as `'\\''`. `sh` expands nothing inside\nsingle quotes — no `$`, no backtick, no backslash, and a newline is literal — so\nthe value becomes exactly that string and can never become a command. It is\ntested: `a_build_time_value_cannot_inject_shell` drives `'; touch <canary>;\necho '`, `$(touch …)`, backticks, `\"; rm -rf /; \"` and a pile of metacharacters\nthrough the real script, runs the resulting assignments under `sh`, and asserts\neach round-trips as text with the canary never created.\n\nOne thing that pass **does** refuse in the value: an embedded line break.\nHarmless inside quotes, but it would split the assignment across two lines and\nleave nothing able to read the value back for the drift guard.\n\nSo this bead is purely the **ergonomics** question it started as: should a\nmistyped releases URL fail the build the way a mistyped key does, instead of\nproducing a silently unreachable update channel that is only discovered at a\nuser's `acs upgrade`? Nothing about safety depends on it.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T22:49:47.309205Z","created_by":"michel","updated_at":"2026-09-25T23:22:52.621273Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-e80","title":"Let a held command key survive a redial attempt, instead of a fresh Detector forgetting it","description":"`reconnect::offline()` constructs a fresh `keys::Detector` on every entry, so a\ncommand-key press held across a backoff expiry is silently forgotten: the first\nCtrl-] of a double tap is dropped and the user's escape does not arm.\n\nWith the default 400 ms escape window this is invisible — a backoff expiry is\nvery unlikely to fall inside it. It becomes reachable with a long configured\n`ACS_ESCAPE_TIMEOUT_MS` and a short backoff, which is not an exotic combination\nfor someone who has deliberately widened the window because they find 400 ms\ntoo quick.\n\nFound while doing acs-o8h (PR #164, f4f2522): it is why that PR had to raise\n`ACS_BACKOFF_MS` from 20 s to 120 s in `the_escape_window_is_the_same_offline`.\nThe test configures a 30 s escape window, and without the longer backoff the\nredial attempt expires inside it and eats the held press. So the workaround is\nalready in the suite, which is a good sign the behaviour is real and a bad sign\nthat it is only recorded as a magic constant.\n\nThe question to answer is whether the detector should outlive one redial\nattempt — carry it across `offline()` entries rather than rebuilding it — and\nwhat that means for a press held across a *successful* reconnect, where the\nclient transitions out of the offline path entirely. DESIGN §6.1 describes the\ncommand-mode state machine and is where the answer belongs.\n\nDone when a command key held across a backoff expiry still arms command mode, a\ntest asserts it with a backoff shorter than the escape window (the inverse of\nthe constant acs-o8h had to set), and DESIGN §6.1 says what the detector's\nlifetime is.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-23T20:35:13.571033Z","created_by":"michel","updated_at":"2026-09-23T21:58:46.712753Z","closed_at":"2026-09-23T21:58:46.712551Z","close_reason":"Fixed in PR #168 (04e4e22): the Detector moves to client::State and lives as long as the client, so a held press survives a redial attempt and a reconnect; the escape window is the only thing that ends a gesture. acs-o8h's ACS_BACKOFF_MS workaround reverted to 20s","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-er6","title":"Drain queued output to the client before failing on a master write error","description":"In `relay()` (src/proxy.rs ~L266) `flush(m, &mut to_master)?` propagates EPIPE when the master closed between this iteration's `read(m)` — which just queued the EXIT frame into `to_client` — and the write. `session()` then errors out without `drain(STDOUT, &to_client)`, so the client sees \"link lost\" and later NO_SESSION instead of the child's exit status. Needs queued client input (a PING or INPUT) in the same iteration, so the window is narrow.\n\nDone when a write error to the master falls through to the drain path instead of `?`, with a test for EOF-with-pending-output ordering.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-18T15:14:21.764668Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.368456Z","closed_at":"2026-09-18T15:39:58.368384Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-evm","title":"Do not ACK input the master has not written to the pty","description":"The master ACKs INPUT when it enters pty_in (src/master.rs:622-630), not when it is written; write_pty (master.rs:860-878) clears pty_in on any error or when !pty_open, so acked input can be silently lost. The client then never resends it on resume because it believes the master has it. Only observable on a pty write error, so rare, but it breaks the §5.2 guarantee that an ACK means written.\n\nDone when the ACK is sent after the write succeeds (or the sequence is only advanced by written bytes), with a test that fails the pty write and asserts no ACK.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-19T21:46:59.453650Z","created_by":"michel","updated_at":"2026-09-19T23:29:41.115656Z","closed_at":"2026-09-19T23:29:41.115564Z","close_reason":"Implemented in #99 (d6cfc12)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-evv","title":"Keep command mode working after Alt+_ and other ESC-prefixed keys","description":"In Lex::Esc (src/keys.rs:226-230) any of ] P _ ^ X after ESC switches the input lexer into Lex::Str, which (keys.rs:283-290) forwards every byte raw until a BEL or ESC \\, and Lex::Str has no timeout (deadline()/tick() only cover seq_since and held state). It exists for terminal replies (OSC 52/11, DCS), but a keyboard Meta+key produces the same two bytes.\n\nScenario: in bash press Alt+_ (readline's default yank-last-arg, sent as ESC _). From then on Ctrl-] Ctrl-] d, legacy or kitty encoding, is forwarded to the program and never detaches, silently, until the user types Ctrl-G. Verified in a scratch harness: ESC _ then 5 s later 1D 1D 64 gives action=None with all bytes forwarded; with a 07 in between it detaches. Contradicts the README's \"press Ctrl-] Ctrl-] quickly, then d\".\n\nDone when only the string introducers the replies actually use are treated as strings and the string state is bounded by a short timeout like PENDING_MS, with a test for ESC _ followed by the command key.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-19T21:46:58.601586Z","created_by":"michel","updated_at":"2026-09-19T22:17:09.743823Z","closed_at":"2026-09-19T22:17:09.743712Z","close_reason":"Implemented in #73 (0f6159f)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-fbo","title":"Require a handshake and authorize KILL on the master","description":"Security, missing authorization. master.rs handles `Msg::Kill if pending` by setting the connection to Ending and calling start_kill() with no HELLO, no identity and no force flag. Verified by reading the arm. The BUSY path leaves the connection Pending, so a client that was just refused (\"busy, alice@laptop is attached\") can send a 5-byte KILL frame on that same connection and destroy the session it was denied. More simply, any process running as that uid can write the frame into any session socket. The menu's confirmation is client-side courtesy only.\n\nThe socket permission is therefore the entire trust boundary, which DESIGN §4.5 accepts for a shared account while still promising nobody ends someone else's session by accident.\n\nDone when KILL requires a completed HELLO and is subject to the same identity and force rules as a takeover, a KILL on a connection already answered BUSY is refused, the proxy's END_SESSION path still works, and tests cover the refused-then-kill sequence.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-20T04:35:59.914450Z","created_by":"michel","updated_at":"2026-09-20T05:05:58.384541Z","closed_at":"2026-09-20T05:05:58.384443Z","close_reason":"Implemented in #111: KILL carries its asker and is held to the takeover rule","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-fh5","title":"Check GitHub weekly for a newer acs release and show a message","description":"Check GitHub for a newer acs release once a week and tell the user.\n\nUser's ask: \"have the acs binary check for a new release on github once a week, if a new release is available display a message\".\n\nProposed design:\n\n- Only the local client checks; the remote roles (`_proxy`, `_master`, `_install`) never do.\n- At most once every 7 days, recorded in `$XDG_STATE_HOME/acs/update-check` (default `~/.local/state/acs/update-check`). It queries `https://api.github.com/repos/michel-onstein/acs/releases/latest` and compares `tag_name` to our version with semver.\n- acs has no HTTP/TLS client, and adding one would break the 1 MB size budget (DESIGN §9). So it shells out to `curl` with a short timeout (for example `-m 3`) and never blocks or delays connecting: run it in the background and show the result on the next start, or finish within the timeout before raw mode.\n- The message goes to stderr outside the session stream, e.g. `acs 0.3.0 is available (you have 0.2.0) — run: acs upgrade`. It is shown once per new version, not on every run.\n- Can be turned off with `ACS_NO_UPDATE_CHECK=1`, and with an `update_check: false` option once the YAML config file (acs-xfk) exists. Being offline or rate-limited stays silent.\n\nDone when: unit tests cover the interval, the version comparison and the \"shown once\" state; an integration test injects the API response (e.g. a fake curl on PATH); README documents it and how to turn it off.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T14:34:32.181828Z","created_by":"michel","updated_at":"2026-09-18T15:22:51.362564Z","closed_at":"2026-09-18T15:22:51.362466Z","close_reason":"Implemented: weekly background _update-check (SHA256SUMS via curl, 3 s), state in XDG_STATE_HOME, message once per new version before connecting, off via ACS_NO_UPDATE_CHECK=1 or update_check: false; silent offline","source_repo":"beads-intake-install","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-install","compaction_level":0,"original_size":0,"labels":["acs","client"],"dependencies":[{"issue_id":"acs-fh5","depends_on_id":"acs-uko","type":"blocks","created_at":"2026-09-18T14:34:41.921922Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-flj","title":"Explain the README's two alias shapes: a host list, or a mapping","description":"Wanted: the README's example configuration uses two shapes for an alias without saying why. `devbox` and `nas` are a list of host entries, while `lab` is a mapping of its own settings with the entries under `hosts:`. A reader asked whether the example is even correct.\n\nVerified (v0.8.1): the example is valid. Loaded as-is with the installed acs, `acs config host list` resolves all five hosts with the expected user, check and key. Both shapes are by design (DESIGN §7.2, \"An alias's own settings\"): YAML cannot make one node both a list and a mapping, so an alias with a setting of its own (`identity_file`, `redraw_on_reconnect`, `reachability_timeout`, `persist`, `reachability_interval`) must be a mapping, and its hosts then need a key. The list form stays the short, usual one. `acs config host set` / `unset` convert between the two. The README's only hint is the comment \"an alias with settings of its own\", plus the settings-table row for `hosts`.\n\nChange: one or two sentences in README \"Configuration\" next to the example (or under \"Host aliases\"): an alias is a list of hosts, or, when it has settings of its own, a mapping of those settings plus `hosts:` with the list. The mapping form is always allowed, and `acs config host set` rewrites a list-form alias into it. Word it to match the top-level key's name at the time; a rename of that key is being proposed separately.\n\nDone when the README explains both shapes and when each is needed, markdownlint passes (scripts/verify.sh), and nothing else changes.\n","status":"closed","priority":3,"issue_type":"docs","created_at":"2026-09-19T20:44:22.001968Z","created_by":"michel","updated_at":"2026-09-19T21:18:12.163730Z","closed_at":"2026-09-19T21:18:12.163636Z","close_reason":"Done in PR #60 (8a9e909): the README explains the list and mapping alias shapes","source_repo":"beads-intake-readme-alias-shapes","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-readme-alias-shapes","compaction_level":0,"original_size":0}
{"id":"acs-ft1","title":"Watch for network changes while connected, not only while offline","description":"netwatch is only polled in reconnect.rs offline(); while connected, a Wi-Fi switch or wake from sleep waits out the full 10 s dead-link timeout before the redial starts. Poll the netwatch fd in client.rs serve() too: on a change, send a PING at once and treat the link as dead if no frame arrives within a short deadline (~2 s), so real drops are caught in 1-2 s and a surviving link costs nothing.\nAlso covers macOS wake, where Instant pauses during sleep so the 10 s window restarts from zero. Done when a netwatch event during serve triggers the early ping, with a test using ACS_NETWATCH_FIFO.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-23T19:28:58.207351Z","created_by":"michel","updated_at":"2026-09-24T02:18:24.379819Z","closed_at":"2026-09-24T02:18:24.379678Z","close_reason":"Implemented in PR #182 (ecd5d5e): serve() polls the netwatch fd; a real change sends a PING at once and gives the host ACS_NETCHECK_MS (2s) to answer. A surviving link costs nothing; a dead one is found at ~2s instead of 10 and recovers via acs-iyq's free redial","source_repo":"beads-intake-latency","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-latency","compaction_level":0,"original_size":0}
{"id":"acs-ftn","title":"Give Timing a HELLO sent phase, now that the greeting has its own moment","description":"acs-pgn (PR #172, d093139) added per-phase connect timings under `-v`.\nacs-trw (PR #176, 2579994) then moved the HELLO to immediately after spawn,\nbefore `ACS-READY` is seen — so the greeting now has a moment of its own that\nthe phase list does not name.\n\nThe acs-trw pass left the documented phase list alone on purpose, to keep churn\nagainst acs-pgn low, and measured its own win with a temporary external relay\nharness that is not in the commit. That is the gap: the change is not\nself-measuring. Anyone asking \"did moving the HELLO actually help on this\nlink?\" has to rebuild that harness instead of reading `-v`.\n\nAdding a `HELLO sent` phase would make the acs-trw win visible from the client\nitself, and would also show the case acs-trw's non-blocking write is designed\nfor — a transport that has not connected yet, where part of the greeting stays\nin `Link::pending` and is written later by `serve()`. Worth deciding whether\nthe phase marks the first write or the moment the whole greeting has gone,\nsince with `pending` those differ and the difference is the interesting bit.\n\nDone when `-v` names the greeting as its own phase, the pending case is\ndistinguishable from the fits-in-one-write case, and DESIGN's phase list and\nthe README's `-v` description match.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-23T23:19:45.541099Z","created_by":"michel","updated_at":"2026-09-23T23:19:45.541099Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-fzx","title":"Make the test remote-env helpers append instead of one silently overwriting the others","description":"`tests/common/mod.rs::Remote::remote_env()` **overwrites** the remote-env file,\nwhile `Remote::remote_umask()` and `Remote::log_master()` (added by acs-o6x,\nPR #158) **append** to it. So calling `remote_env` after either of the others\nsilently discards their setting: no error, no warning, just a test that no\nlonger configures what it says it configures.\n\nNothing is broken today — `remote_env` has one caller, and it happens to be\nthe only thing that caller sets — which is exactly why this is worth fixing\nnow rather than when it bites. The failure mode is a test that still passes\nwhile no longer testing what its name claims, which is the most expensive kind\nof test bug to find.\n\nFound while fixing acs-o6x; left alone there to keep that change to one test.\n\nDone when all three helpers compose — any order, all settings present — with a\ntest that sets two of them and asserts both reached the remote environment.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-23T19:37:01.236396Z","created_by":"michel","updated_at":"2026-09-23T20:42:42.200434Z","closed_at":"2026-09-23T20:42:42.200194Z","close_reason":"Fixed in PR #166 (80553cb): every write to the remote-env file goes through one private append-only helper, so there is no whole-file write left to get the order wrong with","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-g3j","title":"Validate the version parsed out of SHA256SUMS in the installer script","description":"Security, path traversal in the shell installer. scripts/install.sh greps SHA256SUMS with a pattern whose filename part is [^ ]+, which permits / and .., so the extracted version can be something like 1.0/../../../../etc/cron.d/x. That value then reaches mkdir -p \"$lib/$version\" and the place call, as root in the root branch. The Rust side is strict about this (release::find_asset then parse_version, digits and dots only), so the two implementations of the same logic disagree.\n\nReaching it needs full control of the release channel, so this is hardening behind the signing item rather than an independent hole, and the fetch into $tmp/$file fails first for most traversal shapes.\n\nDone when the script requires X.Y.Z exactly, as the Rust code does, and tests/scripts.rs or test_install.sh covers a hostile sums line.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-20T04:36:01.108315Z","created_by":"michel","updated_at":"2026-09-20T06:07:32.925273Z","closed_at":"2026-09-20T06:07:32.925191Z","close_reason":"Implemented in #126: the sums pattern requires X.Y.Z, with a case guard behind it","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-gb4","title":"Make e2e_ssh.sh --no-build refuse or warn when dist/ is stale","description":"`scripts/e2e_ssh.sh --no-build` reuses whatever is in `dist/` with no check that\nit was built from the current source tree. A red e2e failure from a stale\nbinary is **indistinguishable** from a real one: same test, same assertion,\nsame message.\n\nFound during acs-8kv (PR #162, caa3854). While deliberately breaking the\n`Call::Session` gate to prove `e2e_14` could fail, a `--no-build` run handed\nback a red result from a binary built two edits earlier — after the break had\nalready been reverted. It was only noticed because the person running it knew\nexactly what they had just restored. Anyone bisecting an e2e failure, which is\nprecisely when `--no-build` is worth using, has no such luxury.\n\nThe obvious fix is a stamp: `cargo xtask dist` writes the source tree's state\n(a git sha plus a dirty marker, or a hash of the inputs it packaged) beside the\nartifacts, and `--no-build` compares it and either warns loudly or refuses.\nRefusing is probably right — the flag exists to save a minute, and a wrong\nanswer costs far more than the minute — with a way to say \"yes, I mean it\".\n\nNote `--no-build` is genuinely valuable: it takes the suite from ~1:03 to ~20 s,\nwhich is what makes iterating on an e2e case bearable. This is about making it\nhonest, not about removing it.\n\nDone when `cargo xtask dist` records what it built from, `--no-build` compares\nand refuses (or warns unmissably) on a mismatch including an uncommitted change,\nthere is a way to override deliberately, and a test covers the mismatch case.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-23T20:03:50.726919Z","created_by":"michel","updated_at":"2026-09-23T22:13:20.520449Z","closed_at":"2026-09-23T22:13:20.520279Z","close_reason":"Fixed in PR #170 (2d0a719): cargo xtask dist writes a content hash of the sources to dist/source.stamp, and --no-build refuses on a mismatch unless --allow-stale-dist is passed","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-gkl","title":"Fix master spinning on pty POLLHUP while the ring is full","description":"With an active client stalled and the ring full (`room_before_overwrite(next) == 0`) the master polls the pty with `events = 0` (src/master.rs ~L384-395), but Linux `n_tty_poll` reports POLLHUP once the slave side closes, regardless of `events` and even with unread data. Poll then returns immediately, `read_pty` bails at `want == 0` (~L793-796), the drained heuristic does not apply because `asked` is false, `pump_output` cannot add (client at high water), and the loop spins hot until the client drains a chunk or the connection dies. Trigger: `acs host -- cat big.log` over a stalled link when cat finishes.\n\nDone when the pty fd is omitted from the poll set (or the loop sleeps) while `room == 0`, with a test covering hangup-while-stalled.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-18T15:14:21.458270Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.357636Z","closed_at":"2026-09-18T15:39:58.357562Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-gov","title":"The prelude's acs_safe check reads the symlink, not its target, and is wrong on both platforms","description":"`acs_safe` in the ssh prelude decides whether a remote acs binary is safe to\nexecute. It reads the mode with `ls -ldn` — **without `-L`** — so on a symlink\nit inspects the *link*, never the target.\n\nFound during acs-cu8 (PR #186, a5d3274), where it was the mechanism behind 11\nof 36 container failures. It is wrong in opposite directions on the two\nplatforms:\n\n- **Linux**: a symlink is `lrwxrwxrwx`, so the `?????w*|????????w*` pattern\n  matches and the binary is **always refused** — with the message \"it or its\n  directory is writable by others, or not yours\", which is not the reason. A\n  user who symlinks acs into place gets a refusal that sends them looking for\n  a permissions problem that does not exist.\n- **macOS**: a symlink is `lrwxr-xr-x`, so the check **passes** and the\n  target's mode and owner are never examined at all. That is precisely the\n  hole acs-08m was meant to close: a world-writable binary reached through a\n  symlink is executed without complaint.\n\n**Impact today is low**, and that is why this is worth doing calmly rather\nthan urgently: `acs _install` writes a real file, and the Homebrew symlink is\nnot on `remote_candidates`, so no real install is a symlink. The test harness\nwas the only thing producing one, and acs-cu8 changed it to write a real file\n(hard_link, falling back to copy) the way a real install does. So nothing is\ncurrently exploiting it.\n\nBut a check that silently does not check is worse than no check: it reports\nsafety it never established, and the next person to add a candidate path or\npackage acs differently inherits the hole with no warning.\n\nThe fix follows the link — `ls -ldnL`, or resolving it first — and then has to\ndecide what to do about the *link's own directory*, which is a second place an\nattacker could substitute the target. Worth re-reading acs-08m's reasoning\nbefore choosing, since this is its check.\n\nDone when the mode and owner examined are the target's, a symlinked binary\nwhose target is safe is accepted on both platforms, a symlinked binary whose\ntarget is unsafe is refused on both, the refusal message names the actual\nreason, and tests cover all four combinations on at least one platform.","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-24T04:13:09.325010Z","created_by":"michel","updated_at":"2026-09-24T05:01:29.046458Z","closed_at":"2026-09-24T05:01:29.046287Z","close_reason":"Fixed in PR #188 (595cdc7): acs_safe walks the chain with readlink and judges the final file plus every directory on the path. Both halves demonstrated before the fix; ls -ldnL verified on busybox, GNU and BSD","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-hjk","title":"Harden the session socket: fchmod 0600 and an O_NOFOLLOW directory handle","description":"Security, defence in depth. Two small gaps sit under an otherwise solid cross-uid boundary. master.rs bind creates the unix socket at 0777 masked by the inherited umask and never chmods it; the 0700 directory and the SO_PEERCRED/getpeereid check on every accept are the only controls. session.rs checks the directory with symlink_metadata and then calls set_permissions by PATH, a chmod that follows symlinks applied after the lstat, and only the leaf's ownership is checked, never its ancestors.\n\nNeither is exploitable at the default path: /tmp is sticky, a pre-created directory fails the uid check, and tests/multiuser.rs deliberately loosens the directory to 0755 and the socket to 0777 and proves the peer check still refuses. It becomes reachable only with ACS_SOCKET_DIR pointed at a path whose parent others can write, where swapping the leaf for a symlink between the check and the connect redirects the client to an attacker's master.\n\nDone when the socket is fchmod'd 0600 after bind, the directory is verified and chmod'd through an O_DIRECTORY|O_NOFOLLOW handle rather than by path, a socket directory with other-writable ancestors is refused, and multiuser tests cover it.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-20T04:36:01.574342Z","created_by":"michel","updated_at":"2026-09-20T06:58:08.133493Z","closed_at":"2026-09-20T06:58:08.133186Z","close_reason":"Implemented in #131: socket created 0600, dir held by an O_NOFOLLOW handle, ancestors checked","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-hmv","title":"Let acs list over every alias join an existing master without starting one","description":"acs-9n3 (PR #184, cc1ab77) gave first connections and side calls a shared\nmaster. Batch calls — `acs list` over every alias (§7.3) — were deliberately\nleft out: they neither start nor join one.\n\nThe reason was sound. A batch fans out to every alias at once, so letting each\none *start* a master would mean a dozen masters appearing simultaneously, plus\na race where several calls to the same host each decide to be the master. That\nis exactly the footprint and the race acs-9n3 was warned about.\n\nBut **joining** has neither problem. `ControlMaster=no` with the path set means\n\"use the master if one is there, otherwise dial normally\" — no creation, no\nrace, and the fan-out is what makes it worth having: a batch over a dozen\naliases is a dozen handshakes, and any host you already have a session to\nwould answer from its existing master instead. On a real link that is the\ndifference between a listing that takes a second and one that takes several.\n\nCheap to do — `control_opts()` already distinguishes starting from joining, and\n`Call::Batch` is already its own variant — but worth a deliberate look at:\n\n- whether a batch joining a master can slow down the *interactive* session\n  sharing it (ssh multiplexes channels, so mostly no, but a dozen channels\n  opening at once on one connection is not nothing);\n- whether the 2s `ACS_CONTROL_FALLBACK_MS` check per host is worth paying when\n  most hosts in a batch will not have a master — `ssh -O check` costs ~5ms, so\n  probably yes, but measure rather than assume;\n- whether a batch should skip the check entirely and just set the path, letting\n  ssh fall back on its own.\n\nDone when a batch call joins an existing master where one is there, never\nstarts one, the fan-out does not degrade an interactive session on the same\nmaster, and the win is measured with acs-pgn's timings rather than asserted.","status":"open","priority":4,"issue_type":"feature","created_at":"2026-09-24T03:39:19.305881Z","created_by":"michel","updated_at":"2026-09-24T03:39:19.305881Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-hpf","title":"Guard the input-dedupe sequence arithmetic against overflow","description":"Security, peer-steerable arithmetic. resume.rs InputDedupe::accept computes `let end = seq + bytes.len() as u64;` with no guard, where seq comes straight from a peer's INPUT frame, and master.rs written_to_pty then does a plain `self.input.written() - self.pty_in.len() as u64`. Verified by reading both, and Cargo.toml sets no overflow-checks, so release builds wrap.\n\nAn INPUT frame with seq near u64::MAX wraps end to a small number while bytes are queued, so written_to_pty underflows to roughly 2^64 and poisons the ACK the client uses to decide what it may forget. A debug build aborts the master outright, ending the session.\n\nDone when the addition is checked and a frame whose sequence cannot be valid is refused as a protocol error rather than wrapping, with a test for the wrapping sequence.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-20T04:36:01.228358Z","created_by":"michel","updated_at":"2026-09-20T06:07:33.016581Z","closed_at":"2026-09-20T06:07:33.016488Z","close_reason":"Implemented in #127: accept refuses a sequence whose end does not fit","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-hsw","title":"The load failures that are left are all the dial, and T is the only lever","description":"After acs-o6x, acs-o8h, acs-7wu, acs-ryz and acs-2dc, the tests that still fail\nunder heavy load have **one** cause between them, and it is not a window any of\nthose passes could close: the dial is starved, and the test has nothing left to\nwait for.\n\nMeasured during acs-2dc (PR #205, 661e1dc), under 6 `dd` loops plus 10\nfork/exec loops:\n\n- `tests/client.rs` — 86 failures in 850. The two biggest are\n  `unsupported_remote_platform_is_reported` (34) and\n  `the_verbose_command_line_reaches_the_acs_arguments` (30). Both wait for the\n  program's first byte and there is no earlier observable event.\n- `tests/menu.rs` — 64 in 700, i.e. **worse** than client.rs (9.1% against 2.7%)\n  with the identical empty terminal, so this is not a property of one file.\n- `host_aliases` — 13 in 60 on `timed out waiting for \"new session 's' on \"`,\n  after that bead's own failures went to zero.\n- `alias::tests::ping_is_one_packet_with_a_backstop_past_the_deadline` — 4 in 32\n  full-lib runs: a `fork` + `exec` + shell startup taking more than the **10 s**\n  acs-ryz had already widened it to.\n- `tests/reconnect.rs::a_redial_into_a_silent_host_returns_to_the_backoff` — 9 in\n  522, independent of any of these changes.\n\nThe diagnosis is established, not guessed: holding the load fixed and moving `T`\nfrom 30 s to 300 s took client.rs from 86 failures to 6, and **removing the I/O\npressure while keeping load average ~20 gave 850/850 green**. It is I/O\nstarvation of two shells plus a cold 4 MB debug binary — nothing subtler.\n\nSo the question this bead exists to answer is a policy one, not a debugging one:\n\n- **Is `T` (30 s) the right suite-wide deadline?** It is four times smaller than\n  the client's own first-dial timeout (120 s), so a starved dial is\n  indistinguishable from a hang. Raising `T` makes a genuine hang slower to\n  report, which is the trade.\n- **Or is \"the suite is not expected to pass under deliberate I/O starvation\" the\n  honest answer?** If so it should be written down, so the next person does not\n  spend a day rediscovering it. acs-2dc's numbers are the evidence either way.\n- A third option worth weighing: make the dial itself cheaper in tests — a\n  release-profile test binary, or a fake transport that does not fork two shells.\n\nNote what is NOT in scope: the five clients that were **SIGKILLed externally**\nunder memory pressure (swap at 3.4/4 GB), zero bytes written, 1.2-1.9 s after\nspawn. No deadline addresses that and it is not acs's doing.\n\nDone when the suite's deadline policy is decided and recorded — either `T` moves\nwith a stated reason, or the starvation limit is documented as expected — and\nthe four beads above stop being re-filed as individual flakes.","notes":"A data point from acs-br2 (PR #208, 28ee038) that strengthens this bead's\nframing, and one that narrows it.\n\n**Strengthens:** one `cargo test --workspace` run had `exit_command_ends_the_session`,\n`a_note_is_not_swallowed_by_a_session_that_goes_quiet` and\n`a_verbose_note_lands_at_a_boundary_of_the_programs_output` all time out at `T`\n**in the same binary, immediately after a docker run finished**. Three tests\nstarved for 30s together reads like the whole binary losing the CPU rather than\nany one test having a window — which is this bead's thesis. They passed alone\nand never recurred in three later runs.\n\n**Narrows:** acs-br2 itself turned out **not** to belong here. It looked like\nthe same class — cold, full-parallel, unreproducible in 64 targeted runs — and\nthe bead explicitly asked whether it was environmental starvation. It was a\ngenuine product bug: stale in-flight frames answering a netcheck, with\nstarvation as the *trigger* rather than the cause, reproducible 9/9 once the\nordering was forced deterministically.\n\nThat is worth remembering when deciding this bead. \"Only fails cold and\nfull-parallel, cannot reproduce it in isolation\" is consistent with starvation\n**and** with a real ordering bug that only starvation exposes. Before writing\ndown \"the suite is not expected to pass under deliberate I/O starvation\", it is\nworth asking of each remaining failure whether starvation could be exposing an\nordering the code gets wrong — as it was here.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T19:22:40.121741Z","created_by":"michel","updated_at":"2026-09-25T20:56:41.782031Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-iff","title":"Add acs config subcommands to edit the configuration","description":"Add `acs config …` subcommands to manage the configuration file from the command line.\n\nUser's ask: \"add options to the cli to allow configuration of acs through the command line, something like `acs config host add <alias> <hostname> --reachability-check`\".\n\nProposed shape (settle the details during implementation):\n\n- `acs config show`: prints the merged configuration and which file each value came from.\n- `acs config get <key>` and `acs config set <key> <value>`, e.g. `acs config set install_on_remote false`.\n- `acs config host add <alias> <host> [--user U] [--no-reachability-check]`: appends an entry, so repeated adds give an alias fallback hosts in order.\n- `acs config host remove <alias> [<host>]` and `acs config host list`.\n- Writes go to the local file (`~/.config/acs/config.yaml`); `--global` targets `/etc/acs/config.yaml` and needs the right permissions.\n\nNotes:\n\n- `config` becomes a reserved first argument. Today it would be read as a host name (src/cli.rs), so a host literally called `config` needs a way through, e.g. `acs -- config` or `user@config`. Document it.\n- Writing must keep the file readable and stable (no reordering of what it did not touch); comments are nice to keep but not required.\n\nDone when: every subcommand works against a temp HOME in tests; errors (unknown key, wrong type, missing alias) are clear; README documents it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T14:23:38.165184Z","created_by":"michel","updated_at":"2026-09-18T15:04:31.998311Z","closed_at":"2026-09-18T15:04:31.998233Z","close_reason":"Implemented: acs config show/get/set/unset/path and host list/add/remove, --global, comment-preserving validated atomic writes; config reserved as first arg","source_repo":"beads-intake-config","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-config","compaction_level":0,"original_size":0,"labels":["acs","cli","config"],"dependencies":[{"issue_id":"acs-iff","depends_on_id":"acs-xfk","type":"blocks","created_at":"2026-09-18T14:23:47.272385Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-iry","title":"Treat a failure to link ~/.local/bin/acs as a warning, not an install failure","description":"install::finish() (src/install.rs:266-278, 302-314) renames the binary into place and only then calls link_bin; any error there (HOME unset in the ssh environment, ~/.local/bin existing as a regular file or not writable, symlink failing) returns Err, the finisher exits 1 without printing ok, and the client reports \"installing acs failed\" and exits, although ~/.local/share/acs/<v>/acs is complete and every later connection works. DESIGN §8 says nothing in the protocol depends on that link.\n\nDone when link_bin failure is reported as a warning after ok, the install still counts as successful, and a test makes ~/.local/bin a file and asserts the install succeeds with a warning.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-19T21:46:58.936906Z","created_by":"michel","updated_at":"2026-09-19T22:41:52.065437Z","closed_at":"2026-09-19T22:41:52.065307Z","close_reason":"Implemented in #83 (80cf8d5)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-iws","title":"Install creates its directory under the umask, then refuses to run from it","description":"Reported by the user against v0.11.4, connecting to a host that worked before:\n\n    acs: installing acs 0.11.4 on tolot (Linux x86_64)…\n    acs: installed acs 0.11.4 on tolot\n    acs: refusing to run /home/michel/.local/share/acs/0.11.4/acs: it or its\n      directory is writable by others, or not yours\n    acs: acs was installed on tolot but the remote still cannot run it\n      (is $HOME the same for ssh commands?)\n    exit 254\n\nacs-08m (#132) added the prelude's acs_safe check, which refuses a candidate\nwhose file OR its directory is group/other writable, or not owned by the\ncaller (ssh.rs prelude()). It did not change the install, which still creates\nthe directory with a bare `mkdir -p` under the remote's umask\n(install.rs:129,138 — `set -e; mkdir -p {dir}; …`).\n\nThe binary is fine: the upload chmod 755's it, and write_exe sets 0755 for\nreal past the umask (acs-28b). Only the directories are left to the umask.\n\nOn the reported host (umask 002, uid 1000, per-user group):\n\n    drwxr-xr-x  /home/michel                       fine, predates acs\n    drwxr-xr-x  /home/michel/.local                fine\n    drwxr-xr-x  /home/michel/.local/share          fine\n    drwxrwxr-x  /home/michel/.local/share/acs      made by acs, group write\n    drwxrwxr-x  /home/michel/.local/share/acs/0.11.4   made by acs  <- checked\n    -rwxr-xr-x  /home/michel/.local/share/acs/0.11.4/acs   fine\n\nacs_safe checks `dirname \"$b\"`, the version directory, so it refuses the\ndirectory acs itself has just created. Every host with umask 002 is bricked\nby an install: it succeeds and the very next exec is refused, 254. The\nownership half of the check passes (uid 1000 owns all of it); only the group\nwrite bit trips it. Here the group is the user's own per-user group, so it is\nharmless in fact — but the check cannot know that, and should not have to.\n\nWanted: the install creates the directories it owns with 0755 explicitly,\npast the umask, as it already does for the binary — the version directory\nand its ~/.local/share/acs parent. Do not touch $HOME, ~/.local or\n~/.local/share: acs did not create them, and chmod'ing a user's own\ndirectories is not acs's business (the check does not look at them either).\n\nNote for whoever takes it: an existing install on such a host stays broken\nuntil its directory is fixed, since the directory is only created when a\nversion is new. Upgrading to the fixed version creates a new version\ndirectory, so it heals itself; the stale ~/.local/share/acs parent should be\nchmod'd by the install too so it does not linger group-writable.\n\nDone when the install sets the mode on the directories it creates, a test\nruns the real install script under umask 002 and asserts the resulting\ndirectory is not group- or other-writable and that the real prelude then\nexecs it, and DESIGN §8 says the directories are created 0755 past the umask.\n\nRegression of acs-08m; the mode-past-umask reasoning is acs-28b's.","status":"closed","priority":0,"issue_type":"bug","created_at":"2026-09-20T15:07:15.642351Z","created_by":"michel","updated_at":"2026-09-20T15:16:09.459006Z","closed_at":"2026-09-20T15:16:09.458921Z","close_reason":"Fixed in #138 (v0.11.5): the install sets 0755 on the directories it makes","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-iyq","title":"Redial at once on the first attempt after a drop","description":"Backoff::new(1000) in reconnect.rs makes even the first redial after a lost link wait 1 s. Most drops are momentary (a Wi-Fi blip), so the first attempt should go immediately and the 1 s -> 30 s backoff start from the second. Saves 1 s on every resume.\nDone when the first redial after a LinkLost has no wait, later ones keep the backoff, and a unit test on Backoff covers it (ACS_BACKOFF_MS still sets the base).\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-23T19:28:54.761944Z","created_by":"michel","updated_at":"2026-09-24T01:14:36.530256Z","closed_at":"2026-09-24T01:14:36.530096Z","close_reason":"Implemented in PR #178 (f565134): Backoff.next is Option, the first attempt after a drop goes at once, and NetworkChanged uses spent() so a watcher-driven redial still costs one dial. Measured cut-to-resumed median 1030ms to 43ms","source_repo":"beads-intake-latency","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-latency","compaction_level":0,"original_size":0}
{"id":"acs-kip","title":"Make the timing-sensitive tests survive a loaded machine","description":"Two integration tests fail intermittently when the whole suite runs at once, and pass on their own every time. Seen twice during the security burn-down of 2026-09-20:\n\n- tests/master.rs::a_client_that_vanishes_without_closing_is_dropped — pushes 3 MB through a pty and waits on a marker file within a 10 s deadline.\n- tests/reconnect.rs::ctrl_c_during_a_redial_clears_the_status_line — depends on a redial landing inside a window.\n\nNeither was a real failure: re-running scripts/verify.sh passed both times, and the changes in flight (an installer digest check, an input-sequence guard) cannot touch either path. But a gate that fails one run in several teaches people to re-run rather than to read, which is how a real failure eventually gets waved through.\n\nDone when both are steady under `cargo test` with the whole suite running in parallel — a generous deadline driven by an environment knob as the liveness tests already use, waiting on a condition rather than a sleep, or serialising the few that need the machine to themselves — and the reason is written next to whichever mechanism is chosen.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-20T06:07:54.983155Z","created_by":"michel","updated_at":"2026-09-20T06:58:15.007686Z","closed_at":"2026-09-20T06:58:15.007580Z","close_reason":"Implemented in #135: generous deadlines, a condition instead of a sleep, a wider parallel margin","source_repo":"close-three","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/close-three","compaction_level":0,"original_size":0}
{"id":"acs-km8","title":"Draw the session menu's selection bar at one width for every row","description":"Wanted, in the user's words: \"in the session selection menu, make the bar that shows your current selection the same width across all the entries, covering the largest width needed among the entries.\"\n\nVerified (v0.5.0, src/menu.rs `Menu::render`): each selectable row is `\"{mark} {key}  {text}\"` and the cursor's row is written as `\\x1b[7m{text}\\x1b[0m`, so the reversed bar is exactly as long as that row's text. It is short on \"new session\" and \"exit\", and differs between sessions whose last `--list` column (the command) differs in length. It jumps in width as the cursor moves.\n\nFix: pad the cursor's row with spaces to the widest selectable row (sessions, new session, exit) currently shown, clipped at the terminal width (`cols`) as lines already are, so the bar never wraps. Measure in terminal columns rather than chars if a command can hold wide characters; `render` truncates with `chars().take(cols)` today.\n\nDone when the reversed bar has the same width on every row, equal to the widest entry (or the terminal width if that is narrower), with a unit test next to the existing render tests in src/menu.rs asserting the padded width on a short row (exit) and a long session row.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-19T14:26:21.478654Z","created_by":"michel","updated_at":"2026-09-19T15:56:59.395366Z","closed_at":"2026-09-19T15:56:59.395139Z","close_reason":"Implemented in PR #42 (01c9b5c): the selection bar is padded to the widest row, clipped at the terminal width, measured in columns","source_repo":"beads-intake-menu-bar","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-menu-bar","compaction_level":0,"original_size":0}
{"id":"acs-ktm","title":"Let a fork set its release signing key at build time, as it now can the releases URL","description":"Follow-up to acs-5em (PR #153, be220ed), which made the default releases URL\nsettable at build time via `ACS_DEFAULT_RELEASES_URL`. The signing key did not\nmove with it, and the two are only useful together.\n\n`signature::key_for(base)` returns the built-in `RELEASE_KEY` when `base ==\nDEFAULT_RELEASES_URL`, and only otherwise consults `ACS_RELEASE_KEY`. So a fork\nthat bakes in its own releases URL and keeps acs's key **cannot verify its own\nreleases**: `acs upgrade` reaches the fork's SHA256SUMS and then refuses them,\nbecause the key it checks against is upstream's. The fork's two options today\nare to patch `src/signature.rs` (a merge conflict on every upstream sync — the\nexact problem acs-5em removed for the URL) or to tell every user to export\n`ACS_RELEASE_KEY` forever.\n\nThe mechanism to copy already exists and is one commit old: `build.rs` passes\nthe variable through with `cargo:rerun-if-env-changed`, `src/release.rs` has\n`const fn default_releases_url(Option<&'static str>)` with an empty value\ncounting as unset, and `release::tests::the_default_releases_url_can_be_set_at_build_time`\nis the shape of the test.\n\nNote this is *not* a loosening of acs-95w or of acs-o9v's verification. The\nruntime `ACS_RELEASE_KEY` override and its guards stay exactly as they are; a\ncompile-time key is chosen by whoever builds the binary, who already decides\nwhat it does. Say so in the doc comment, as acs-5em did for the URL.\n\nDone when the default release key can be set at build time and falls back to\ntoday's `RELEASE_KEY` when unset, an empty value counts as unset, a test covers\nboth cases the way the URL's does, and the Forking table in docs/VERSIONING.md\nmoves the key from \"source edit\" to \"build-time knob\".","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-23T18:39:40.190647Z","created_by":"michel","updated_at":"2026-09-23T19:46:02.439837Z","closed_at":"2026-09-23T19:46:02.439655Z","close_reason":"Implemented in PR #160 (df58317): ACS_DEFAULT_RELEASE_KEY at build time, empty falls back to acs's own key, a malformed key fails the build; demonstrated against a locally signed release","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-ljl","title":"Take the create lock before --list removes a stale socket","description":"`list()` (src/proxy.rs ~L317-325) removes a socket after ECONNREFUSED/ENOENT without holding `create_lock`, while master start (src/master.rs ~L304-315) does check → unlink → bind under that lock. Interleaving: list connects (refused, old stale file) → master A unlinks the stale file and binds a fresh socket → list unlinks A's *live* socket. A is unreachable for up to `rebind_every()` (60 s); any proxy in that window spawns master B; when A's `check_socket` runs it sees another inode and leaves it — A is orphaned forever with its shell still running.\n\nDone when the refused → unlink step takes `dir.create_lock(name)` (try_lock; skip if busy), with a test for list racing a bind.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-18T15:14:21.703859Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.371134Z","closed_at":"2026-09-18T15:39:58.371049Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-m0l","title":"Replace acs --list without a host by an acs list subcommand","description":"Wanted, in the user's words: \"`acs --list` lists the sessions on all hosts. This is wrong: --list is an option style, not a command. It should be `acs list`. This does increase the number of reserved aliases/hosts.\"\n\nVerified (v0.7.0): `acs [ssh options] --list` (or `-l`) with no host lists every alias's sessions (src/cli.rs, \"Only `--list` goes without a host\"; src/list.rs `run_all`; DESIGN §7.3 \"Every alias at once\"; README \"Use\"; `acs --help`). The other whole-program actions are already subcommands: `config` and `upgrade` are reserved **first** arguments (DESIGN §7.4, §7.5), and a host literally named like one is reached as `user@config` or with an option before it (`acs -p 22 config`). `list` becomes the third reserved first argument, with the same escape.\n\nChange: `acs list` (taking the ssh options, as `acs --list` does today, e.g. `acs -p 2222 list` or `acs list -v`, whichever the parser settles on) runs today's all-alias listing (parallel, BatchMode, the HOST table). Drop the hostless `--list` outright, with no alias kept (not yet a compatibility contract; say so in the PR). Open point: whether the per-host `acs <host> --list` stays as an option (it is a modifier of a host) or moves to `acs list <host>` for symmetry. Decide and write it down in DESIGN.\n\nDone when `acs list` lists every alias, a hostless `acs --list` is a usage error that points to `acs list`, `list` is documented as reserved with the `user@list` / option-before escape, cli.rs tests cover the parse (including a host named `list`), and DESIGN §4.4/§7.3/§7.4, the README, the module docs in cli.rs and `acs --help` are updated.\n","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-19T16:19:42.942453Z","created_by":"michel","updated_at":"2026-09-19T16:45:27.456959Z","closed_at":"2026-09-19T16:45:27.456873Z","close_reason":"Implemented in PR #45 (f5164ab): acs list [host] replaces the -l/--list option; list is a reserved first argument","source_repo":"beads-intake-list-cmd","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-list-cmd","compaction_level":0,"original_size":0}
{"id":"acs-m9i","title":"Automatic semantic versioning (scripts/version-bump.sh)","description":"Bump the version automatically after each merge: PATCH for small fixes, MINOR for larger fixes and features, MAJOR only on explicit instruction (resets MINOR and PATCH). Reads unreleased commits since the last vX.Y.Z tag, updates Cargo.toml/Cargo.lock, pushes a chore(release) commit and tag to origin/main via a throwaway worktree. Run by the ship skill.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T02:52:48.289045Z","created_by":"michel","updated_at":"2026-09-18T02:56:34.924724Z","closed_at":"2026-09-18T02:56:34.924634Z","close_reason":"cargo xtask bump + scripts/version-bump.sh: PATCH small fixes, MINOR features/larger fixes (>=300 shipped lines, Semver trailer or semver label), MAJOR only via --major (resets minor/patch); release commit + annotated tag pushed atomically from a throwaway worktree; idempotent; docs/VERSIONING.md; unit tests + end-to-end lifecycle test","source_repo":"semver","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/semver","compaction_level":0,"original_size":0,"labels":["acs","infra"]}
{"id":"acs-mam","title":"Publish release binaries to GitHub Releases","description":"Attach per-platform archives (acs-<version>-<target>.tar.gz), SHA256SUMS and install notes to a GitHub Release for each vX.Y.Z tag, built from the exact tag. scripts/release-binaries.sh does it; scripts/version-bump.sh calls it after cutting a release so shipping publishes binaries. Packaging lives in cargo xtask package (tested).","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T03:13:45.205920Z","created_by":"michel","updated_at":"2026-09-18T03:17:18.448517Z","closed_at":"2026-09-18T03:17:18.448422Z","close_reason":"cargo xtask package (archives with acs + README, SHA256SUMS, NOTES.md) + scripts/release-binaries.sh (build tag in throwaway worktree, gh release create/upload) + version-bump.sh publishes new tags; tests for packaging; v0.1.0 dry run verified (checksums, macOS and Linux binaries run)","source_repo":"release-binaries","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/release-binaries","compaction_level":0,"original_size":0,"labels":["acs","infra"]}
{"id":"acs-mbd","title":"Configure an ssh identity file per alias and per alias host","description":"Wanted, in the user's words: \"add the option to specify the SSH identity file in the configuration, either at alias level and/or at alias>host level. The identity configuration supplied through the commandline has precedent, then the one at alias>host level, followed by the alias level.\"\n\nToday the identity only comes from `-i <identity_file>` on the command line (src/cli.rs, forwarded by src/ssh.rs; repeatable, ssh tries them in order) or from ~/.ssh/config. Config `hosts` entries (src/config.rs `HostEntry`, `HOST_KEYS`) accept only `host`, `user` and `reachability_check`, and an alias is just a list of entries (or one entry), so there is nowhere to put an alias-wide setting yet.\n\nPrecedence, highest first: `-i` on the command line; `identity_file` on the chosen host entry; `identity_file` on the alias. Only the winning level is passed to ssh as `-i`. It applies to every ssh call that uses the resolved destination (session, --list, install, redial).\n\nOpen design points: the YAML shape for an alias-level setting, keeping today's list form valid (for example `devbox: {identity_file: …, hosts: [...]}`); whether one level may hold several files, like repeated `-i`; `~` expansion; how global and local files merge an alias-level value (local replaces, as with other settings); and `acs config host add --identity-file` plus `config show` / `host list` output.\n\nDone when the config validates and `acs config` edits the new key at both levels, the precedence is covered by tests, and DESIGN §7.2–7.4 and the README describe it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T19:36:00.001340Z","created_by":"michel","updated_at":"2026-09-18T21:45:02.405013Z","closed_at":"2026-09-18T21:45:02.404934Z","close_reason":"Implemented with tests: identity_file per host entry and per alias, -i > host > alias, acs config edits both (PR #30, which replaced #28)","source_repo":"beads-intake-ssh-identity","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-ssh-identity","compaction_level":0,"original_size":0}
{"id":"acs-mkb","title":"tests/master.rs has its own numbers() with the trailing-torn-line bug acs-2dc just fixed","description":"acs-2dc (PR #205, 661e1dc) moved `numbers` / `assert_consecutive` /\n`last_number` into `tests/common` — `tests/e2e_ssh.rs` had held a byte-identical\ncopy — and fixed two real faults in them: a note tearing a number, and the\nsnapshot itself cutting one in half (`c.text()` is a prefix of a live stream, so\nits last bytes are as likely as not to be `#17` of `#175#`).\n\n`tests/master.rs` has **its own** `numbers`, which that pass did not touch, and\nit has the second fault: `split(['\\r','\\n'])` plus `strip_prefix('n')` happily\nparses a truncated `n17` as the number 17. Same class, same consequence — a torn\nnumber reported as lost output, which is the `174 → 17` shape acs-ryz chased and\ncould not explain.\n\nIt has not been seen failing, which is why this is P4 rather than P3. But it is\nthe last copy of a helper that has now been demonstrated wrong twice, and the\nfix is known: drop a trailing fragment the snapshot may have cut, exactly as\n`tests/common` now does.\n\nWorth checking while there whether `master.rs`'s copy should simply use the\nshared one. Its prefix differs (`n` rather than `#…#`), so it may need a\nparameter rather than a straight substitution — decide rather than duplicating\nthe fix a third time.\n\nDone when no copy of this helper can report a torn value as a gap, `master.rs`\neither shares the common implementation or has a stated reason not to, and a\ntest covers the truncated-fragment case the way `numbers_leaves_out_what_the_snapshot_cut_in_half`\ndoes.","status":"open","priority":4,"issue_type":"bug","created_at":"2026-09-25T19:22:12.785228Z","created_by":"michel","updated_at":"2026-09-25T19:22:12.785228Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-mq0","title":"ACS_ESCAPE_TIMEOUT_MS widens the double tap but not the window to choose the command key","description":"`ACS_ESCAPE_TIMEOUT_MS` widens the window *between* the two presses of the\ncommand key. The window to then choose what to do — `d` to detach, and the\nrest — is `keys::Config::command_timeout_ms`, a bare 2 s constant with no\nenvironment knob and no config setting.\n\nSo someone who widens the escape window because 400 ms is too quick for them\nhas said, in effect, \"I am slower than the default\" — and acs answers by\nwidening the half of the gesture they did not complain about while leaving the\nother half exactly as it was. The setting does not do what its name suggests\nto the person most likely to set it.\n\nFound during acs-ryz (PRs #194/#195), which could not remove this window: it\nis the one wall-clock window left in `the_bell_rings_while_the_link_is_down_too`\nand `the_escape_window_is_the_same_offline`. The test cannot merge the two\nwrites either, because a feed ending with the action key never leaves the\ndetector armed and so never rings the bell those tests are named for. Closing\nit needs a `src/` change, which is why acs-ryz documented it in the tests\ninstead.\n\nTo settle when taking it: whether the two windows should be one setting or\ntwo. One is simpler to explain and matches what a user means by \"give me\nlonger\"; two is more precise and lets the double tap stay tight while the\nchoice is relaxed. `ACS_ESCAPE_TIMEOUT_MS` is the existing name, so widening\nits meaning is a behaviour change for anyone already setting it — small, since\nthe default is unchanged either way, but say so in DESIGN.\n\nDone when the command-mode window follows the configured escape timeout (or\nhas its own knob, decided deliberately), DESIGN §6.1 says which windows the\nsetting covers, and the two acs-ryz tests can drop their documented wall-clock\nwindow.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-24T07:44:17.610983Z","created_by":"michel","updated_at":"2026-09-25T17:32:31.586842Z","closed_at":"2026-09-25T17:32:31.586480Z","close_reason":"Fixed in PR #203 (33e6fdc): ACS_ESCAPE_TIMEOUT_MS sets the gap outright and the command-key window as that value or 2s whichever is longer, so it widens both and narrows only the first. command_timeout_ms removed. Both acs-ryz wall-clock windows dropped","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-msy","title":"Rename the top-level hosts: configuration key to aliases:","description":"Wanted: the top-level `hosts` key is confusing, since `hosts` and `host` appear at several levels of the hierarchy. Rename it so each word means one thing.\n\nToday (v0.8.1): the word \"hosts\" means two things. The top-level `hosts:` maps **alias names** (what you type: `acs devbox`) to their definitions, and inside an alias's mapping form `hosts:` is the list of host entries, the ways to reach it, tried in order. Each entry's `host:` is one address given to ssh. Everything else already says \"alias\": the README's \"Host aliases\" section, DESIGN §7.3, `-v` output and errors (\"no host for 'lab' is reachable\"), and `acs config host add <alias> <host>`.\n\nChange: the top-level key becomes `aliases:`. Nothing else changes: an alias's `hosts:` list, the entry key `host:`, and `acs config host …` (it adds a host to an alias) all keep their names. The hierarchy then reads aliases → <name> → hosts → host.\n\n```yaml\naliases:\n  devbox:\n    - host: devbox.lan\n  lab:\n    identity_file: ~/.ssh/id_lab\n    hosts:\n      - host: lab.lan\n```\n\nA clean break, since the project is not in production: no alias kept for the old key. A top-level `hosts:` stays an unknown key and is refused, but with a specific message so existing files are easy to fix: `<file>:<line>: 'hosts' is now 'aliases': rename the key`.\n\nTouches: src/config.rs (`KEYS`, the parser and the `hosts` match arm), src/config_cmd.rs (host list / add / remove / set write the top-level key when rewriting the file), the `(hosts:)` hint in `acs --help` (src/cli.rs), the README example and settings table, DESIGN §7.2–7.4, and the config tests.\n\nDone when `aliases:` is the only accepted top-level key for aliases, a file with `hosts:` fails with the rename message (tested), `acs config` edits write `aliases:`, all docs and help say `aliases:`, and scripts/verify.sh passes. The README's explanation of the two alias shapes (added by #60 for the closed bead \"Explain the README's two alias shapes: a host list, or a mapping\") keeps its inner `hosts:` wording, but its settings-table row for the top-level `hosts` must become `aliases`, and the example must use `aliases:`.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-19T21:22:58.210576Z","created_by":"michel","updated_at":"2026-09-19T21:42:06.483303Z","closed_at":"2026-09-19T21:42:06.483185Z","close_reason":"Implemented in PR #63 (8fb5f72): the top-level key is aliases:; a hosts: file is refused with a rename message","source_repo":"beads-intake-aliases-key","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-aliases-key","compaction_level":0,"original_size":0}
{"id":"acs-n1m","title":"A sibling session on the shared master dies when another session's link is lost","description":"Consequence of acs-9n3 (PR #184, cc1ab77), prescribed by that bead and\ndocumented in DESIGN §7.1 — filed so it is tracked rather than only described.\n\nWhen a link is lost, the redial runs `ssh -O exit` on the master the lost link\nran on. That is correct when the TCP connection itself died: the master is\nholding a dead connection and everything on it is doomed anyway. It is wrong\nwhen only *our channel* died — the master and the network are fine, and any\n**other** acs session sharing that master is dropped as collateral.\n\nThe user-visible shape: two sessions to the same host, one hiccups, both\nreconnect. On the second one you lose whatever you had typed, because a redial\ndiscards typed-ahead input (§5.2). Nothing tells you why.\n\nacs-9n3 chose the blunt version deliberately: ending the master is what\nguarantees a redial gets a genuinely fresh connection rather than re-joining a\nwedged one, and the wedged-master case is the one that actually strands a user.\nGetting it wrong in the other direction — keeping a dead master — costs the\n2s fallback on every subsequent join.\n\nThe fix is to distinguish the two, which needs the `Outcome` reason plumbed\ninto `connect_and_serve` so the redial knows whether the link ended because the\ntransport died or because the session ended/was taken over. Only the former\nshould end the master.\n\nNote the blast radius is bounded by `ACS_CONTROL_PERSIST` (default 300s) and\nby how many sessions a person actually runs to one host — this is a papercut,\nnot a stranding, which is why it is P3.\n\nDone when a redial ends the master only when the evidence says the connection\nitself failed, a sibling session on the same master survives a channel-level\nloss (test it with two clients on one master), the wedged-master case still\nrecovers — acs-9n3's `a_master_that_does_not_answer_is_ended_and_the_dial_made_again`\nmust still pass — and DESIGN §7.1's note is updated to say what now happens.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-24T03:39:05.169116Z","created_by":"michel","updated_at":"2026-09-24T08:34:33.035941Z","closed_at":"2026-09-24T08:34:33.035711Z","close_reason":"Fixed in PR #197 (bec3939): Outcome::LinkLost carries a LinkEnd reason, and the master is ended unless the loss was LinkEnd::Channel — a decode error, the one case with positive proof the connection was alive. All silence and all transport deaths stay blunt","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-nau","title":"Keep the hosts: line comment when host unset removes an alias's last setting","description":"config_cmd host unset (src/config_cmd.rs:781-787): when only hosts is left, `node.value = hosts.value; node.flow = hosts.flow` copies the value but not hosts.comment or hosts.before.\n\nScenario: file `d: # dev\\n    identity_file: k\\n    hosts: # the list\\n      - host: a`, then `acs config host unset d identity_file` gives `d: # dev\\n    - host: a`; \"# the list\" is gone. README and DESIGN say comments survive edits. The d: comment and the item comments are kept correctly.\n\nDone when the comment and before-lines move with the value, with a round-trip test.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-19T21:46:59.257168Z","created_by":"michel","updated_at":"2026-09-19T23:11:24.900828Z","closed_at":"2026-09-19T23:11:24.900724Z","close_reason":"Implemented in #93 (71f6dda)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-o31","title":"Ping an alias's hosts in parallel, with a configurable 0.5 s deadline","description":"Wanted: when checking which of an alias's hosts is reachable, ping them in parallel instead of one after another, and make the ping deadline configurable, defaulting to 0.5 seconds.\n\nVerified (v0.5.0): `pick` in src/alias.rs loops over the alias's entries and pings each checked host in turn with `ping -c 1` and a fixed 2 s deadline (`ping`, src/alias.rs), stopping at the first that answers. Every host that does not answer adds up to 2 s before the next is tried: home/office/public entries where only the last answers take about 4 s to choose. It happens again on every redial (DESIGN §7.3), and nothing reachable costs 2 s × N before the error. `acs --list` with no host already runs aliases in parallel threads (src/list.rs), but each alias's hosts are still sequential.\n\nParallel, keeping the configured order as the priority: start every checked host's ping at once; take host k as soon as it has answered and every checked host before it has failed. A host with `reachability_check: false` still wins without a ping as soon as the hosts before it have failed. The result is exactly what the sequential loop would choose, in at most one deadline. Log lines (`-v`) and the \"tried …\" error keep the configured order.\n\nDeadline: a new setting, for example `reachability_timeout` (duration or milliseconds; default 0.5 s), global and probably per alias like `identity_file`, possibly with an `ACS_*` env override. Portability: macOS `ping -t` and BusyBox `-W` take whole seconds only (macOS `-W` is per-packet milliseconds; iputils `-W` takes fractions only in newer releases). So acs should enforce the deadline itself: start `ping -c 1` with a generous own timeout, and kill and reap the child when acs's deadline passes, counting that as no answer. `ACS_PING` (the test hook) keeps working.\n\nDone when the hosts are pinged concurrently with order-preserving selection, the deadline is configurable (validated, shown and edited by `acs config`) with a 0.5 s default, tests cover the selection order (a later host answering first must not beat an earlier host that answers within the deadline) and the timeout path, and DESIGN §7.2/§7.3 and the README describe it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-19T14:21:44.848126Z","created_by":"michel","updated_at":"2026-09-19T15:22:52.377615Z","closed_at":"2026-09-19T15:22:52.377487Z","close_reason":"Implemented: parallel pings with order-preserving selection and the reachability_timeout setting (default 500ms, global and per alias)","source_repo":"beads-intake-parallel-ping","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-parallel-ping","compaction_level":0,"original_size":0}
{"id":"acs-o6x","title":"Fix the flaky reconnect test that makes scripts/verify.sh non-deterministic","description":"`tests/reconnect.rs::a_client_busy_with_output_answers_the_masters_ping` fails\nintermittently under load:\n\n    assertion `left == right` failed: the link was dropped\n      left: 2\n     right: 1\n\nObserved 2026-09-23 during the acs-5em work (PR #153): one failure in a full\n`scripts/verify.sh` run on a loaded laptop, then passing on its own immediately\nafterwards and in two later full runs. The change under test was `build.rs`,\n`src/release.rs` and `src/install.rs` — nothing on the reconnect path — so this\nis the test, not a regression.\n\nWhy it matters more than one flaky test usually does: `scripts/verify.sh` is\nthis repo's *only* gate. There is no `.github/workflows` at all, so a red run\nis the single signal that stops a change landing. A gate that fails at random\nteaches people to re-run it until it is green, which is the same as not having\none — and it will eventually swallow a real failure.\n\nThe assertion counts dials, so the likely shape is a timing budget: the client\nis deliberately busy with output, and under load the master's ping is answered\nlater than the deadline allows, so the client concludes the link dropped and\nredials. Worth checking whether the test asserts on wall-clock at all, and\nwhether the timeout it depends on can be driven by the test rather than by the\nmachine's mood.\n\nDone when the test no longer depends on the host's load — either the timing is\ninjected rather than real, or the assertion is on the behaviour rather than on\nthe dial count — it fails for a genuine regression on the reconnect path (show\nthis, e.g. by reverting the relevant guard and watching it go red), and it\npasses repeatedly under artificial load (a few hundred runs, or the suite run\nalongside a busy machine).","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-23T18:39:51.293869Z","created_by":"michel","updated_at":"2026-09-23T19:36:41.172695Z","closed_at":"2026-09-23T19:36:41.172055Z","close_reason":"Fixed in PR #158 (106a2c8): the test waits for 8 answered pings on one link instead of a 3s wall-clock budget; 360/360 under the load that failed 2/120 before","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-o8h","title":"Remove the remaining wall-clock windows from tests/reconnect.rs","description":"acs-o6x (PR #158, 106a2c8) fixed one load-sensitive test in `tests/reconnect.rs`\nby waiting for an observable event — 8 answered pings on one link — instead of\nsleeping 3 s and asserting on a dial count. Three tests in the same file still\nhave the old shape, found while doing it and left alone to keep that change to\none test.\n\n`scripts/verify.sh` is this repo's only gate (there is no `.github/workflows`),\nso each of these is a way for the gate to go red at random.\n\n1. **`keys_typed_during_a_redial_are_dropped`** — the clearest one.\n   `remote.slow_dial(Some(\"1.5\"))`, then `sleep(300ms)`, then `c.send(…)`: the\n   key has to land inside a 1.5 s window that the test has already spent 300 ms\n   of before it starts. `ctrl_c_during_a_redial_clears_the_status_line` solved\n   exactly this under acs-kip with\n   `c.wait_until(\"the client is in cooked mode\", |c| c.echo_on(), T)`; the same\n   substitution fits here and removes the window outright.\n2. **`a_silent_host_is_given_up_on`** — `assert!(t0.elapsed() < Duration::from_secs(5))`\n   with `t0` taken before `c.wait(T)`, so process start, the dial, the 500 ms\n   dial timeout and exit all have to fit in 5 s. Tight on a loaded machine.\n3. **`the_escape_window_is_the_same_offline`** — a real `sleep(1000ms)` inside a\n   configured 3000 ms command-key window; 2 s of slack against a starved client.\n\nThe pattern to apply is acs-o6x's: wait for the thing the test is actually\nabout, so a stalled host only makes the test slower, never red. Where the\nassertion really is about elapsed time (2), the question is whether it can\nassert the behaviour — that the host was given up on — rather than the clock.\n\nDone when none of the three depends on the host's load, each still goes red for\na genuine regression (show it, by breaking the guard it covers), and the file\nhas no remaining `sleep` that gates an assertion.","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-23T19:36:52.937797Z","created_by":"michel","updated_at":"2026-09-23T20:35:01.928918Z","closed_at":"2026-09-23T20:35:01.928704Z","close_reason":"Fixed in PR #164 (f4f2522): the redial is gated by the test rather than a 1.5s window, the silent-host clock assertion is replaced by the behaviour, and the bell's two bytes go in one write; all four proven red by breaking their guard","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-o96","title":"Let a host entry be preferred when several of an alias's hosts answer","description":"Wanted, in the user's words: \"add functionality to a host which indicates which of the alternative is the primary one to pick if both are reachable. Something like 'prefer': true\".\n\nVerified (v0.8.1): an alias's host is chosen in src/alias.rs `pick` (DESIGN §7.3): every checked entry is pinged at once and the first entry **in configured order** that answers within `reachability_timeout` wins; an entry with `reachability_check: false` wins as soon as the entries before it have failed. The order is therefore the only way to say which host is primary, and it cannot always be set: when both files define an alias its entries concatenate, **global file first** (src/config.rs `apply`), so a user's local file cannot put its own host ahead of the global ones without redefining them.\n\nChange: a host entry key `prefer: true` (src/config.rs `HostEntry`, `HOST_KEYS`, validated like `reachability_check`). When several entries answer, a preferred one wins over an unpreferred one; among preferred (or among unpreferred) entries the configured order still decides. So selection waits for the preferred hosts' pings up to the deadline before taking an earlier unpreferred host that answered first; with no preferred entry answering, today's order-based choice is unchanged.\n\nOpen points:\n1. A preferred entry with `reachability_check: false`: it cannot answer a ping; say whether it wins outright (it is \"reachable\" by fiat) or is refused as a contradiction at load time.\n2. Several `prefer: true` entries in one alias: allowed (order among them) or an error.\n\nDone when `prefer` is parsed, validated and shown (`acs config show`, `acs config host list`, and settable with `acs config host add --prefer`), `-v` says a host was chosen because it is preferred, unit tests in src/alias.rs cover a preferred later host beating an earlier one that answered first, an unanswering preferred host falling back to order, and the unchecked case as decided, and DESIGN §7.2/§7.3 and the README describe it. The bead about choosing a host by the networks this machine is on also changes selection; state how the two combine if it lands first.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-19T20:48:48.155854Z","created_by":"michel","updated_at":"2026-09-19T21:06:50.169153Z","closed_at":"2026-09-19T21:06:50.169059Z","close_reason":"Implemented in PR #56 (5dcc31f): prefer: true on host entries, tried first","source_repo":"beads-intake-prefer-network","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-prefer-network","compaction_level":0,"original_size":0}
{"id":"acs-o9v","title":"Sign release artifacts and verify the signature before running the download","description":"Security, supply chain. SHA256SUMS and the .tar.gz are fetched from the same base URL with no signature, no attestation and no pinned key (release.rs, upgrade.rs, scripts/install.sh). Confirmed: a grep for minisign/cosign/gpg/signature/attestation over src, scripts and xtask finds nothing but a gpgsign=false line in a test. The checksum therefore proves only that the tarball matches what the same server said it should be.\n\nAttack: anyone able to write to the GitHub release (a leaked token, a compromised account, a hostile runner) replaces both files. Every `acs upgrade` verifies happily, then EXECUTES the binary (upgrade::check_runs runs it with --version) and renames it over the destination and the ~/.local/bin and /usr/local/bin links. scripts/install.sh does the same. acs then installs that binary onto every remote host the user connects to, so it fans out.\n\nDone when SHA256SUMS is signed with a key embedded in the binary (minisign/cosign) or GitHub artifact attestations are verified, the check runs before the downloaded binary is ever executed, the release script signs as part of publishing, and a test covers a good signature, a bad one and a missing one.\n","notes":"PARKED 2026-09-20, blocked on a decision only the user can make. Two things are needed before the code can be written: (1) the signing scheme — neither minisign nor cosign is installed on this machine, and GitHub artifact attestations are not available because the repo has no .github/workflows and releases are published from a laptop by scripts/release-binaries.sh; ssh-keygen -Y sign/verify is present and would fit, since acs already requires ssh; (2) which key signs releases and where its private half lives. Implementing the machinery against a placeholder key would ship a verification path that verifies nothing, which is worse than none. Everything else in the bead (verify before check_runs, sign in the release script, tests for a good, bad and missing signature) is mechanical once those are settled.","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-20T04:35:59.826019Z","created_by":"michel","updated_at":"2026-09-20T16:08:32.935034Z","closed_at":"2026-09-20T16:08:32.934962Z","close_reason":"Implemented in #140 (v0.12.0): SHA256SUMS signed with ssh-keygen -Y, checked before the sums are read","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-odd","title":"A local_forwards setting, global and per alias, so a host can always forward its port","description":"Step 5 of acs-6f5, deliberately left out of PR #155 (27cbc1e) because it is a\ndifferently-shaped change from the command-line flag and would have doubled\nthat PR.\n\n`acs -L 8080:localhost:80 box` is now the way to forward a port, and it has to\nbe retyped every time. The motivating case is standing, not ad hoc: \"this host\nalways needs its database forwarded\". That belongs in the config beside the\nother per-host facts, the way `identity_file` (acs-mbd) and `local_networks`\n(acs-9yv) already do.\n\n`local_networks` is the exact precedent to copy, and the work is mechanical:\na `Config` field, an `Option` field on `Alias`, an entry in `KEYS` and in\n`ALIAS_KEYS`, a type const beside `NETWORKS`, a `*_for(alias)` accessor, and a\nbranch in `config_cmd::typed`.\n\nTwo things to decide when taking it, rather than by omission:\n\n- **Do command-line `-L` and the setting merge, or does the flag replace the\n  list?** Merging matches how ssh treats repeated `-L`, and matches the flag\n  already being repeatable; replacing gives a way to suppress a configured\n  forward. Merging is probably right, with a documented way to say \"none\".\n- **Validation happens at config-read time**, not at dial time. `-L` is checked\n  before any ssh is spawned (`ssh::check_local_forward`); a bad spec in the\n  config should be a normal config error naming the line, not an ssh stderr\n  line mid-reconnect. Reuse the same checker.\n\nThe `Call::Session` gating from acs-6f5 applies unchanged: a configured forward\nmust reach the session ssh only, never the `Call::Side` and `Call::Batch` calls,\nor several children fight over the same local port.\n\nDone when `local_forwards` can be set globally and per alias, is validated when\nthe config is read with an error naming the offending line, is applied only to\nthe session ssh, merges with (or explicitly overrides) command-line `-L` as\ndecided above, has tests covering the config round-trip and the three `Call`\nkinds, and is documented in README and DESIGN §7.1 and §7.4 alongside `-L`.","status":"open","priority":3,"issue_type":"feature","created_at":"2026-09-23T18:58:08.668574Z","created_by":"michel","updated_at":"2026-09-23T18:58:08.668574Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-ode","title":"Make the master ping its client so a vanished client stops blocking the pty","description":"DESIGN §5.3 says each side sends PING after 3 s of silence, but src/master.rs only ever answers with Pong (master.rs:632): no timer, no Ping, and an Active conn is only dropped on read EOF/error (master.rs:553-567) or takeover. The client ignores an incoming Ping too (client.rs:853, the \"other =>\" arm). Backpressure is applied against the Active conn (master.rs:416-422, 833-846): once room_before_overwrite is 0 the pty is no longer read.\n\nScenario: laptop lid closed and powered off, no FIN reaches the host. sshd keeps the TCP session for the keepalive period (2 h by default, ClientAliveInterval 0), so proxy and Active conn live on. A build or tail -f in the session stalls after 1 MiB of output until the next attach takes over. §4.2 says a detached program never blocks, but the master cannot tell \"detached\" from \"dead client\".\n\nDone when the master pings after 3 s of silence, the client answers, the master drops a conn silent for 10 s (freeing the pty), and a test covers a client that stops reading without closing.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-19T21:46:58.474203Z","created_by":"michel","updated_at":"2026-09-19T22:03:03.158697Z","closed_at":"2026-09-19T22:03:03.158593Z","close_reason":"Implemented in #68 (b3e517f)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-okz","title":"Test build.rs's release-key validation, which no test target covers","description":"acs-ktm (#160, df58317) added `build.rs::check_release_key`, which rejects a\nmalformed `ACS_DEFAULT_RELEASE_KEY` by failing the build: it requires one line,\na key type starting `ssh-`/`ecdsa-`/`sk-`, and a base64 body of at least 16\ncharacters. The multi-line rejection is the load-bearing one — a value with an\nembedded newline would write a second allowed-signers line, which nobody means,\nand would corrupt the `cargo:rustc-env=` directive as well.\n\nNone of it is covered by a test. Build scripts are not a test target, so its\nbranches were exercised only by a by-hand `cargo build` during that pass, and\nthe evidence is a paragraph in the PR rather than something that runs again.\nThe validation can therefore rot silently: a later edit that loosens it, or a\nrefactor that stops calling it, breaks nothing any suite would notice.\n\nThe obvious shape is an integration test that shells out to `cargo build` with\na bad key and a throwaway `--target-dir`, asserting the build fails and the\nmessage names the variable. The cost is honesty about runtime: that is a slow\ntest, and it would want to be behind the same arrangement as anything else\nexpensive in this suite. An alternative worth weighing first is moving\n`check_release_key` into a tiny module that both `build.rs` and a unit test can\ninclude, so the logic is tested at unit speed and only the wiring stays\nuntested — `build.rs` already has the precedent of emitting `ACS_TARGET`.\n\nDone when each branch of the key validation is covered by a test that runs in\n`scripts/verify.sh` (empty, single well-formed key, wrong type, short body,\nembedded newline), the test fails if the validation is removed — show this —\nand whichever approach is taken, the by-hand paragraph is no longer the only\nrecord that the check works.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-23T19:46:28.406292Z","created_by":"michel","updated_at":"2026-09-25T22:49:34.911427Z","closed_at":"2026-09-25T22:49:34.911095Z","close_reason":"Done in PR #212 (9ce69b0): the whole decision moved to src/release_key.rs, include!d by build.rs and compiled under cfg(test) by the crate, so all five branches are unit-tested. Empty was in main() not the check, so extracting the check alone would have left that branch untested","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-ome","title":"Send Ctrl-L on reconnect to refresh the screen, configurable per alias","description":"Wanted, in the user's words: \"when reconnecting to a session, send a Ctrl-L to refresh the screen first thing, unless disabled in the configuration, globally or per alias.\"\n\nToday (DESIGN §5.2, §5.4): a fresh attach or a resume gap clears the local screen and the master SIGWINCHes the foreground group (src/master.rs `resize(.., force_redraw)`); a lossless resume redraws nothing unless a status line was shown while offline, and then the client sends two RESIZE frames (src/reconnect.rs `on_welcome`). Nothing sends Ctrl-L. Ctrl-L (0x0c) is input to the program, not a terminal operation: shells and most TUIs repaint on it, but a program reading raw input receives a form feed. That is why the off switch matters.\n\nConfig: a global boolean (for example `redraw_on_reconnect`, default true, next to `command_bell` in `KEYS`/`BOOLS` in src/config.rs) and the same key on an alias (`ALIAS_KEYS`, the alias-mapping form that `identity_file` introduced). The alias value wins over the global one. Probably also an `ACS_*` env override, as `ACS_COMMAND_BELL` has.\n\nOpen points: whether \"reconnecting\" means the automatic resume after a drop, a re-attach by a new client (`acs host session`), or both; how it sits with the existing SIGWINCH / double-RESIZE redraw (replace it or add to it); and sending it right after WELCOME as INPUT, so it goes through the resend/ACK path and is never typed twice.\n\nDone when the client sends Ctrl-L after reconnecting unless disabled, both config levels validate and `acs config` shows and edits them, tests cover the precedence and the on/off cases, and DESIGN §5.2/§5.4/§7.2 and the README describe it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-19T02:38:58.404114Z","created_by":"michel","updated_at":"2026-09-19T03:17:33.896913Z","closed_at":"2026-09-19T03:17:33.896816Z","close_reason":"Implemented with tests: Ctrl-L on any attach to an existing session, after resent input, never into an open paste; redraw_on_reconnect global and per alias, ACS_REDRAW_ON_RECONNECT (PR #32)","source_repo":"beads-intake-redraw-picker","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-redraw-picker","compaction_level":0,"original_size":0}
{"id":"acs-ov1","title":"Guard against tcgetpgrp returning 0 so hard_kill cannot kill the master","description":"Linux `tiocgpgrp` on a pty *master* fd returns `pid_vnr(real_tty->ctrl.pgrp)`, which is 0 when the slave has no foreground group (after TIOCNOTTY, after a hangup, or during the leader's exit between `disassociate_ctty()` and reaping). `hard_kill` (src/master.rs ~L886-888) only filters `p != ch.pid`, so `kill(-0, SIGKILL)` == `kill(0, SIGKILL)` signals the master's own process group — the master is a session leader after setsid, so it dies and takes the user's session with it. `start_kill` (~L871) and `resize` (~L772) hit the same value harmlessly. Rare (child ignores SIGHUP and dropped its ctty, or exits at the 3 s deadline) but the consequence is total.\n\nDone when every tcgetpgrp result is filtered with `p > 0`, with a unit test on the filter.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-18T15:14:21.580009Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.363019Z","closed_at":"2026-09-18T15:39:58.362941Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-ovq","title":"Stop trusting the self-asserted identity and record attaches, takeovers and kills","description":"Security, impersonation and no audit trail. client.rs takes the identity from ACS_IDENTITY with no validation beyond non-empty, and the master compares it as a string to decide whether a takeover is the same person's second terminal. Verified by reading both.\n\nAttack, inside the one-account model DESIGN §4.5 describes: person B sets ACS_IDENTITY=alice@laptop and the master treats B's attach as alice's own, taking over SILENTLY with no prompt, while acs list's WHO column and every BUSY message name alice. Sending an EMPTY identity is worse: the takeover comparison short-circuits on non-empty, so while B is attached anyone takes over silently and WHO shows a dash, leaving no mark at all. The only record is an mlog! that writes nothing unless ACS_MASTER_LOG is set, and last_identity is overwritten rather than appended to.\n\nThe design accepts no security boundary here, but it does claim the identity mechanism prevents accidents, and it does not survive even trivial intent. Follows acs-5v9.18, which built the mechanism.\n\nDone when an empty identity cannot suppress the takeover prompt, the identity is bounded and control-free, and every attach, takeover and kill is appended unconditionally to a per-session record with the peer uid and pid, so the shared-account model has a trace even without a boundary. Tests for the empty-identity path and the trail.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-20T04:36:00.277351Z","created_by":"michel","updated_at":"2026-09-20T06:58:07.733925Z","closed_at":"2026-09-20T06:58:07.733816Z","close_reason":"Implemented in #129: an empty identity matches nothing, and every attach/takeover/kill is recorded","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-p46","title":"ACS_DIAL_TIMEOUT_MS sets the first connection and the redial deadline together","description":"The defaults differ 4:1 for a stated reason — **120 s** for a first connection\n(which may prompt for a passphrase, touch a hardware key, or install acs on the\nremote) against **30 s** for a redial (which should give up and back off). But\n`ACS_DIAL_TIMEOUT_MS` sets both, so anything that shortens one shortens the\nother.\n\nThe consequence acs-ryz hit: a test that wants a short *redial* deadline must\nalso shorten the *first* dial, and the first dial is the one that has the most\nwork to do. That is the residual window in\n`a_redial_into_a_silent_host_returns_to_the_backoff`, which acs-ryz could only\nwiden (4 s to 10 s) rather than remove, and which still failed 9 times in 63 at\nextreme load.\n\nIt is not only a test problem, though that is where it shows. A user who\nshortens the dial timeout because their redials hang too long also shortens the\nwindow their first connection has to accept a hardware-key touch — which is the\none dial that genuinely needs a human.\n\nTwo candidate fixes: honour the same 4:1 ratio when the variable is set, so one\nknob keeps its meaning for both; or add a second knob and let the variable set\nonly the first dial. The ratio is less to explain and cannot be set\ninconsistently; two knobs are more honest about there being two things.\n\nDone when shortening the redial deadline does not shorten the first\nconnection's, whichever way it is spelled, the README environment table and\nDESIGN agree, and `a_redial_into_a_silent_host_returns_to_the_backoff` can use\na short redial deadline without giving its first dial an unrealistic one.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-24T07:45:02.791433Z","created_by":"michel","updated_at":"2026-09-24T07:45:02.791433Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-p4u","title":"acs's own writes to the terminal can land inside a sequence the program left open","description":"acs-z22 (PR #201, da1368c) held `-v` notes to a boundary of the program's\nstream. Three of acs's **own** writes to the terminal have the same hazard and\nno boundary check:\n\n- `reconnect::show_status` / `clear_status` — the bottom-row status line;\n- `client::leave`'s `observer.reset_sequence()`;\n- the `\\x1b[H\\x1b[J` a Fresh or Gap attach writes.\n\nThe window is narrower than acs-z22's but real, and the status line is the\nworst of the three: it goes up **after a link is lost**, and the decoder only\ndelivers whole frames — the one that was cut is discarded, and the last one it\ndid deliver may well end mid-CSI or mid-OSC. So the explanation the frozen\nterminal is owed can itself be what corrupts it.\n\n**Why this is not simply acs-z22 applied again, and is the harder problem:** a\nnote can wait, and acs-z22's whole fix is waiting. A status line cannot — it\nexists to explain a terminal that has just stopped responding, and holding it\nfor a boundary that may never come is the opposite of what it is for.\n\nThat leaves aborting the program's unfinished sequence first (`ESC \\`), which\nis a real design decision rather than a mechanism: it throws away the bytes\nthat would have completed the sequence. And note what happens next — a\n`Resumed` attach really does deliver the rest of that sequence afterwards, so\naborting turns the remainder into printed junk on screen. Doing nothing leaves\na corrupted sequence; aborting leaves visible garbage. Neither is obviously\nright, which is why acs-z22 documented the hazard in DESIGN §7 rather than\nguessing.\n\nThings that might make it tractable: the observer already knows *what kind* of\nsequence is open, so a CSI could be closed differently from an OSC; and the\nstatus line's `ESC 7` / `ESC 8` save-restore may already limit the damage for\nsome sequence kinds — check before assuming it does not.\n\nDone when acs's own terminal writes cannot corrupt a sequence the program left\nopen, or the decision to accept it is recorded in DESIGN with the reasoning and\nthe cases where it shows; either way a test should demonstrate what a status\nline drawn mid-sequence actually does to the screen today.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-24T18:21:40.471661Z","created_by":"michel","updated_at":"2026-09-25T21:30:28.273116Z","closed_at":"2026-09-25T21:30:28.272866Z","close_reason":"Fixed in PR #210 (ad04421): acs ends the program's open sequence before its own write and hands it back after, per kind — CSI, bare ESC and partial UTF-8 replayed exactly; OSC/DCS/APC ended but not replayed because the body may already have acted. leave and the attach clears end only; the signal handler opens with an unconditional ST","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-pcs","title":"Say that no configuration file exists when acs list finds none","description":"Wanted: when there is no acs configuration at all, `acs list` should say so, instead of the message for a configuration without aliases.\n\nVerified (v0.8.0, installed binary with throwaway XDG_CONFIG_HOME / ACS_GLOBAL_CONFIG): with no config files, with an empty file, and with an empty `hosts:`, `acs list` prints the same line and exits 2 without dialling: \"acs: no host aliases in the configuration: list one host with acs list <host>, or add an alias with acs config host add <alias> <host>\". Both paths use it: src/list.rs `run_all` (piped table) and src/pick.rs `every_host` (terminal menu), via `list::NO_ALIASES`. With no files the wording is misleading, since there is no \"the configuration\".\n\nChange: when no configuration file exists (`Config.files` is empty; `config::global_path()` is /etc/acs/config.yaml or `ACS_GLOBAL_CONFIG`, `config::local_path()` is `$XDG_CONFIG_HOME/acs/config.yaml` or ~/.config/acs/config.yaml), print a distinct message naming both paths it looked in, with the same hints, e.g. \"acs: no configuration (looked for /etc/acs/config.yaml and ~/.config/acs/config.yaml): list one host with acs list <host>, or add an alias with acs config host add <alias> <host>\". Keep today's message when a file exists but defines no aliases. The exit code stays 2 (usage) in both cases, and both the table and the menu path use the new message.\n\nDone when the two cases print their own messages from both paths, with unit or integration tests for no files vs. a file with no aliases (tests/ already sets ACS_GLOBAL_CONFIG and XDG_CONFIG_HOME for config tests), and DESIGN §7.3 (\"Every alias at once\") mentions the no-configuration case.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-19T17:30:01.613813Z","created_by":"michel","updated_at":"2026-09-19T18:32:04.122834Z","closed_at":"2026-09-19T18:32:04.122726Z","close_reason":"Implemented in PR #52 (a418b49): acs list names the files it looked for when there is no configuration","source_repo":"beads-intake-no-config-msg","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-no-config-msg","compaction_level":0,"original_size":0}
{"id":"acs-pgn","title":"Report per-phase connect timings under -v","description":"Measure before optimising connect latency. Under -v, print how long each phase of a connection took: alias resolved (pings, lookups), ssh spawned, ACS-READY marker seen, WELCOME received, first output byte. Applies to the first connection, the menu path (_proxy --pick) and every redial. Today -v shows none of this, so it is unknown whether the ssh handshake, remote startup files or the reachability deadline dominates on real hosts.\nTouches client.rs (dial, serve), pick.rs, reconnect.rs. Done when -v prints one timing line per phase and a test asserts the lines appear in order.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-23T19:28:50.005533Z","created_by":"michel","updated_at":"2026-09-23T22:23:06.550032Z","closed_at":"2026-09-23T22:23:06.549854Z","close_reason":"Landed in #172: -v prints one timing line per connect phase (alias resolved, ssh spawned, ACS-READY seen, session list received, WELCOME received, first output byte) for the first connection, the menu path and every redial; tests/timing.rs asserts the order.","source_repo":"beads-intake-latency","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-latency","compaction_level":0,"original_size":0}
{"id":"acs-pho","title":"Fix flaky acs_list_in_a_terminal_ends_and_creates_on_the_rows_host in tests/menu.rs","description":"The `tests/menu.rs` integration test `acs_list_in_a_terminal_ends_and_creates_on_the_rows_host`\nfailed once in a full `cargo test --workspace` run: after sending `n` on nas's row,\n`e.nas.session_exists(\"1\")` was false and the screen had fallen back to the menu header\n(\"\\x1b[Hacs: detached sessions on every host\").\n\nIt passes in isolation (3/3) and the full suite went green on rerun, so this is timing-\ndependent. The test sequences on explicit `std::thread::sleep` calls (20ms/200ms in the\nsame file) and on a race in which \"the cursor follows whichever host answered first\"\n(the comment on the `> 1  nas` cursor assertion).\n\nFix: make the menu interaction deterministic in the test — deadline-based waits /\nharness coordination instead of sleeps, and/or deterministic fake-host answer ordering.\nIf the failure is a real race in the menu code rather than the test, fix that instead;\ncapture the full screen bytes from a failing run for the diff.","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-19T21:12:07.671832Z","created_by":"michel","updated_at":"2026-09-19T23:36:07.684448Z","closed_at":"2026-09-19T23:36:07.684285Z","close_reason":"Implemented in #101 (2dd175d)","source_repo":"menu-flake","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/menu-flake","compaction_level":0,"original_size":0}
{"id":"acs-pjk","title":"Let scripts/test_linux.sh run one test file instead of the whole suite","description":"`scripts/test_linux.sh` runs everything: the full suite, the multiuser pass as\nroot, and now the netns pass under CAP_NET_ADMIN. There is no way to run one\ntest file.\n\nThat matters because the script is now the **only** place three things are\nexercised at all — Linux `ls` behaviour in the prelude (acs-gov), the\nroot-populated system-wide install (acs-6w9), and the kernel netlink watcher\n(acs-4i2). Anyone debugging one of those pays the whole suite per iteration.\n\nThe acs-4i2 pass hand-reconstructed the script's `docker run` to iterate on\n`tests/netns.rs`, including re-deriving the `cksum`-based target-volume name.\nThat is both slow and a good way to accidentally run with different flags from\nthe real thing and draw a wrong conclusion.\n\nA pass-through argument — `scripts/test_linux.sh --only netns`, or simply\nforwarding extra arguments to the inner `cargo test` — would cover it. Points\nto settle: whether `--only` should also skip the image build and volume setup\nwhen they are already current (the acs-gb4 staleness stamp is the precedent\nfor doing that honestly), and whether the privileged passes stay opt-in by\nenv var as they are now.\n\nSmall and purely developer-facing, hence P4 — but it is paid by every person\nwho touches the three things only this script covers.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-24T06:00:32.210848Z","created_by":"michel","updated_at":"2026-09-24T06:00:32.210848Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-q4f","title":"Create the config temp file 0600 and bound YAML nesting depth","description":"Security, two small local issues in config handling, filed together because they share the config path (say so if they want splitting).\n\nconfig_cmd::write creates its temp file with std::fs::write, so it lands at the process umask (typically 0644) in the config directory and only then copies the real file's permissions onto it. If the config file does not exist yet, the new file keeps 0644 permanently. The content is host names, login names and identity_file paths: not secrets, but a private host inventory on a shared machine.\n\nyaml.rs Flow::value and block_node/seq/map recurse with no depth limit, so a config with a few thousand nested brackets, which fits on one line, overflows the stack. With panic = abort in the release profile that is an immediate abort rather than an error.\n\nDone when the temp file is created 0600 before any content is written and widened only to match an existing file, the parser rejects nesting past a fixed depth with a normal config error, and both have tests.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-20T04:36:01.808966Z","created_by":"michel","updated_at":"2026-09-20T06:58:14.494273Z","closed_at":"2026-09-20T06:58:14.493976Z","close_reason":"Implemented in #133: config created 0600, and both parsers bounded at 64 deep","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-q8e","title":"Write acs config edits through a symlinked config instead of replacing the link","description":"config_cmd::write() (src/config_cmd.rs:1014-1025) writes a temp file and renames it over the path. read_doc follows a symlink, but rename replaces the link itself.\n\nScenario: ~/.config/acs/config.yaml -> ~/dotfiles/acs.yaml (stow, chezmoi). `acs config set update_check false` reports success; afterwards config.yaml is a regular file with the new content and ~/dotfiles/acs.yaml still says update_check: true. Same for a hard link: the other name keeps the old content. DESIGN §7.4 promises \"replaces the file (atomically, keeping its mode)\".\n\nDone when write resolves the path (fs::canonicalize) before the temp-and-rename, and a test edits a symlinked config and asserts the target changed and the link survived.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-19T21:46:58.680257Z","created_by":"michel","updated_at":"2026-09-19T22:21:27.481512Z","closed_at":"2026-09-19T22:21:27.481422Z","close_reason":"Implemented in #75 (5251a06)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-q9t","title":"Fix payload install failing when the login shell prints to stdout","description":"Step 1 of the payload install plan (`gzip -dc > tmp`, src/install.rs ~L114) prints nothing, and `side_call` (src/install.rs ~L150) errors with \"unexpected reply while installing\" whenever stdout is non-empty and no line is exactly `ok`. Any `.bashrc` echo or motd script that writes to stdout for non-interactive sessions — the very noise the ACS-READY marker was designed to tolerate (DESIGN §3) — therefore fails the macOS → Linux self-install after the upload succeeded, leaving `acs.new.<token>` behind. The self-copy path only passes because the finisher happens to print `ok`.\n\nDone when the upload step is judged on exit status alone (or prints its own marker) and tests/install.rs has a case with a chatty login shell.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-18T15:14:21.073925Z","created_by":"michel","updated_at":"2026-09-18T15:28:29.489556Z","closed_at":"2026-09-18T15:28:29.489444Z","close_reason":"Fixed with regression tests (bugs-install branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-qis","title":"Under -v, log why each of an alias's hosts was not chosen","description":"User's words: \"when verbose mode is selected '-v' log why a particular host is not selected as a target for the alias\".\n\nToday `-v` explains the **winner** and nothing else. `alias::pick` logs why the chosen entry was chosen, but most of the losing entries produce no line at all, so from the output you cannot tell whether a host was beaten, skipped, or never considered.\n\n## What is logged now (v0.13.0)\n\n`client::resolve_alias` (src/client.rs:192) passes a `log` closure into `alias::resolve`, printed as `acs: …` notes when `-v` is given. The lines it can emit, all in `src/alias.rs`:\n\n- `:66` `<alias>: logging in as <u>, from the command line`\n- `:168` `<alias>: <host> is on the local network <net>` (`prefer_local_network`)\n- `:170` `<alias>: this machine is on <net>, so <host> is local` (`local_networks`, acs-9yv)\n- `:195` `<alias>: using <dest> (reachability_check is off, …)`\n- `:211` `<alias>: <host> answers ping, using <dest> (…)`\n- `:217` `<alias>: <host> does not answer ping within <timeout>`\n- the error `no host for '<alias>' is reachable (tried a, b, c)`\n\nSo exactly **one** \"why not\" line exists — `does not answer ping` — and it only covers entries tried *before* the winner.\n\n## The three silences\n\n1. **Everything ranked after the winner.** The trial loop (`src/alias.rs:180`) returns as soon as a host is taken (`:196`, `:215`). Entries ranked below it are never visited, so they are never mentioned. With four hosts where the first answers, `-v` says nothing at all about the other three — a host missing from the output looks identical to a host that is not configured.\n\n2. **Entries pruned before any ping.** `candidates` (`src/alias.rs:131`) truncates the order at the first entry with `reachability_check: false`, because nothing after an unchecked host can ever be chosen. Those entries are not pinged and not logged. This is the most confusing case: the host is configured, reachable, and silently unreachable-by-construction.\n\n3. **Why an entry ranked where it did.** Rank is local, then `prefer`, then configured order. `-v` says why a host *is* local or preferred, never why it is not — there is no line for \"on no local network\", \"not preferred\", or \"listed third\".\n\n## Wanted\n\nUnder `-v`, every entry of the alias accounts for itself: taken, beaten, not answering, or never eligible — with the reason. Roughly:\n\n    devbox: devbox.lan does not answer ping within 500ms\n    devbox: devbox.vpn answers ping, using devbox.vpn (preferred)\n    devbox: devbox.example.com not tried: devbox.vpn was chosen first\n    devbox: nas.lan not tried: it is listed after devbox.old, whose\n            reachability_check is off\n\nScope decisions for whoever takes it:\n\n- **Where the lines come from.** The pruned set is known at `candidates`, before any ping; the \"chosen first\" set is known only once the loop returns. Emitting both means either logging after the choice (one pass over `order`, easy) or threading the reason out of `candidates`. The first is likely simpler and keeps the configured order in the output, which the existing lines already preserve.\n- **Ordering.** Today's lines come out in rank order as the loop walks. Adding the untried ones at the end keeps that; interleaving them in configured order reads better. Pick one and say so.\n- **`-v` vs `-vv`.** `args.verbose` is a `u8` and `-v` is repeatable (src/cli.rs:181), but nothing currently distinguishes the levels — `let verbose = args.verbose > 0` (src/client.rs:193). Per-host \"not tried\" detail is a plausible first use of `-vv` if the single-level output gets noisy for a long alias. Decide rather than default into it.\n- **Does the failure path change?** When nothing answers, every entry is already tried and logged, and the error names them (`tried a, b, c`). That path is fine; this is about the success path.\n\n## Done when\n\n`acs -v <alias>` accounts for every entry of the alias — one line each, naming the entry and why it was not used — including the entries ranked after the chosen one and the entries pruned for sitting behind a `reachability_check: false` host. Unit tests in `src/alias.rs` assert the full log for at least: a winner with hosts ranked after it, an alias pruned by an unchecked entry, and the existing no-host-answers path (unchanged). README's `-v` line and DESIGN §7.3 describe what the output now covers.\n\nExtends acs-sia and acs-9yv (which added the locality lines this builds on).\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-23T18:05:01.552465Z","created_by":"michel","updated_at":"2026-09-23T18:25:33.810144Z","closed_at":"2026-09-23T18:25:33.809989Z","close_reason":"Implemented in PR #151 (2ca89da): under -v every entry of an alias accounts for itself","source_repo":"beads-intake-verbose-why-not","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-verbose-why-not","compaction_level":0,"original_size":0}
{"id":"acs-qjx","title":"Accept a tab after a YAML key and strip a UTF-8 BOM in the config parser","description":"yaml::split_key (src/yaml.rs:379) only accepts a colon followed by a space or end of line; YAML 1.2 also allows a tab. `install_on_remote:<TAB>false` is not recognised as a key, block_node (yaml.rs:245-264) returns a root scalar, and config.rs:288-296 reports \"config.yaml:1: expected settings as 'key: value' lines, found a single value\", pointing nowhere near the tab. The parser detects tab indentation (yaml.rs:170-172) but not this. Related: a UTF-8 BOM is not stripped (yaml.rs:166-173), giving \"unknown setting '\\u{feff}install_on_remote'\" for a file every other YAML reader accepts.\n\nDone when a tab after the colon separates the key, a leading BOM is skipped, and tests cover both.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-19T21:46:59.191282Z","created_by":"michel","updated_at":"2026-09-19T23:06:43.815580Z","closed_at":"2026-09-19T23:06:43.815489Z","close_reason":"Implemented in #91 (bca5b96)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-qrn","title":"Pop the title stack and status line when the session ends during an outage","description":"After a link drop, `offline()` (src/reconnect.rs ~L207-219) draws the bottom status row and pushes the xterm title (`CSI 22;0 t`), setting `status_shown`. The pop (`clear_status`) only runs from `on_welcome` and the offline Detach path. If the redial gets `Error{NO_SESSION}`, `Exit`, or `Takeover` before any WELCOME (src/client.rs ~L487-529), `serve` returns and `run()` calls only `leave()` (~L301), which never pops. The terminal keeps the \"acs: host — connection lost\" title and the inverse-video status row after acs exits — a DESIGN §5.4 violation. tests/reconnect.rs only covers the detach-while-down path.\n\nDone when `leave()` (or those return paths) calls `clear_status` whenever `status_shown`, with a test for session-end-during-outage.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-18T15:14:21.335251Z","created_by":"michel","updated_at":"2026-09-18T15:48:09.572705Z","closed_at":"2026-09-18T15:48:09.572611Z","close_reason":"Fixed with regression tests (bugs-client branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-qty","title":"Drop keys typed during a redial instead of echoing and delivering them","description":"The client suspends raw mode before every dial (src/client.rs:576-578) so ssh can prompt. RawMode::suspend/resume (src/tty.rs:112-126) use sys::tcsetattr = TCSADRAIN (sys.rs:246-249), which never discards pending input. In cooked mode ICANON+ECHO are on: anything typed during the dial (ConnectTimeout plus auth, up to 30 s on a redial) is echoed at the cursor, corrupting a screen a lossless resume will not repaint, and sits in the canonical line buffer; when RawMode::resume() runs on WELCOME that line becomes readable and queue_input sends it to the program. reconnect::offline drops keys during the wait, but the dial phase does not. ISIG is also on, so Ctrl-C during the dial kills the client via tty::on_fatal, restoring termios but not popping the title stack or blanking the status row.\n\nContradicts DESIGN §5.2 \"keys typed while the client knows the link is down are dropped\".\n\nDone when a redial only leaves raw mode when ssh really needs the tty (or resumes with TCSAFLUSH and discards typed-ahead), Ctrl-C during a dial cleans up the status line, and a test feeds input during a redial and asserts none reaches the pty.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-19T21:46:58.538858Z","created_by":"michel","updated_at":"2026-09-19T22:08:22.986101Z","closed_at":"2026-09-19T22:08:22.986015Z","close_reason":"Implemented in #70 (abf94e8)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-r1k","title":"List the timer knobs somewhere, and stop the netwatch tests reading the environment","description":"Two small pieces of hygiene from acs-0n8 (PR #199, 491c637).\n\n**1. There are now six undocumented timer knobs and no list of them.**\n`ACS_BACKOFF_MS`, `ACS_PING_MS`, `ACS_DEAD_MS`, `ACS_NETCHECK_MS`,\n`ACS_DIAL_TIMEOUT_MS` and now `ACS_EARLY_MS`. None is in the README environment\ntable — deliberately, since they are internal — but there is also no single\nplace that names them together, so the only way to find them is to grep for\n`env_ms`. Each new bead adds one and nobody can see the set.\n\nA short table in DESIGN §5.3 would do it: name, default, what it bounds. That\nalso makes the interactions visible, which matter — acs-p46 records that\n`ACS_DIAL_TIMEOUT_MS` sets two deadlines that differ 4:1 by default, and that is\nexactly the kind of thing a table surfaces and prose hides.\n\n**2. The netwatch unit tests set process-wide environment variables.**\n`ACS_NETWATCH_NETS` is read from the environment by a function called deep\ninside the library, so two tests in the same binary can race each other. acs-0n8\nadded a `STANDIN` mutex and a `Standin` helper so its own two tests cannot, but\nthe hazard stays armed for the next test anyone adds to that binary — and a\nmutex is a reminder, not a fix.\n\nTaking the network source as a field on the watcher, rather than reading it from\nthe environment at the point of use, removes the class. The env var stays as the\nway to set it; only the reading moves to construction.\n\nDone when the timer knobs are listed in one place with their defaults and what\neach bounds, and a netwatch test can set its network source without a mutex\nbecause the source is no longer process-wide state.","notes":"Two more surfaces for the same problem, from acs-mq0 (PR #203, 33e6fdc).\n\n**1. `acs --help` understates what is configurable.** `src/cli.rs`'s help text\nlists four environment variables — `ACS_DEFAULT_SESSION`, `ACS_IDENTITY`,\n`ACS_ESCAPE_KEY`, `ACS_ESCAPE_TIMEOUT_MS` — while the README documents about\ntwenty and several more exist only in tests. This is a distinct surface from\nthe timer-knob list this bead is about: the docs being incomplete is one\nproblem, the *program's own help* being incomplete is another, and the help is\nwhat someone reads first. Worth deciding whether help should list everything,\nlist a category, or point at the README.\n\n**2. `PENDING_MS` (100 ms) in `src/keys.rs`** — how long an incomplete escape\nsequence is held before being released as typed — is another bare constant of\nthe same shape. Noted for completeness only: it is local-stdin only, so no link\nlatency reaches it, and the acs-mq0 pass found nothing wrong with the value.\nInclude it in the table if one is written; it does not need a knob.\n\nNote acs-mq0 deliberately did **not** add a seventh knob, on the strength of\nthis bead: it made one existing setting cover both halves of the gesture rather\nthan introducing `ACS_COMMAND_TIMEOUT_MS`. That is the precedent worth keeping\n— the table this bead asks for is partly so the next person can see the cost of\nadding one.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-24T17:34:12.357706Z","created_by":"michel","updated_at":"2026-09-25T17:32:38.644951Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-r3m","title":"Restore the terminal title after a reconnect succeeds","description":"User report: when the connection is lost, acs changes the terminal title to \"acs - <alias> - connection lost - reconnecting...\" — but once the connection is recovered it never restores the title to what it was (if possible), or at least removes the \"connection lost - reconnecting\" part.\n\nWhat the code does today: `show_status` (src/reconnect.rs ~L437) pushes the title on the xterm title stack (`CSI 22;0 t`) once, then sets `OSC 2 \"acs: <host> — <msg>\"`; `clear_status` (~L456), called from `on_welcome` after a resume, pops it with `CSI 23;0 t` (DESIGN §5.4). So restoration relies entirely on the terminal supporting XTWINOPS title push/pop. Terminals that ignore 22/23 (e.g. macOS Terminal.app, among others) keep acs's \"connection lost\" title forever — the likely cause. Worth confirming on the user's terminal first, and checking that the WELCOME path really reaches `clear_status` in every resume/re-attach variant (fresh attach after `instance` is lost, persist mode, network-changed redial).\n\nSuggested fix: have the output observer (src/modes.rs, which already parses OSC strings) remember the last OSC 0/2 title the remote program set; on recovery, after the pop, re-emit that title explicitly — or, if none was ever seen, set a neutral one (e.g. \"acs: <host>\") rather than leaving the outage text. The pop stays for terminals that honour the stack.\n\nDone when: after a successful reconnect the title is either the program's last title or at least no longer says \"connection lost\", on terminals without title-stack support; a test drives drop → reconnect and asserts the bytes written include the restored title after `CSI 23;0 t`; DESIGN §5.4 updated to describe the fallback.\n","status":"open","priority":2,"issue_type":"bug","created_at":"2026-09-25T20:46:24.027154Z","created_by":"michel","updated_at":"2026-09-25T20:46:24.027154Z","source_repo":"beads-intake-title-restore","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-title-restore","compaction_level":0,"original_size":0}
{"id":"acs-rip","title":"Cap remote-driven buffers so a hostile host cannot exhaust client memory","description":"Security, denial of service. Three client buffers grow on remote data with no ceiling. proto.rs MarkerScanner::push bounds noise at 64 KiB but only drains buf when a newline was seen, so output containing NO newline grows without limit for the whole dial window (120 s on a first connection, 30 s on a redial) and re-scans the unfinished line on every read, which is also quadratic. install.rs side_call reads the remote's stdout to the end with no cap and no deadline. list.rs and pick.rs accumulate STATUS_REPLY frames in a Vec with no cap on count or total bytes, each up to the 1 MiB frame limit.\n\nMAX_FRAME bounds one frame, not the stream. A malicious or merely broken remote can drive the client to OOM, gigabytes on a fast link.\n\nDone when each of the three has a byte cap that ends the operation with a clear error, and tests drive each past its cap.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-20T04:36:00.565455Z","created_by":"michel","updated_at":"2026-09-20T05:40:13.323284Z","closed_at":"2026-09-20T05:40:13.322957Z","close_reason":"Implemented in #119: scanner line cap, install reply cap, listing session cap","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-rxw","title":"Only prune version directories under $HOME/.local/share/acs","description":"`versions_root()` (src/prune.rs ~L31-38) is `parent(parent(current_exe))` whenever the parent dir is named after the version, with no check that it lies under `$HOME`. A binary exec'd from the prelude's second candidate, `/usr/local/lib/acs/<v>/acs` (src/ssh.rs ~L107), therefore touches `/usr/local/lib/acs/<v>`, writes `/usr/local/lib/acs/.pruned`, and `remove_dir_all`s other users' system-wide versions older than 30 days that are not in *this user's* live set. Silent for a non-root user (errors ignored) but effective for root or wherever `/usr/local/lib` is user-writable (Homebrew on Intel macOS). DESIGN §8 says acs never writes there.\n\nDone when pruning only runs with `root == $HOME/.local/share/acs`, with a test that a system-path binary prunes nothing.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-18T15:14:21.641811Z","created_by":"michel","updated_at":"2026-09-18T15:28:29.495718Z","closed_at":"2026-09-18T15:28:29.495641Z","close_reason":"Fixed with regression tests (bugs-install branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-ryz","title":"Three wall-clock windows remain inside reconnect tests, and they go red under contention","description":"acs-o6x (#158), acs-o8h (#164) and acs-7wu (#174) removed the wall-clock gating\n*around* assertions in `tests/reconnect.rs`. Three windows remain *inside*\ntests, and the acs-iyq pass (PR #178) hit them: one run of\n`cargo test --test reconnect --test shared --test timing --test persist` went\nred on four tests at once when the machine was ~3.5x contended (the reconnect\nbinary took 34.8 s against its usual 10 s). The identical command was green\nimmediately after, and six further rounds were green.\n\nThe windows, each a stimulus that must complete inside a configured timeout:\n\n- **The 4 KiB ring must be overwritten inside the 700 ms backoff** —\n  `output_lost_to_a_small_ring_is_a_gap_and_redraw` and\n  `modes_of_a_restarted_session_are_reset`.\n- **The second client must be up inside 3 s** —\n  `modes_of_a_restarted_session_are_reset`.\n- **`d` must follow the BEL inside the 2 s command-mode expiry** —\n  `the_bell_rings_while_the_link_is_down_too` and\n  `the_escape_window_is_the_same_offline`.\n\nThese are not the same shape as what acs-7wu fixed. There the test waited a\nfixed time and then asserted; here the *client* is given a deadline and the\ntest must get the machine to do something inside it. The fix is correspondingly\ndifferent: either drive the deadline from the test (as acs-o6x did with\nACS_DEAD_MS) so contention cannot eat it, or make the stimulus event-driven so\nthe deadline only starts once the test knows the client is ready.\n\nNote acs-iyq's `refuse_one_dial()` restores each of these to the window it had\non main — the change did not widen them. And the failures were not captured\nbefore the re-run, so the messages above are reconstructed from which tests\nfailed and what their windows are.\n\nContext worth knowing: this machine was also carrying six orphaned\n`acs 0.13.0 _master` processes from older test runs, ~6 h old, which is part of\nwhy contention was reachable at all. Clean environment first before concluding\na run is telling you something.\n\nDone when none of the three windows can be lost to host contention, each test\nstill goes red for the regression it covers (show it), and the suite survives\nthe cold full-parallel `cargo test --workspace` that produced the red runs.","notes":"A sixth test of this class, from acs-6w9 (PR #190, ae86191):\n`reconnect::a_network_change_ends_a_frozen_link_without_the_dead_timeout`\nfailed once with `tests/common/mod.rs:674: only 1 connections`, inside a\nverify.sh run that was simultaneously running clippy. It passed alone\nimmediately after and in every later run.\n\nNote this test is new — it arrived with acs-ft1 (#182) — so it was written\nafter this bead was filed and is not one of the three in the description.\nIts shape is the same family: it SIGSTOPs the transport with ACS_DEAD_MS at\n60000 so that only the netcheck can end the link, then waits for a second\nconnection. Under contention the question is whether the wait is a condition\nor a fixed duration; `wait_connections` is the absolute-count API this bead\nalready flags.\n\nWorth checking together with the wait_connections rework, since that is the\nsame call site.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-24T01:15:07.602538Z","created_by":"michel","updated_at":"2026-09-24T07:44:07.776460Z","closed_at":"2026-09-24T07:44:07.776286Z","close_reason":"Fixed in PRs #194 (50fc179) and #195 (3ce7f21): wait_connections(n) removed in favour of connections()/wait_more_connections(base,extra) so the class cannot be rewritten; six tests moved from windows to events; 15 -v waits anchored on the session announcement. Under load: main 8 failures in 72 runs, after 2 in 72 then 0 in 48","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-s0g","title":"Pick a detached session from a menu on a plain acs <host>","description":"Wanted, in the user's words: change the behaviour of a plain `acs [<user>@]<alias | hostname>`. Instead of always starting a new session, check for detached sessions on the host. If there are none, create a new one. If there are, present a list of the detached sessions, like --list, and allow selecting one with a single press of its number, or by moving a cursor with up/down (and the vim equivalents j/k). Enter connects to the selection. '.' expands the list to include attached sessions too, which can then be connected to with extra confirmation. 'x' on a session exits that session on the target, and the menu is updated. At the bottom of the list, an 'Exit' option leaves the menu and returns to the command line; Esc does the same at any point.\n\nVerified: today a plain `acs <host>` attaches to `main` (or `$ACS_DEFAULT_SESSION`), creating it if absent (DESIGN §4.4). §4.4 says outright that \"the default never depends on what else happens to exist\", and Decision 3 (§12) rests on it, so this reverses a recorded decision: update §4.4, §12 and the README's Use section with it. `acs <host> <name>`, `--new` and `--list` stay as they are. The session list already exists: `_proxy --list` sends each master STATUS (src/list.rs `query`, `render`), which gives name, attached/detached, who, idle time and command. Killing a session from outside it does not exist: today only Ctrl-] Ctrl-] `x` inside the session sends KILL. 'x' in the menu needs a new side call, for example `_proxy --kill <name>`, and probably a confirmation.\n\nOpen points: what \"create a new one\" names (`main` if absent, else the lowest free number as `--new` does); behaviour when stdin/stdout is not a tty (keep the old attach-or-create of `main`); the takeover confirmation for an attached session (reuse the BUSY/`--force` path, §4.5); an alias with several hosts resolves to one machine first (§7.3); Esc must be told apart from arrow-key escape sequences; the menu runs in raw mode and restores the terminal on every exit path; with more than nine sessions a single digit cannot address all of them.\n\nDone when a plain `acs <host>` creates with no detached sessions and otherwise shows the menu with number/arrow/j/k selection, Enter, '.', 'x', Exit and Esc working; tests cover the menu's key handling and the create/menu decision; and the docs describe the new default. Filed as one feature, not decomposed; the remote kill side call could be split out as its own bead.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-19T02:38:58.478628Z","created_by":"michel","updated_at":"2026-09-19T03:17:34.005301Z","closed_at":"2026-09-19T03:17:34.005219Z","close_reason":"Implemented with tests: plain acs <host> creates when nothing is detached, else a menu (digits, arrows/j/k, Enter, '.', 'x' via _proxy --kill, n, Esc); no tty keeps attach-or-create (PR #33)","source_repo":"beads-intake-redraw-picker","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-redraw-picker","compaction_level":0,"original_size":0}
{"id":"acs-sia","title":"Choose an alias's host by the networks this machine is on (IPv4, IPv6)","description":"Wanted, in the user's words: \"add functionality that overrides the selection of alternates process based on the network(s) the current host is on. Determine the 'network' based on its current IP address and check it against the IP the target host resolves to. Keep in mind to support both IPv4 and IPv6\".\n\nVerified (v0.8.1): an alias's host is chosen only by pinging (src/alias.rs `pick`, DESIGN §7.3): the first entry in configured order that answers within `reachability_timeout`. Nothing looks at the client's own addresses: there is no interface enumeration in the code (src/netwatch.rs only watches for route changes, to trigger an early redial), and entries' host names are never resolved by acs (ssh resolves them).\n\nChange: before (or alongside) the pings, list this machine's interface addresses with their prefixes (`getifaddrs` via the `libc` crate, no new dependency: IPv4 address + netmask, IPv6 address + prefix length), resolve each entry's `host` to all its addresses (A and AAAA, `getaddrinfo`), and prefer an entry one of whose addresses lies in one of the client's directly connected networks. Such an entry overrides the order-based choice; entries on no local network keep today's rule. Example: at home the laptop is on 192.168.1.0/24 and devbox.lan resolves to 192.168.1.20, so devbox.lan is used even if it is listed after devbox.example.com.\n\nOpen points:\n1. Opt-in or always on: a setting (e.g. `network_match: true`, global and per alias like `persist`) or the default behaviour.\n2. Still ping the matched entry (reachable and on the local network) or dial it without a ping.\n3. Which addresses count: skip loopback; IPv6 link-local (fe80::/10) needs a scope and is probably skipped; several matching entries (configured order, or longest prefix).\n4. Cost and failure of resolution: resolve in parallel with the pings and bound it by `reachability_timeout`, so a slow or failing DNS costs no more than today; a name that does not resolve simply does not match.\n5. How it combines with the bead that marks a host entry as preferred (`prefer: true`): the user's words say the network match \"overrides\" selection, so network match first, then `prefer`, then order.\n6. A redial after a network change (netwatch, DESIGN §5.3) re-evaluates the match, since the client's networks changed.\n\nDone when an entry on one of the client's networks is chosen over the configured order for IPv4 and IPv6, with unit tests on the matching (addresses and prefixes as injected data, both families, loopback and link-local excluded) and on the selection with injected interface and resolver results, `-v` says an entry was chosen because it is on a local network (naming the network), and DESIGN §7.3 and the README describe it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-19T20:48:50.553059Z","created_by":"michel","updated_at":"2026-09-19T21:15:47.627967Z","closed_at":"2026-09-19T21:15:47.627847Z","close_reason":"Implemented in PR #58 (f550587): prefer_local_network ranks hosts on this machine's networks first, IPv4 and IPv6","source_repo":"beads-intake-prefer-network","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-prefer-network","compaction_level":0,"original_size":0}
{"id":"acs-spd","title":"A terminal stall during an outstanding netcheck now costs a redial where stale bytes used to cover it","description":"The trade-off acs-br2 (PR #208, 28ee038) made, named here so it is not a\nsurprise to whoever meets it.\n\nBefore, *any* byte arriving after the netcheck PING cleared the probe. That was\nthe bug — bytes written before the network moved were answering for a link that\nhad gone. Now only the PONG for that PING answers.\n\nThe case that rule was incidentally protecting: if the terminal stalls for\nlonger than `ACS_NETCHECK_MS` **while a netcheck is outstanding**, the PONG sits\nunread in the pipe, the deadline expires, and the link is declared dead where\nstale bytes used to cover it. Cost is one redial, bounded by the change's own\ndeadline — not a stranding.\n\nIt is narrow by construction: the stall has to begin **after** the probe was\narmed. A terminal already stalled when the network moves is the case acs-br2\nfixed, and a link that is genuinely fine answers its PONG within the deadline\nlike any other frame.\n\nThis is the same tension acs-7k7 settled the other way for `ACS_DEAD_MS` (a\nstalled terminal must not look like a dead link), so it is worth deciding\ndeliberately rather than leaving the two rules quietly inconsistent.\n\nTwo ways to close it, both from the acs-br2 pass:\n\n- **decode what was just read before the tick**, so a PONG already in the buffer\n  is credited rather than being one poll iteration too late;\n- **do not count time the client spent blocked in `write_output`** against the\n  netcheck deadline, which is the honest measure — the link was not slow, the\n  terminal was.\n\nThe second is closer to what the deadline is actually asking.\n\nDone when a terminal stall that begins after a netcheck is armed does not cost a\nredial, the acs-br2 regression test\n(`bytes_in_flight_when_the_network_moved_do_not_answer_the_netcheck`) still\nfails without its fix, and DESIGN §5.3 and §5.4 agree about when a stalled\nterminal is and is not a dead link.","status":"open","priority":4,"issue_type":"bug","created_at":"2026-09-25T20:56:29.742502Z","created_by":"michel","updated_at":"2026-09-25T20:56:29.742502Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-t8t","title":"scripts/test_install.sh has been dark since release signing landed and cannot pass","description":"`scripts/test_install.sh` — one of the five checks CLAUDE.md lists, and **the\nonly thing that exercises the one-line installer against real releases** — has\nbeen unable to pass since acs-o9v added release signing. Found during acs-x57\n(PR #214, 1d00169), and reproduced byte-for-byte against `origin/main`'s scripts\nin a temp checkout, so it is not that change's doing.\n\nTwo stale spots, both fatal:\n\n1. **`ACS_TEST_OLD` defaults to `0.1.0`, which predates signing.**\n   `v0.1.0/SHA256SUMS.sig` is a 404, so the pinned-version case dies.\n2. **The local checksum-mismatch mirror it builds has no `SHA256SUMS.sig`.** The\n   installer refuses at the signature check before ever reaching the checksum\n   check that case exists to test.\n\nWith `ACS_TEST_OLD=0.15.0` the three reachable cases pass — latest into the\ndefault layout, pinned then upgraded in place, and `ACS_INSTALL_DIR` — and then\nit stops at (2) and **never reaches the Alpine and Ubuntu containers at all**.\nSo the container coverage has been dark the whole time too.\n\nThis is the same shape as acs-cu8, where `scripts/test_linux.sh` was red long\nenough to stop being read, and it deserves the same priority for the same\nreason: a check nobody can run is a check nobody consults, and this one guards\nthe path a new user's very first contact with acs takes — `curl … | sh`, often\nas root.\n\nA third thing that will bite the moment (1) and (2) are fixed, found by running\nthe containers by hand: **`alpine:3` in that suite has no `ssh-keygen`.** Its\nsetup is `adduser -D tester` only, so the installer dies on \"need ssh-keygen\".\nUbuntu will want `openssh-client` for the same reason. (By hand, with those\ninstalled, both the repository script and the substituted packaged one installed\nthe real 0.16.0 release in `alpine:3` with busybox wget and ash, and in\n`ubuntu:24.04`, as root and as a normal user — so the installer itself is fine;\nit is the harness that is stale.)\n\nFor (2) there are three ways out, worth choosing between: sign the mirror with a\nthrowaway key and substitute that key into the installer under test (acs-x57\nmade that easy); test the checksum path against a signed real release with a\ncorrupted archive; or have the mirror carry a real `SHA256SUMS.sig` copied from\nupstream, which tests less.\n\nDone when `scripts/test_install.sh` passes end to end on a clean machine\nincluding both containers, the checksum-mismatch case actually reaches the\nchecksum check, and VERIFICATION.md's installer rows say what ran rather than\nwhat was assumed.","status":"open","priority":2,"issue_type":"bug","created_at":"2026-09-25T23:22:26.077711Z","created_by":"michel","updated_at":"2026-09-25T23:22:26.077711Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-tb1","title":"Stop link_bin clobbering or downgrading ~/.local/bin/acs","description":"`link_bin` (src/install.rs ~L279) creates a symlink to the version just installed and renames it over `~/.local/bin/acs` unconditionally. Consequences: a hand-installed regular file there (what the release notes tell users to `install -m 755`) is replaced by a symlink into `~/.local/share/acs/<v>/`; an older client connecting to a host with a newer version installed silently downgrades the host's link (DESIGN §8 says it points at the *newest* installed version); a later prune of that version leaves the link dangling.\n\nDone when the link is only replaced if absent or a symlink into `~/.local/share/acs/`, and only when the target version is >= the one it currently points at, with a test for a pre-existing file and a pre-existing newer symlink.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-18T15:14:21.148801Z","created_by":"michel","updated_at":"2026-09-18T15:28:29.492889Z","closed_at":"2026-09-18T15:28:29.492809Z","close_reason":"Fixed with regression tests (bugs-install branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-trw","title":"Send HELLO as soon as ssh is spawned, not after ACS-READY","description":"serve() only builds and writes HELLO after dial() has seen the ACS-READY marker (client.rs), so every dial and redial pays one extra round trip. The proxy reads stdin only after printing the marker, so the client can write HELLO into ssh's stdin right after spawning it; marker and WELCOME then come back together. Saves 1 RTT on every connection, more through a jump host.\nOn ACS-NEED the link is closed anyway, so a HELLO sitting unread there is harmless; check the install path and a remote startup file that reads stdin (rare) do not break. Done when HELLO is written before the marker is awaited, with a test that the handshake still works when the marker arrives after noise.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-23T19:28:52.351257Z","created_by":"michel","updated_at":"2026-09-23T23:19:19.224384Z","closed_at":"2026-09-23T23:19:19.224170Z","close_reason":"Implemented in PR #176 (2579994): HELLO is written into the transport's stdin immediately after spawn, non-blocking, with Link::pending for the remainder. Measured ACS-READY to first output byte ~172ms to ~57ms over a 100ms-RTT relay","source_repo":"beads-intake-latency","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-latency","compaction_level":0,"original_size":0}
{"id":"acs-txt","title":"Keep retrying a lost host, dialling when it answers a ping every 5 s","description":"Wanted, in the user's words: \"add functionality that an acs instance will keep trying to reconnect after a session has been disconnected. It will use the same icmp echo infra to detect if a host is reachable again. Add a command line option to enable this, e.g. --reconnect. Also add a configuration option per alias and individual host that enables this. Run the pings once every 5 seconds. Make this configurable as well.\"\n\nWhat exists (v0.6.0), so the implementation adds to it rather than duplicating it:\n- A client that has attached already redials forever after a drop (src/reconnect.rs `run`, DESIGN §5.3): backoff 1 s → 30 s by ssh dial (ConnectTimeout 10 s, 30 s handshake deadline), an early retry on a network change (netwatch.rs), and for an alias `redial_alias` re-pings its hosts before each dial (`alias::resolve`, parallel pings with `reachability_timeout`, default 500 ms). `--no-reconnect` turns it off; `-r` is accepted and ignored because reconnecting is the default.\n- Where the client gives up today, exiting with the unreachable code 255 and \"reattach with: acs <host> <session>\": the first connection fails to dial or gets no WELCOME within 120 s (client.rs `connect_and_serve`, `Outcome::Exit(code::UNREACHABLE)`); no host of an alias answers a ping at start (client.rs `resolve_alias`); or the link is lost before the session was ever attached (`state.instance.is_none()` in reconnect.rs `run`).\n\nNew behaviour:\n- **Ping-gated waiting**: while the host is unreachable, ping it once every interval (default 5 s, configurable) with the existing ping code (`alias::ping` / the parallel resolve and `reachability_timeout`), and only dial ssh once it answers. This applies to a plain hostname as well as an alias. A host with `reachability_check: false` cannot be pinged, so it keeps the ssh-dial backoff.\n- **Don't give up**: when enabled, the cases above that exit 255 today wait and retry instead, showing the offline status line (§5.4). The command keys keep working (Ctrl-] Ctrl-] d leaves), as in the redial wait.\n- **Switches**: a command-line flag, and a setting on an alias and on an individual host entry (the same levels as `identity_file` and `redraw_on_reconnect`; the entry beats the alias, and the flag beats both). A global default and an `ACS_*` override would fit the other settings. The interval, e.g. `reachability_interval: 5s`, is a duration like `reachability_timeout`, global and per alias. `acs config` / `acs config host set` edit both.\n\nOpen points:\n1. **The flag's name.** Reconnecting is already the default and `-r`/`--reconnect`-style names read as that, so either give `--reconnect` this new meaning explicitly (and say how it relates to `--no-reconnect`) or pick a distinct name (`--wait`, `--persist`).\n2. Whether the enabled mode should also replace the 1 s → 30 s ssh-dial backoff for an attached session's redials with the ping gate, or only cover the cases that give up today.\n3. A network change (netwatch) should still trigger an immediate ping.\n\nDone when, with the option enabled, an unreachable host (plain or alias) is pinged every interval and dialled as soon as it answers, both at first connect and after a drop; the flag, alias and host settings follow that precedence and validate; tests cover the gating, the interval, the precedence and the `reachability_check: false` fallback (ACS_PING as the test hook); and DESIGN §5.3/§7.2/§7.3 and the README describe it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-19T15:49:13.378551Z","created_by":"michel","updated_at":"2026-09-19T17:11:51.742158Z","closed_at":"2026-09-19T17:11:51.742051Z","close_reason":"Implemented in PR #49 (8088046): --persist / persist setting, ping-gated waiting every reachability_interval","source_repo":"beads-intake-persistent-reconnect","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-persistent-reconnect","compaction_level":0,"original_size":0}
{"id":"acs-u3c","title":"Fix master dropping the child's final output at exit (stale revents)","description":"The \"pty drained\" check (src/master.rs ~L453-458) decides from the revents of the poll that just returned: if the child has been reaped, POLLIN was requested and the pty did not report POLLIN, `pty_open = false` and the next iteration goes to `finish()` without reading the pty again. But `sys::poll` maps EINTR to `Ok(0)` with all revents zero, and SIGCHLD is installed without SA_RESTART, so the child's own exit interrupts the poll; `reap()` then sets `status` and the same iteration closes the pty. On Linux a slave write reaches the master's read queue asynchronously (flip buffer → workqueue), so `echo done; exit` can lose its last line. The same happens when poll returned for another fd and the child wrote and exited before `reap()` ran.\n\nDone when the drained decision requires a subsequent quiet poll of the pty *after* the status was observed (e.g. the full 50 ms timeout genuinely expiring), and a regression test exercises output-then-exit repeatedly.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-18T15:14:21.211006Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.354502Z","closed_at":"2026-09-18T15:39:58.354402Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-u3qn","title":"dist and package can be run in different environments, yielding fork binaries beside an upstream installer","description":"Found during acs-x57 (PR #214, 1d00169), which made the packaged installer,\nformula and notes carry the build's own releases URL and key.\n\nxtask reads those values at **its** compile time. So `cargo xtask dist` with the\nvariables set, followed by `cargo xtask package` without them, produces a fork's\nbinaries sitting beside an **upstream** installer, upstream notes and an\nupstream formula — silently, with nothing in the output saying the two halves\ndisagree.\n\nThe user-visible shape: a fork publishes a release; the archives contain its\nbinaries, and the `curl | sh` line beside them installs acs's. Or the reverse,\nwhich fails closed but confusingly.\n\n`scripts/release-binaries.sh` runs both in one environment, so the supported\npath cannot hit it, and `package` now prints what it substituted — but neither\nis a check. Anyone who runs the two steps by hand, or who caches a built xtask\nacross environments, can reach it.\n\nThe fix the acs-x57 pass suggested: have `dist` record the channel it built into\n`dist/`, and `package` refuse a mismatch. Note **acs-gb4 already established\nexactly this pattern** — `dist/source.stamp` records what the tree was, and\n`scripts/e2e_ssh.sh --no-build` refuses when it does not match. Extending that\nstamp to carry the releases URL and key fingerprint is the same idea and\nprobably the same few lines.\n\nA second, smaller thing from the same pass, related because it is the other end\nof the publish path: **`update-tap.sh` parses the release version back out of\nthe formula's URLs** by matching `…/releases/download/vX.Y.Z/…`. A fork whose\nreleases base is not GitHub-shaped renders a perfectly valid formula that the\ntap script then rejects as \"names no release\". It is documented as a caveat\nrather than fixed. Decide whether the version should be passed rather than\nre-derived — it is already known at the call site.\n\nDone when a `package` whose channel differs from the `dist/` it is packaging\nrefuses rather than publishing a mismatched pair, a test covers the mismatch,\nand the tap script's version comes from something more reliable than a regex\nover URLs it did not choose the shape of.","status":"open","priority":3,"issue_type":"bug","created_at":"2026-09-25T23:22:46.754311Z","created_by":"michel","updated_at":"2026-09-25T23:22:46.754311Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-ue2","title":"Resolve host aliases on connect with ping check and fallback hosts","description":"Resolve `acs <name>` through the configured host aliases, checking reachability and falling back to alternate hosts.\n\nUser's ask: \"whenever connecting to a host, use the name as an alias if it is defined in the configuration. When it is, connect to the 'hostname', with user 'user' if defined (otherwise SSH will use its config). If reachability_check is true, use icmp ping once to check if the host is up. If it is, use it; if it is not, check the alternate 'host' if available. If not, display an error message and exit.\"\n\n- Lookup: if the destination given to acs (without a `user@` part) is a key in `hosts`, try its entries in order.\n- An entry with `reachability_check: true` is pinged once: `ping -c 1` with a short timeout. The timeout flag differs between macOS (`-t`) and Linux (`-W`). The first entry that answers is used. An entry with `reachability_check: false` is taken without a check.\n- The chosen entry becomes the ssh destination: `user@host` if `user` is set, otherwise just `host`, so ~/.ssh/config decides the user.\n- None reachable: print which hosts were tried and exit with the unreachable code (255, src/client.rs `code::UNREACHABLE`).\n- Applies to every ssh call: session, `--list`, install. On a redial (src/reconnect.rs) the alias should be resolved again, so a fallback host is picked up after a network change. The master session stays the same only if the fallback is the same machine; say so in the docs.\n- A name that is not an alias keeps today's behaviour exactly.\n- `-v` should show which entry was chosen and why.\n\nDone when: tests cover alias hit and miss, fallback to the second entry, `reachability_check: false`, all-unreachable → error/255, and `user` handling (ping is injected so tests don't need ICMP); README documents it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T14:23:40.843666Z","created_by":"michel","updated_at":"2026-09-18T14:59:50.225731Z","closed_at":"2026-09-18T14:59:50.225629Z","close_reason":"Implemented: acs <alias> resolves through hosts with ping check (ACS_PING), fallback, user handling, exit 255 when none answers, re-resolved on redial","source_repo":"beads-intake-config","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-config","compaction_level":0,"original_size":0,"labels":["acs","client","config"],"dependencies":[{"issue_id":"acs-ue2","depends_on_id":"acs-xfk","type":"blocks","created_at":"2026-09-18T14:23:47.372937Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-uj6","title":"net.pinged() is read as a positive assertion, but the fake logs when a ping is scheduled","description":"Four tests assert \"host X was pinged\" by reading `net.pinged()`:\n`an_unchecked_host_is_used_without_a_ping`,\n`an_alias_connects_to_its_first_reachable_host`,\n`an_ipv6_host_is_pinged_with_ping6`, and the `user_at_alias_…` case.\n\nThe fake ping writes its line **when the ping is scheduled**, not when it has\nrun, so under load the line may not be there yet when the test looks. Measured\nduring acs-2dc (PR #205, 661e1dc): 4-6 failures per 60 runs in every load run.\n\nThe author of that pass hit it directly — a first draft of the alias test made\nthe same mistake and failed **56 of 60** until it was rewritten to assert only\nthe negative.\n\nThe asymmetry is the point: **`pinged()` is sound as a negative** (\"this host\nwas never pinged\" — nothing appears because nothing was scheduled) and unsound\nas a positive (\"this host was pinged\" — the line may be pending). acs-2dc used\nit negative-only for exactly this reason and left the four positive uses alone\nas out of scope.\n\nTwo ways out, to choose between: make the fake log when the ping **completes**\nso the positive claim is true when observed, or give the harness a\n`wait_pinged(host)` that waits for the line rather than sampling once. The\nsecond keeps the fake simple and matches how the rest of the suite waits for\nevents; the first makes every existing call site correct without changing them.\n\nDone when \"host X was pinged\" cannot pass or fail on timing, the four tests use\nwhichever form is chosen, and a load run shows the rate at zero — acs-2dc's\nrecipe (parallel copies plus `dd` and fork/exec pressure) is the one that\nsurfaces it.","status":"open","priority":3,"issue_type":"bug","created_at":"2026-09-25T19:21:52.534627Z","created_by":"michel","updated_at":"2026-09-25T19:21:52.534627Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-uko","title":"Add acs upgrade: self-upgrade from GitHub releases","description":"Add `acs upgrade`: replace the running acs with the latest (or a given) GitHub release.\n\nUser's ask: \"add ability to do a self upgrade (from github)\".\n\nProposed design:\n\n- `acs upgrade [--version X.Y.Z] [--check]`. `upgrade` becomes a reserved first argument, like `config` in acs-iff.\n- It finds the latest release (or the given one), downloads `acs-<version>-<own target>.tar.gz` and `SHA256SUMS` via `curl` (acs has no HTTP/TLS client; DESIGN §9 size budget), and verifies the checksum.\n- It replaces the binary behind `current_exe()` atomically: write a temp file next to it, then rename it into place, keeping the mode. If that directory is not writable (e.g. `/usr/local/bin`), it says to re-run with sudo.\n- It refuses a downgrade unless `--version` asks for it, and does nothing when already current. `--check` only reports.\n- Remote hosts need nothing: the next connection finds no binary for the new version and installs it (versioned install, DESIGN §8).\n- On macOS, a curl download carries no quarantine attribute; the ad-hoc signature must still verify after the rename.\n- The download-and-verify logic should match the one-line installer (same asset names), so the two agree.\n\nDone when: tests cover upgrading from a fake release server or injected curl (success, checksum mismatch, already current, unwritable directory), a real upgrade from v0.1.0 → latest is exercised once, and README documents `acs upgrade`.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T14:34:34.770077Z","created_by":"michel","updated_at":"2026-09-18T15:16:37.097018Z","closed_at":"2026-09-18T15:16:37.096920Z","close_reason":"Implemented: acs upgrade [--version X.Y.Z] [--check] via SHA256SUMS + curl/wget, checksum and --version sanity check, atomic replace (plain or versioned layout with links), sudo hint; real 0.1.0->0.2.0 upgrade verified on macOS (codesign valid) and Linux","source_repo":"beads-intake-install","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-install","compaction_level":0,"original_size":0,"labels":["acs","client","install"]}
{"id":"acs-uxj","title":"Pick a session from a menu across every configured host","description":"Wanted, in the user's words: \"just like the session menu that is presented if sessions exist on a host through `acs <alias|host>`, it should be possible to list all the sessions across all the hosts.\"\n\nVerified (v0.7.0): the session menu (src/menu.rs, driven by src/pick.rs, DESIGN §4.4) serves one host. `Menu::render` takes a single `host` for its title, and since #40 the whole menu runs over one `_proxy --pick` connection to that host, which also carries 'x' (end a session): `_proxy --kill` no longer exists (src/proxy.rs rejects it). The all-host listing exists as a table only (`acs --list`, src/list.rs `run_all`: every alias resolved and asked in parallel with `_proxy --list`, BatchMode + ConnectTimeout=10 so no prompts, and a HOST column).\n\nWanted behaviour: the same menu (number / ↑↓ jk / Enter / '.' to include attached sessions / 'x' to end / Esc and Exit to leave) over every alias's sessions, with a HOST column, rows grouped in configuration order, and the hosts that could not be asked (unreachable, not installed, no sessions) shown as lines as `--list` shows them. Enter attaches to that session on that host (the normal session dial, which may prompt, so resolve that alias again and use its key and user as `acs <alias> <session>` does).\n\nOpen points:\n- **Entry point**: `acs list` in a terminal (with the table when stdout is not a tty), a bare `acs` with no host, or a flag. This touches the bead \"Replace acs --list without a host by an acs list subcommand\"; decide there first if the menu lives on `acs list`.\n- **'x' on another host**: either a short `_proxy --pick` connection to that host, or bring back a kill side call; either way refresh only that host's rows.\n- '.' and \"new session\": whether \"new\" applies (it needs a host: the cursor row's) or is left out of the all-host menu.\n- Latency: the parallel query takes as long as the slowest host (up to the 30 s answer limit); draw hosts as they answer rather than waiting for all.\n\nDone when the all-host menu works with the per-host menu's keys, attaches and ends sessions on the right host, tests cover the menu's multi-host state and rendering (menu.rs unit tests) and an end-to-end pick across two hosts, and DESIGN §4.4/§7.3 and the README describe it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-19T16:19:43.015807Z","created_by":"michel","updated_at":"2026-09-19T16:55:15.874458Z","closed_at":"2026-09-19T16:55:15.874357Z","close_reason":"Implemented in PR #47 (34a29db): acs list in a terminal is the session menu over every host alias","source_repo":"beads-intake-list-cmd","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-list-cmd","compaction_level":0,"original_size":0}
{"id":"acs-uyk","title":"Ring the terminal bell when Ctrl-] Ctrl-] arms command mode","description":"Ring the terminal bell when Ctrl-] Ctrl-] arms command mode, so the user knows the next key (d, x) is a command. Configurable, on by default.\n\nUser's ask: \"if it is not breaking our concept of a transparent stream, trigger the bell in the terminal when the command mode sequence has been typed: ctrl-] ctrl-]. Make it possible to configure this behavior, defaulting to on\".\n\nDoes it break the transparent stream? No, if done right. The bell is written by the client to the local terminal (like the reconnect status line, DESIGN §5.4). Nothing is added to the program's input, and the pty output bytes are unchanged. The one hazard is interleaving: BEL (0x07) also terminates OSC sequences, so writing it while the remote output is in the middle of an unterminated OSC/DCS would end that sequence early. The client already parses output to track terminal modes (src/modes.rs; src/keys.rs lexes escapes too); the bell should only be written at a ground-state boundary, deferred otherwise.\n\nProposed design:\n\n- Ring when the detector (src/keys.rs `Detector`) arms: the second Ctrl-] within the window, online and in the offline wait (src/reconnect.rs).\n- `command_bell: true|false` in the configuration (DESIGN §7.2; src/config.rs BOOLS, so `acs config set command_bell false` works), plus `ACS_COMMAND_BELL=0` in the environment.\n- Never ring inside a bracketed paste (the detector already ignores the key there).\n\nDone when tests show the BEL on the client's terminal after Ctrl-] Ctrl-], none with the setting off, and none written in the middle of an OSC sequence the program is emitting; DESIGN §6 and the README document it.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T19:06:27.248670Z","created_by":"michel","updated_at":"2026-09-18T21:45:02.033082Z","closed_at":"2026-09-18T21:45:02.032961Z","close_reason":"Implemented with tests: bell on command-mode arm, deferred past OSC/DCS, command_bell / ACS_COMMAND_BELL (PR #22)","source_repo":"beads-intake-list-bell","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-list-bell","compaction_level":0,"original_size":0}
{"id":"acs-vdl","title":"Accept non-UTF-8 login noise while reading the remote installer reply","description":"install::side_call (src/install.rs:164-175) reads the whole remote stdout with read_to_string and then looks for the finisher's `ok` line. read_to_string returns InvalidData and leaves the buffer empty if any byte is invalid UTF-8 (confirmed with a scratch program: b\"Welcome \\xe9\\n\\nok\\n\" gives out == \"\"). The result is ignored, so expect_ok fails with `unexpected reply while installing: \"\"` although `_install --finish` completed and printed ok.\n\nScenario: a remote .bashrc or motd prints a Latin-1 or CP437 banner on stdout (bash sources .bashrc for ssh commands). The binary is installed and ~/.local/bin/acs linked, but the client exits INSTALL_FAILED; the next `acs host` works because the prelude then finds the binary. dial()'s MarkerScanner tolerates non-UTF-8 noise line by line, so the two paths are inconsistent.\n\nDone when side_call reads bytes and scans lines with from_utf8_lossy or byte comparison, with a test whose fake finisher emits an invalid byte before ok.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-19T21:46:58.744433Z","created_by":"michel","updated_at":"2026-09-19T22:25:48.658010Z","closed_at":"2026-09-19T22:25:48.657879Z","close_reason":"Implemented in #77 (649d386)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-vq1","title":"Consider a shorter or RTT-adaptive dead-link timeout","description":"Optional. A link is declared dead after 10 s of silence (pings after 3 s). Resume is lossless, so a false alarm only costs a redial; a shorter or RTT-adaptive timeout (a few times the measured PING/PONG RTT, with a floor) would catch drops sooner where netwatch sees no change. Decide once per-phase timings exist; ACS_DEAD_MS already overrides it.\nTouches reconnect.rs Liveness, DESIGN §5.3.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-23T19:29:12.862164Z","created_by":"michel","updated_at":"2026-09-23T19:29:29.362186Z","source_repo":"beads-intake-latency","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-latency","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"acs-vq1","depends_on_id":"acs-pgn","type":"blocks","created_at":"2026-09-23T19:29:29.361709Z","created_by":"michel","metadata":"{}","thread_id":""}]}
{"id":"acs-vxg","title":"Ping a lost host every second at first when persisting","description":"With --persist, the ping gate pings a lost host every reachability_interval (5 s default), so the dial can start up to 5 s after the host is back. Pings are cheap: ping every 1 s for the first ~30 s after a loss, then back off to reachability_interval.\nTouches reconnect.rs wait_for_answer and client.rs wait_for_host, DESIGN §5.3. Done when the interval ramps, with a test using ACS_PING.\n","status":"open","priority":3,"issue_type":"feature","created_at":"2026-09-23T19:29:07.200656Z","created_by":"michel","updated_at":"2026-09-23T19:29:07.200656Z","source_repo":"beads-intake-latency","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-latency","compaction_level":0,"original_size":0}
{"id":"acs-w1z","title":"Filter control characters out of remote text before it reaches the terminal","description":"Security, escape-sequence injection. Nothing in the client filters remote-supplied text before printing it: a grep for is_control over src/ hits only config.rs (local alias names) and yaml.rs (emit). StatusInfo.name, .identity, .command, .version, Welcome.session and the ERROR frame's message all reach the terminal verbatim through list::cells and list::render (which does not clip at all), Menu::render, Menu's note, client::note and reconnect::ask_takeover. menu::clip truncates by display width and char_width gives ESC width 1, so clipping neutralises nothing and can also cut a sequence in half.\n\nWhy it matters. `acs list` with no host contacts every alias in parallel and prints every reply, so one compromised or untrusted host in the config can redraw rows in a list the user is acting on: the row read as \"trusted-host main detached\" need not be the row the cursor is on, and Enter attaches to, or x ends, something else. The same primitive rewrites the takeover question, which is a security decision, and the ERROR note printed mid-session while the terminal is in raw mode. On terminals that answer queries (title report, OSC 52) the reply lands on the client's stdin and can be delivered as INPUT to a different, trusted host. Session names are validated on the remote (session::validate_name) but the client never re-validates what it receives, and identity and command are validated nowhere.\n\nHonest scope: an attached session's stream is deliberately unfiltered (DESIGN's unfiltered guarantee) and dial() inherits stderr, so a hostile login banner already reaches the terminal. What is new here is acs's own UI, the listing and the menu.\n\nDone when one helper replaces C0/C1 controls with a visible placeholder and caps length, applied at every point remote text enters a terminal-bound string (list::cells, menu note and render, client::note, reconnect::ask_takeover), Welcome.session and each StatusReply.name are re-validated client-side, and tests cover a hostile name, identity and ERROR message rendering inert.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-20T04:35:59.687832Z","created_by":"michel","updated_at":"2026-09-20T04:49:02.128215Z","closed_at":"2026-09-20T04:49:02.128136Z","close_reason":"Implemented in #109: safe::display at every terminal sink, inbound session-name validation","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-wxa","title":"Honour ACS_ESCAPE_TIMEOUT_MS in the offline command-key detector","description":"`client_escape()` (src/reconnect.rs ~L194-203) copies only ACS_ESCAPE_KEY into the detector config used while the link is down; `escape_config()` (src/client.rs ~L74-89) reads both ACS_ESCAPE_KEY and ACS_ESCAPE_TIMEOUT_MS. A user with a custom escape window who double-taps Ctrl-] d during an outage gets the hard-coded 400 ms window instead, so the timing silently differs while disconnected.\n\nDone when both sites share one config constructor and a test asserts the offline detector uses the configured window.\n","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-18T15:14:21.886595Z","created_by":"michel","updated_at":"2026-09-18T15:48:09.575477Z","closed_at":"2026-09-18T15:48:09.575408Z","close_reason":"Fixed with regression tests (bugs-client branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-wza","title":"Fix proxy busy-spin after client EOF while input is backpressured","description":"In `relay()` (src/proxy.rs ~L217-225) once stdin hits EOF `stdin_open = false` and its pollfd gets `events = 0`, but poll reports POLLHUP on a closed pipe regardless of `events`. Every iteration then returns immediately from poll, reads 0, and loops, for as long as `to_master` cannot drain — which is exactly when the master has stopped reading (PTY_IN_HIGH_WATER, child not consuming input). Trigger: paste a lot into a program not reading stdin, then the ssh connection drops → the proxy spins at 100 % CPU until the child reads or the session ends.\n\nDone when the stdin pollfd uses `fd = -1` once closed (poll ignores negative fds), with a backpressure test in tests/proxy.rs.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-18T15:14:21.396697Z","created_by":"michel","updated_at":"2026-09-18T15:39:58.365776Z","closed_at":"2026-09-18T15:39:58.365690Z","close_reason":"Fixed with regression tests (bugs-master branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-x1k","title":"Run the downloaded binary from its destination directory in acs upgrade, not from /tmp","description":"Scratch::new (src/upgrade.rs:238-247) puts the archive and the extracted acs-<v>-<target>/acs under std::env::temp_dir(), and download() (upgrade.rs:375-378) runs it with --version as the \"it works here\" proof. On hosts with /tmp mounted noexec (common Linux hardening, some CI images) Command::new(&new) fails with EACCES: \"the downloaded acs does not run here: Permission denied\", and the upgrade is impossible with no hint. scripts/install.sh does not have this problem, it only runs the binary from its final location.\n\nDone when the binary is copied to the destination directory under a temporary name first (already probed writable by check_writable) and --version is run from there before the rename, with a test or at least an error hint naming noexec.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-19T21:46:58.999335Z","created_by":"michel","updated_at":"2026-09-19T22:46:57.996844Z","closed_at":"2026-09-19T22:46:57.996768Z","close_reason":"Implemented in #85 (b063216)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-x1z","title":"Remove dsh references from the README and help","description":"Remove the dsh references from the README; the general public does not know what dsh is.\n\nUser's ask: \"remove 'It replaces the dsh shell function (ssh + dtach).' in the README.md file. In general we don't have to talk too much about dsh. The general public doesn't know what it is\".\n\nFound in README.md today:\n\n- line 10: \"It replaces the `dsh` shell function (`ssh` + `dtach`). The design is in [docs/DESIGN.md](docs/DESIGN.md).\" Drop the dsh sentence and keep the pointer to the design.\n- line 113: \"`-l` is `--list` (as in `dsh`)\". Keep the fact (`-l` means `--list`, not ssh's login option) without naming dsh.\n\nAlso check the other public surfaces for dsh: `acs --help` (src/cli.rs USAGE) and the release notes (xtask/src/package.rs). docs/DESIGN.md keeps its dsh history: that is design rationale (§1, §10), not user documentation.\n\nDone when README, --help and the release notes no longer mention dsh, and markdownlint passes.\n","status":"closed","priority":3,"issue_type":"docs","created_at":"2026-09-18T19:28:42.639035Z","created_by":"michel","updated_at":"2026-09-18T21:45:02.132260Z","closed_at":"2026-09-18T21:45:02.132182Z","close_reason":"Done: README no longer mentions dsh; --help and release notes had none (PR #24, docs: drop the dsh references from the README)","source_repo":"beads-intake-list-bell","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-list-bell","compaction_level":0,"original_size":0}
{"id":"acs-x57","title":"Substitute the build's releases URL and key into the packaged installer and formula, finishing the fork knobs","description":"acs-5em (#153, be220ed) and acs-ktm (#160, df58317) made the two knobs that\nmatter settable at build time: the default releases URL and the release signing\nkey. The Forking table in docs/VERSIONING.md now has two build-time knobs and\nthree remaining **source edits**, all of the same shape — a fork still has to\npatch files it will then re-conflict on at every upstream sync, which is the\nexact problem those two beads removed.\n\nWhat is left, and why each is the same problem:\n\n1. **`scripts/install.sh`** carries its own `releases=` default and its own\n   hardcoded `release_key=`. `cargo xtask package` copies the script into each\n   release **verbatim**, which is why a build-time value cannot reach it today.\n   If package substituted the build's URL and key into the packaged copy, a\n   fork would need no source edit for the installer at all. The drift guard\n   `tests/scripts.rs::the_installer_carries_the_same_release_key_and_checks_before_reading`\n   is the test that would have to move with it.\n2. **`xtask/src/formula.rs`'s `REPO`** — the Homebrew formula's URLs.\n   `scripts/update-tap.sh` already takes `ACS_TAP_REPO`, so the tap half is\n   done and only the formula contents are not.\n3. **`xtask/src/package.rs`** — the install instructions written into NOTES.md\n   point at upstream, so a fork's own release notes send its users to acs.\n\nRelated asymmetry worth deciding on at the same time: `scripts/install.sh`\nhonours `ACS_RELEASES_URL` at runtime (https-only, ignored under sudo) but has\n**no `ACS_RELEASE_KEY` equivalent** — its key cannot be overridden the way the\nbinary's can via `signature::key_for`. That is not a hole, because it fails\nclosed: a release signed by another key is simply refused. But it means the\ninstaller and the binary disagree about what is configurable, and if the\npackaged copy starts carrying the build's key the question is whether the\nruntime override should exist too, or whether the build-time value is the whole\nanswer. Decide explicitly rather than by omission.\n\nDone when a fork can build and publish a complete release — binary, packaged\ninstaller, Homebrew formula and notes — without editing a tracked source file,\nthe drift guard still catches an installer that disagrees with the binary, the\nForking table lists no remaining source edits (or says plainly which are\ndeliberate), and the runtime-override question above is answered in the doc.","status":"closed","priority":3,"issue_type":"feature","created_at":"2026-09-23T19:46:16.764002Z","created_by":"michel","updated_at":"2026-09-25T23:22:14.626379Z","closed_at":"2026-09-25T23:22:14.626193Z","close_reason":"Implemented in PR #214 (1d00169): a fork edits no tracked file — three env vars reach the binary, the packaged installer, the formula and the notes. Values are single-quoted POSIX shell words so injection is impossible. Found and fixed a fourth source edit in update-tap.sh","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-xfk","title":"Add a YAML config file (/etc/acs, ~/.config/acs) with install_on_remote","description":"Add a YAML configuration file for the acs client.\n\nUser's ask: \"add support for a configuration file, stored as YAML, using the latest unix directory standard, so local in ~/.config/acs/config.yaml as well as a global /etc/acs/config.yaml equivalent. If the global one exists, it is read first, overridden by the local one. Individual parameters override global, dictionaries and arrays are additive.\"\n\n- Locations (XDG Base Directory): global `/etc/acs/config.yaml`, then local `$XDG_CONFIG_HOME/acs/config.yaml` (default `~/.config/acs/config.yaml`). Either may be missing.\n- Merge: the local file wins for scalars; dictionaries merge key by key (recursively); arrays concatenate (global entries first).\n- First option: `install_on_remote` (bool, default `true`). When false, the ACS-NEED path (src/install.rs) does not install; it exits with a message saying acs is missing on the host and how to install it.\n- `hosts`: a map of alias → list of entries `{host: string, user: string (optional), reachability_check: bool (default true)}`. This bead only parses and validates it; using it is the alias-resolution bead.\n- A malformed file is an error that names the file and line, not a silent default.\n- Nothing exists yet: no config code in src/, and today the only knobs are env vars (README \"Environment\").\n- Binary size matters (budget 1 MB slim, DESIGN §9; deps today are only libc and lexopt). Choose between a small YAML crate and a hand-written parser for the subset used, and record the size cost.\n\nDone when: both files are read and merged with those rules; `install_on_remote: false` stops a remote install; unit tests cover the merge rules, the defaults and error reporting; README and DESIGN document the file.\n","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-09-18T14:23:35.734333Z","created_by":"michel","updated_at":"2026-09-18T14:54:06.164666Z","closed_at":"2026-09-18T14:54:06.164586Z","close_reason":"Implemented: YAML config (/etc/acs, XDG local) with merge rules, install_on_remote, hosts parsing; hand-written YAML subset (+17 KB)","source_repo":"beads-intake-config","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-config","compaction_level":0,"original_size":0,"labels":["acs","config"]}
{"id":"acs-xk4","title":"Write the mode resets before clearing the observer on a gap or fresh resume","description":"On AttachKind::Fresh and AttachKind::Gap the client writes ESC[H ESC[J and then state.observer.clear() (src/client.rs:765-774) without first writing observer.reset_sequence(). The modes the remote program had on before the drop (alt screen ?1049, mouse ?1000/?1006, kitty keyboard stack, bracketed paste) are forgotten, so leave() emits no resets for them.\n\nScenario: htop runs; the link is dead long enough for its output to overrun the 1 MiB ring; the resume comes back as Gap (or Fresh after \"session was restarted\"). On a later Ctrl-] Ctrl-] d, or when the session ends, the local terminal stays on the alternate screen with mouse reporting on - the dtach failure DESIGN §6.4 says acs fixes. tests/reconnect.rs::output_lost_to_a_small_ring_is_a_gap_and_redraw only checks the clear sequence.\n\nDone when the resets are written before the clear on Fresh/Gap (or Gap keeps its modes) and a regression test asserts the reset bytes after a gap resume followed by detach.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-19T21:46:58.397308Z","created_by":"michel","updated_at":"2026-09-19T21:56:18.194705Z","closed_at":"2026-09-19T21:56:18.194615Z","close_reason":"Implemented in #66 (148f35c)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-xo1","title":"A network change during the handshake is drained and ignored, so a doomed dial runs its full timeout","description":"acs-ft1 (PR #182, ecd5d5e) made a network change act on a *connected* session:\na PING at once, and the link is declared dead in ~2s rather than ~10s if the\nhost does not answer. acs-6p8 (#180) made it act on an *offline* one. Between\nthe two sits the handshake, where a hint is drained and nothing else.\n\nSo between the dial and the WELCOME, a network change is the one moment it is\nignored — and it is the moment it is most likely to be decisive. A dial in\nflight when the machine's addresses move is very probably doomed: the ssh\nconnect is bound to an address that no longer exists. Today it waits out\nACS_DIAL_TIMEOUT_MS — **30s on a redial, 120s on the first connection** —\nbefore anything happens, while the evidence that it will fail arrived\nimmediately.\n\nThe user-visible shape: close the laptop on the train, open it on a different\nnetwork while acs was mid-connect, and wait two minutes for a connection that\nwas dead the moment the lid opened.\n\nDeliberately out of scope in acs-ft1 because it is a structurally different\nchange: `dial()` **blocks** before `serve()` is entered, so there is no poll\nloop to add a fifth fd to. Acting on the hint means either making the dial\ninterruptible or giving it its own watcher — which is also why it carries more\nrisk than acs-ft1 did, and deserves its own pass.\n\nPoints to settle when taking it:\n\n- **Abandon or shorten?** Killing the ssh child on a change is decisive but\n  loses a connect that might have survived (a second interface appearing while\n  the first still routes). Bringing the deadline forward to something small,\n  the way acs-ft1 brings the dead-link deadline forward rather than replacing\n  it, is the more conservative shape and matches the precedent.\n- **The first connection is the 120s case** and the one a user actually feels;\n  a redial's 30s is already tolerable. Consider whether they want the same\n  treatment.\n- Interaction with acs-trw (#176): the HELLO is already written into the\n  transport's stdin before the marker, so abandoning a dial has to not leave\n  a half-sent greeting behind.\n- The prelude and the remote-install path (ACS-NEED) both run inside this\n  window.\n\nDone when a network change during a dial brings that dial's deadline forward\ninstead of being ignored, the ACS-NEED/install path still completes across one,\na test drives it with ACS_NETWATCH_FIFO + ACS_NETWATCH_NETS the way acs-ft1's\ndo, and DESIGN §5.3 says what happens in each of the three phases — offline,\nhandshake, connected.","status":"open","priority":3,"issue_type":"feature","created_at":"2026-09-24T02:18:47.644562Z","created_by":"michel","updated_at":"2026-09-24T02:18:47.644562Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-xvg","title":"Attach straight from _proxy --pick when nothing is detached","description":"Plain acs <host> already shares one connection between the menu and the attach (decision 8), but with nothing detached it still costs two round trips: the list, then HELLO -> WELCOME. The client could send a HELLO template up front on the pick call, and the proxy, finding nothing detached, would attach at once and answer with the list and WELCOME together. Saves 1 RTT on the most common plain acs <host>.\nTouches proxy.rs pick(), pick.rs choose(), DESIGN §4.3/§4.4. With a menu, reading time dominates, so that path is unchanged.\n","status":"open","priority":3,"issue_type":"feature","created_at":"2026-09-23T19:29:03.084023Z","created_by":"michel","updated_at":"2026-09-23T19:29:03.084023Z","source_repo":"beads-intake-latency","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-latency","compaction_level":0,"original_size":0}
{"id":"acs-y5r","title":"Clear the force flag after the attach it was granted for","description":"Security, stale consent. pick.rs does `args.force |= force;` and client.rs passes args.force to hello() on every dial, including automatic redials. Verified by reading both. So a takeover confirmed once sticks to the process for its whole life.\n\nScenario: you answer y to take the session from alice at 09:00. At 14:00 your link drops and bob has attached meanwhile. The redial carries force, so bob is kicked out with no question, hours later and about a different person. A command-line --force behaves the same way. This is exactly the accident the confirmed-takeover design is meant to prevent.\n\nDone when force applies to the attach it was granted for and is cleared afterwards, a redial that finds a different identity attached asks again, and a test covers drop, new attacher, redial.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-20T04:36:00.456787Z","created_by":"michel","updated_at":"2026-09-20T05:33:54.306036Z","closed_at":"2026-09-20T05:33:54.305954Z","close_reason":"Implemented in #117: force is a one-shot, spent at the WELCOME it was given for","source_repo":"beads-intake-security","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-security","compaction_level":0,"original_size":0}
{"id":"acs-ysx","title":"Nothing enforces that a note raised outside the frame loop knows the terminal's state","description":"Two loose ends from acs-p4u (PR #210, ad04421), both small.\n\n**1. The invariant is held by accident, not by construction.**\n\nacs-z22 holds `-v` notes to a boundary, but only inside `serve`'s frame loop.\nacs-p4u fixed `leave`'s parting note by ending the open sequence first. Every\nother note raised **outside** the frame loop — the dial, the offline wait,\nalias resolution, the exit notes — is written with no knowledge of terminal\nstate at all.\n\nToday none of them is broken, and the reason is circumstantial: they all happen\neither before raw mode is entered, or after `leave` has written its `ST`. So\nthe terminal is never inside a program's sequence when they run. Nothing in the\ncode says so, nothing tests it, and a note added mid-outage by the next person\nwould land in whatever the last frame left open — which acs-p4u showed is not a\nhypothetical: `leave`'s own note was being eaten by an unfinished CSI.\n\nThe cheap version is a debug assertion or a single documented entry point, so\nthe constraint is stated where a new note would be written rather than inferred\nfrom where the existing ones happen to sit.\n\n**2. `show_status` redraws unconditionally on every backoff step.**\n\nEach one now writes `ST` + the line + the reopened sequence. Harmless, and the\nreopen is what makes it correct — but the text often has not changed, so a long\noutage sends the whole span repeatedly for nothing. A \"only when the text\ndiffers\" check would cut terminal traffic during exactly the period when the\nlink is worst.\n\nWorth knowing while in there, from the same pass: after a `Resumed` attach the\nobserver's lexer state and the terminal's are now in sync where they used to\ndiverge, and that divergence was what could make `notes::at_boundary` hold a\nnote for up to `ACS_NOTE_HOLD_MS`. Relevant if anyone tunes that hold.\n\nDone when a note written outside the frame loop cannot land inside a program's\nsequence — by construction or by an assertion that would fire if one did — and\nthe status line does not redraw a message that has not changed.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T21:30:36.646158Z","created_by":"michel","updated_at":"2026-09-25T21:30:36.646158Z","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-z22","title":"A -v note can land inside a program's output, splitting an escape sequence","description":"`client::note` writes to **fd 2** while the session's bytes go to **fd 1**, and\nunder `-v` both land on the same terminal. Nothing synchronises them, so a note\ncan be written in the middle of a frame the remote program is emitting —\nsplitting an escape sequence, a UTF-8 character, or a line the program is\ndrawing.\n\nFound during acs-4i2 (PR #192, 9bc0b5c), which deliberately kept\n`assert_consecutive` out of its new `-v` tests for exactly this reason. It is\n**pre-existing and `-v`-only**, not caused by the notes that pass added — but\nthat pass added three more notes on a path that fires while a session is live\n(a network hint arriving mid-session), so the window is wider than it was.\n\nWhy it is worth fixing rather than accepting: `-v` is what someone uses when\nsomething is already wrong. A `-v` that corrupts the screen it is explaining\ncosts exactly when it is most needed, and the corruption looks like the bug\nbeing investigated.\n\nNot trivial, which is why it is P3 rather than P2: acs owns the frame boundary\non the way *in* (it decodes frames from the link), so queueing a note until the\ncurrent frame has been flushed to the tty is possible — but notes are also\nemitted from paths that are not inside the frame loop at all (the dial, the\noffline wait, alias resolution), where there is no frame in flight and no\nreason to wait. The answer is probably \"queue only while a frame is being\nwritten\", which needs the writer to say when it is between frames.\n\nWorth checking what the status line already does here — it writes to the same\nterminal from `reconnect::offline` and has the same hazard, and may already\nhave a convention worth reusing.\n\nDone when a `-v` note cannot split a frame the program is emitting, notes from\noutside the frame loop are not delayed, a test asserts an interleaved note\nleaves the program's bytes consecutive (the assertion acs-4i2 had to leave\nout), and DESIGN says what the ordering guarantee is.","status":"closed","priority":3,"issue_type":"bug","created_at":"2026-09-24T06:00:20.172641Z","created_by":"michel","updated_at":"2026-09-24T18:21:16.875239Z","closed_at":"2026-09-24T18:21:16.875068Z","close_reason":"Fixed in PR #201 (da1368c): notes are held to a ModeObserver boundary of the program's stream, the same convention as the command-mode bell, spliced inside the next frame rather than waiting for one to end. Bounded by ACS_NOTE_HOLD_MS and flushed on every serve exit","source_repo":"acs","source_repo_path":"/Users/michel/src/qjam/acs","compaction_level":0,"original_size":0}
{"id":"acs-zc4","title":"Fetch tags before snapshotting them in version-bump.sh so stale tags are not re-released","description":"scripts/version-bump.sh (lines 15-29) takes `before` from `git tag --list` on the local checkout; `cargo xtask bump` then runs `git fetch --tags origin` (xtask/src/bump.rs:356). Every tag that existed on origin but not locally shows up in `comm -13 before after` and is passed to scripts/release-binaries.sh, which rebuilds it and `gh release upload --clobber`s its assets, writing a new SHA256SUMS for an already-published release. update-tap.sh guards the tap against going backwards, but the release assets are still replaced and CI time spent.\n\nTrigger: running the bump from a checkout whose tags are stale.\n\nDone when the script runs `git fetch --tags origin` before taking the before snapshot (or diffs against the tag the bump reports), and tests/scripts.rs covers a pre-existing remote tag not being released again.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-19T21:46:58.873808Z","created_by":"michel","updated_at":"2026-09-19T22:36:52.251167Z","closed_at":"2026-09-19T22:36:52.251060Z","close_reason":"Implemented in #81 (335d0f9)","source_repo":"beads-intake-review-bugs","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-review-bugs","compaction_level":0,"original_size":0}
{"id":"acs-znr","title":"Bound dial and the HELLO→WELCOME handshake with a deadline","description":"`dial()` (src/client.rs ~L124-170) reads the ssh pipe with a plain blocking `read` — no poll, no deadline, the signal fd is not watched — and `serve()` uses `timeout = -1` and only starts `Liveness` after the first WELCOME (~L551, L639). ssh's own keepalive is disabled by design (ServerAliveInterval=0) and ConnectTimeout=10 only covers TCP connect. A redial whose connect succeeds but then stalls (half-alive host, firewall that accepts and blackholes, a wedged remote before WELCOME) hangs the client until Ctrl-C. The 10 s dead-link guarantee of DESIGN §5.3 has a hole exactly in the reconnect path. A login file with `printf` and no trailing newline (see the marker bead) triggers the same hang deterministically.\n\nDone when the marker wait and the handshake are covered by a deadline (poll-based, with the signal fd), a timed-out redial returns to backoff, and a test with a silent remote proves it.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-18T15:14:21.272611Z","created_by":"michel","updated_at":"2026-09-18T15:48:09.569464Z","closed_at":"2026-09-18T15:48:09.569363Z","close_reason":"Fixed with regression tests (bugs-client branch)","source_repo":"beads-intake-bughunt","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-bughunt","compaction_level":0,"original_size":0}
{"id":"acs-zpj","title":"Dial the last winning alias entry at once while pings confirm","description":"Alias resolution waits up to reachability_timeout (500 ms default) whenever a higher-ranked entry is down, e.g. a preferred devbox.lan while away from home, and pays it on every connect. Remember the last entry chosen per set of local networks (state under XDG_STATE_HOME) and, on the same networks, dial it immediately while the pings confirm in parallel.\nAlso consider probing the ssh port with a TCP connect instead of ICMP: a truer signal and it works for hosts that drop pings, at the cost of resolving the port (ssh -G, ProxyJump). Touches alias.rs, client.rs resolve_alias, DESIGN §7.3.\n","status":"open","priority":3,"issue_type":"feature","created_at":"2026-09-23T19:29:05.072685Z","created_by":"michel","updated_at":"2026-09-23T19:29:05.072685Z","source_repo":"beads-intake-latency","source_repo_path":"/Users/michel/src/qjam/acs/.claude/worktrees/beads-intake-latency","compaction_level":0,"original_size":0}
